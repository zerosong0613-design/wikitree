# CODEX 마스터 계획서 (CLAUDE.md) v0.5

> 팀의 판단을 **누가·언제·왜 정했는지까지 남는, 기계가 읽는 지식 계층**으로 쌓는 시스템.
> 사람이 읽는 문서이자, Claude Code가 매 세션 처음 읽는 **헌법**이다.
> 작업 전 이 문서를 먼저 읽는다. 구조 원칙과 스키마는 어떤 작업에서도 어기지 않는다.
> 구조를 바꿔야 하면 코드부터 고치지 말고 이 문서를 먼저 고친다.

---

## ★ 이번 판(v0.5)에서 새로 정해진 것

v0.4는 "지도 있는 위키" — 사람이 정한 카테고리 안에서만 AI가 채웠다. **v0.5는 무게중심을 다시 확장한다 — 지식은 계약에서만 나오지 않는다.** 자문·회의·사고·규제·내부규정도 판단의 원천이다. 계약 도구가 아니라 **회사 전체 판단 저장소**가 되려면, 룰 하나에 여러 축(원천·의뢰부서·주제)을 태그로 달고, 위키는 축을 바꿔가며 같은 데이터를 다른 관점으로 본다.

새로 들어온 것:

1. **다축 태그 모델** — 룰 하나가 `path`(판단 축, 정본) + `tags`(원천·의뢰부서·주제)를 갖는다. 원천=계약이면 `contract_view`(계약종류·조항)도. 축을 늘려도 스키마 구조는 안 바뀐다. (4장)
2. **닫힌 축 3 + 열린 축 1** — 판단·원천·의뢰부서는 사람이 정한 목록에서만(원칙 11 확장). 주제는 AI 자유. AI가 닫힌 축을 어기면 코어가 "미분류"로 강제. (4.6장)
3. **위키 상단 축 토글** — 목차 위에 [판단별] [계약서별] [부서별] [원천별]. 같은 데이터를 축만 바꿔 재그룹핑. **판단별 = 집계**(같은 판단 묶어 관철/양보), **계약서별 = 나열**(계약종류>조항 순, 원천=계약만). (10.3)
4. **원천 확장** — 계약 / 자문 / 회의·의사결정 / 사고·분쟁대응 / 규제·법령대응 / 내부규정. 자문·회의도 다 들어옴. (4.6)
5. **`contract_view` AI 자동 채움** — 원천=계약이면 마이닝·검토 추출·[지식 넣기]에서 AI가 계약 종류·조항까지 함께 제안. 사람이 확정. (6장)
6. **[카테고리 설정] 3축 관리** — 판단(기존)·원천·의뢰부서 각 축을 탭으로. 프리셋 3세트. (10.6)

**하위호환:** v0.4 룰의 `path`는 그대로 살아있음(판단 축). `tags`가 없으면 빈 값으로 폴백(원천=null, 의뢰부서=null, 주제=[]). 사용자가 사후에 태그 채움.

이전 판(v0.1~v0.4)의 요지는 문서 말미 **15장(히스토리)** 에 남긴다.

---

## 0. 이 문서의 사용법 (Claude Code에게)

- 매 작업 전 이 파일을 읽는다. 프로젝트 구조는 여기에만 고정돼 있다.
- **구조 원칙(3장)·룰 스키마(4장)는 불가침이다.** 편의로 새 구조·새 필드를 임의로 만들지 않는다. 필요하면 이 문서 수정을 먼저 제안한다.
- 한 번에 전체를 만들지 않는다. 작업은 별도 지시서로 조각 단위로 들어온다. 그 조각만 만들고 완료 기준을 확인하고 멈춘다.
- 기존 코드 구조를 모르겠으면 새로 짓지 말고 이 문서와 현재 파일을 먼저 읽는다.
- **현재 코드(v0.4 조각 6a~6e까지 반영)와 이 문서(v0.5)가 어긋난 지점은 v0.5가 정답이다.** 조각 7a~7f 지시서로 코드를 v0.5에 맞춘다. 특히 다축 태그·축 토글·contract_view·3축 카테고리 설정은 아직 코드에 없다.

---

## 1. 이게 뭐고 왜 만드나

**문제.** 이 회사는 보안 때문에 외부 AI·클라우드·데이터 반출이 막혀 있다. 우리 데이터를 외부 모델에 넘기는 방식의 AX는 영원히 못 한다.

**유일한 답.** 그 제약 안에서 AX를 준비하는 길은 하나다 — 우리 판단을 기계 판독 형태로 우리 안에 쌓아두고, 어떤 AI가 들어오든 그걸 읽게 하는 것.

**위키 한 층 (v0.3~).** 위키가 지식 계층이자 곧 화면.

**지도가 있는 위키 (v0.4).** 카테고리는 사람이 정한다. AI는 그 안에서만 채운다.

**★ 다축 저장소 (v0.5).** 지식은 계약에서만 나오지 않는다. 판단·원천·의뢰부서·주제를 축으로 붙이면 축을 바꿔가며 같은 데이터를 다르게 본다. **인사팀은 노조 자문을, 재무팀은 세무 판단을, 법무는 계약 판단을 — 같은 저장소에 쌓고 부서·주제·원천 어느 각도로든 꺼낸다.**

**한 문장.** "우리가 만든 건 앱이 아니라 **회사 전체 판단 저장소**다. 계약·자문·회의·사고 무엇에서 왔든 판단이 태그를 달고 남고, 사람이 퇴사해도 축을 돌려 찾을 수 있다."

---

## 2. 무게중심 — 누가 쓰느냐

진짜 위키의 신뢰는 내용이 아니라 **편집 역사**에서 나온다. "누가, 언제, 왜 이렇게 썼는가"가 없으면 문서 더미다.

이 시스템의 중심은 AI가 아니라 **저작자**다. **주인 없는 판단은 트리에 넣지 않는다.**

**편집 장벽과 저작자 필수, 양립시키기.**
- 사용자는 **처음 편집 시 이름을 한 번**만 넣는다(설정의 "내 이름", localStorage).
- 이후 모든 액션마다 `authors`·`history`에 **누가·언제·무엇을**이 자동 stamp.
- 이름 없이 편집 시도 → 이름 입력창으로 조용히 유도.
- 사용자는 로그·저작자 기록에 신경 쓰지 않는다. **투명 기록.** 룰 페이지의 `[역사]` 토글에서 확인.

---

## 3. 구조 원칙 (불가침)

1. **코어와 편집/렌즈를 분리한다.** 코어(트리·스키마·저장·로그·md)는 편집 방식·렌즈와 독립.
2. **모든 편집은 같은 룰 스키마(4장)만 뱉는다.**
3. **트리는 저장이 아니라 뷰다.** 룰은 평면 배열로 저장. 사람은 축으로 그룹핑해 본다.
4. **도메인 중립.** 축 목록을 코드에 하드코딩하지 않는다. `codex_categories`(사용자 저장)와 `path`·`tags`(각 룰)에서 온다.
5. **로컬 전용, 데이터 반출 금지.**
6. **원본은 안 남기되 출처는 남긴다.** provenance만 저장.
7. **AI는 초안, 사람이 확정.**
8. **위키가 곧 앱이다.** 별도 프레임 없음.
9. **편집 장벽은 낮게, 편집의 자리는 명확하게.** 룰 페이지 안 EditableField로 5초 편집. 새 룰은 상단 [지식 넣기].
10. **AI 제안은 닫힌 축 안에서만.** 새 축값 발명 금지. 안 맞으면 사용자에게 유도.
11. **닫힌 축은 사람이 먼저 정한다.** `codex_categories`에 저장. AI·모든 입구는 이 안에서만 분류.
12. ★ **지식은 계약에서만 나오지 않는다 (v0.5 신설).** 자문·회의·사고·규제·내부규정도 판단의 원천이다. 룰은 여러 축(판단·원천·의뢰부서·주제) 태그를 갖는다. 닫힌 축(판단·원천·의뢰부서)은 사람이 정한 목록에서만, 열린 축(주제)만 AI 자유. **보기는 "어느 축으로 묶느냐"이며, 새 축이 필요하면 태그 종류만 추가한다(구조 불변).**

---

## 4. 공통 룰 스키마 (하나로 고정)

트리의 원자 단위. 모든 편집 방식이 이 형태로만 만들고, 모든 화면·내보내기가 이 형태를 읽는다. **필드를 임의로 늘리지 않는다.** (★ = v0.2, ✚ = v0.3, ● = v0.4, ▲ = v0.5)

```json
{
  "id": "RULE-0001",
  "path": ["대분류", "중분류", "항목"],           // 판단 축의 정본 저장 위치 (기존 유지, 병합 앵커)
  "judgment": "판단 한 문장",
  "kind": "hard | soft | neu | hold",
  "strength_label": "마지노선 | 협상 여지 | 신규·미확정 | 사람 확인",
  "held": 4, "total": 4, "badge": "관철 4/4",
  "note": "근거·양보 이력 한 줄",
  "summary": "본문 요약 (룰 페이지 요약 카드용)",
  "origin": "룰 페이지 편집 | 새 룰 | 검토 추출 | 계약 마이닝(diff) | 인터뷰 채굴",
  "source": "제3조 등 근거 위치(선택, 없으면 null)",
  "status": "candidate | approved",

  "tags": {                                       // ▲ v0.5 신설 — 판단 외 다축 태그
    "원천": "계약",                                // 닫힘, 단일 값. null 허용(초기·손 편집 시).
    "의뢰부서": "구매·조달",                        // 닫힘, 단일 값. null 허용.
    "주제": ["배상", "NDA"]                        // 열림, 자유 배열. AI가 자유롭게.
  },
  "contract_view": {                              // ▲ v0.5 신설 — 원천="계약"일 때만 채움, 아니면 null
    "contract_type": "NDA(국문)",
    "contract_group": "정보보호 계약",
    "article": "제7조",
    "article_title": "위반시 배상"
  },

  "authors": [ ... ],                             // ★ 이 룰의 주인
  "history": [ ... ],                             // ★ 편집 역사
  "provenance": { ... },                          // ★ 어디서 나왔나 (선택)

  "review_trigger": {
    "keywords": [...], "law_group": null, "test": "..."
  },
  "renew_trigger": "...(선택)",
  "related": ["RULE-0007"],                       // ● v0.4 관련 판단
  "created_at": ""
}
```

**필드 규칙**
- **`path`는 판단 축의 정본이자 저장 앵커다.** 병합은 여전히 path 기준(5장). `tags`는 판단 외 축들만.
- ▲ **`tags.원천`·`tags.의뢰부서`는 닫힌 축.** 값은 반드시 `codex_categories`의 해당 축 목록 안 것. AI가 어겨도 코어가 "미분류"로 강제.
- ▲ **`tags.주제`는 열린 축.** AI 자유·사용자 자유. 검색·연결용.
- ▲ **`contract_view`는 원천="계약"일 때만 채움.** 계약 아니면 `null`. AI가 마이닝·검토 추출·[지식 넣기]에서 자동 제안, 사람 확정.
- **하위호환:** v0.4 룰(`tags` 없음)은 `tags = {원천:null, 의뢰부서:null, 주제:[]}`, `contract_view = null`로 폴백. 사용자가 사후에 채움. `path`는 그대로 판단 축.
- `kind`는 등장 빈도가 아니라 **판단의 성격**.
- `authors`·`history`는 자동 stamp.
- `origin`·`provenance` 기존 규칙 그대로.

---

## 4.5 축 규약 (v0.5 재작성 — 기존 "path 규약" 확장)

### 원칙 (팀·회사 무관하게 통함)
1. **판단 축(`path`)은 성격으로 묶는다.** 문서 종류·계약 종류가 달라도 같은 성격이면 같은 path에 병합돼 두터워진다.
2. **판단 축은 `codex_categories.판단` 안에서만.** AI 자유 발명 금지.
3. **판단 축 대분류 3~7개.** 넘치면 축이 뭉개진 것.
4. **원천 축은 판단의 출처.** 계약·자문·회의·사고·규제·내부규정 등. `path`에 넣지 마라 — 다른 축이다.
5. **의뢰부서 축은 판단 요청자.** 법무·인사·재무 등. 크로스 부서 지식이 쌓이려면 이 축이 필요.
6. **주제 축은 열려 있음.** 검색·연결·태그 클라우드용. AI가 자유롭게, 사용자가 편집.
7. **새 축이 필요하면 태그 종류만 추가.** 구조는 안 바뀐다 — 다른 축을 늘려도 코어는 그대로.

### AI 축 제안 (v0.5 확장)

편집 방식(6.1~6.4)에서 판단문이 입력되면 AI가 **`codex_categories`(3축) + 열린 주제 축을 컨텍스트로 받아** 판단 path 3개 + 원천 1 + 의뢰부서 1 + 주제 2~4를 제안한다:
- **판단(path)·원천·의뢰부서는 반드시 목록 안에서 고른다.** 새 값 발명 금지.
- **주제(`tags.주제`)만 자유롭게** 2~4개.
- **원천="계약"이면 `contract_view` 자동 채움** — 계약종류·계약군·조항 함께 제안.
- 어디에도 안 맞으면 그 축을 "미분류" 또는 null로.
- 사용자는 accept 또는 override.

### 시작 발판 — 3축 프리셋 (4.6 참고)

각 닫힌 축에 프리셋 시드. 편집·삭제 가능(원칙 11).

---

## 4.6 카테고리 저장소 (v0.5 확장 — 3축)

**localStorage 키 `codex_categories`.** v0.5 v2 형식:

```json
{
  "version": 2,
  "판단": [
    { "name": "정보·보안", "subs": ["비밀유지", "목적 외 이용", "데이터 활용"] },
    { "name": "책임·분쟁", "subs": ["손해배상", "면책", "관할"] },
    { "name": "계약·거래", "subs": ["대금·정산", "IP·라이선싱"] },
    { "name": "인사·노무", "subs": ["채용", "이별", "노조"] },
    { "name": "규제·컴플라이언스", "subs": ["개인정보", "공정거래"] },
    { "name": "지식재산", "subs": ["저작권", "특허"] },
    { "name": "조직·지배구조", "subs": ["이사회", "M&A"] }
  ],
  "원천": [
    { "name": "계약" },
    { "name": "자문" },
    { "name": "회의·의사결정" },
    { "name": "사고·분쟁대응" },
    { "name": "규제·법령대응" },
    { "name": "내부규정" }
  ],
  "의뢰부서": [
    { "name": "법무" },
    { "name": "인사" },
    { "name": "재무·세무" },
    { "name": "구매·조달" },
    { "name": "영업" },
    { "name": "연구개발" },
    { "name": "IT" },
    { "name": "경영지원" }
  ]
}
```

**규칙:**
- 코드에 하드코딩하지 않는다. 이 파일은 **사용자 소유.** AI·입구는 **읽기만.**
- 각 축 별로 [카테고리 설정]에서 사람이 CRUD.
- **미분류**는 각 축의 특수 값 (저장소엔 명시 안 함, 코어가 자동 처리).
- **프리셋 3종** — 판단(v0.4 유지)·원천·의뢰부서. 시드일 뿐, 편집·삭제 자유.
- **판단 축만 subs를 가짐.** 원천·의뢰부서는 단일 값(subs=[]).

**v1 → v2 마이그레이션:**
```
v1: { version: 1, categories: [...] }
→ v2: { version: 2, 판단: [...v1.categories], 원천: [], 의뢰부서: [] }
```

원천·의뢰부서 빈 상태로 시작. 사용자가 프리셋 불러오거나 직접 추가.

**AI 프롬프트 개조 (필수):**
모든 AI 호출(계약 마이닝·검토 추출·AI 축 제안)이 프롬프트에 3축 목록을 주입:
> "path의 [대분류, 중분류]는 반드시 [판단 축]에서, tags.원천은 [원천 축]에서, tags.의뢰부서는 [의뢰부서 축]에서 고른다. 목록 밖 값 발명 금지. 주제는 자유. 원천이 '계약'이면 contract_view도 채운다."

**응답 검증 (코어 강제):**
- `path[0]`이 `판단` 목록에 없으면 → `["미분류", ...path.slice(1)]`.
- `tags.원천`이 `원천` 목록에 없으면 → `null` (또는 "미분류").
- `tags.의뢰부서`가 `의뢰부서` 목록에 없으면 → `null`.

---

## 5. 저장·병합·로그 (코어)

- localStorage 키 `codex_tree`에 룰 **평면 배열**로 저장. 새로고침해도 유지.
- **병합:** 같은 `path`의 룰이 있으면 병합 — `held/total` 합산, `note`·`badge`·`strength_label` 최신 갱신, `history`·`authors` 누적(덮어쓰지 않는다). 없으면 추가.
- ▲ **v0.5 태그 병합:** 같은 path 룰이 있으면 `tags`는 각 축별로 병합:
  - `tags.원천`·`tags.의뢰부서`(단일 값): 두 룰이 다르면 최신 값 유지 or `null`(사람 확인 필요 신호). 기본: 최신 유지.
  - `tags.주제`(배열): 합집합.
  - `contract_view`: 새 값 우선 · 없으면 기존 유지.
- 가지(대분류·중분류)는 저장하지 않는다. `path`에서 그때그때 생성.
- **편집 로그 자동화.** 모든 편집이 저장 시 `history`에 `{who, action, at}` 자동 append. 액션 어휘 확장(v0.5): 기존 + `원천 변경`·`의뢰부서 변경`·`주제 추가`·`주제 제거`·`contract_view 수정`.
- ● **v0.4 미분류 처리:** `path[0]`이 카테고리에 없으면 코어가 `"미분류"`로 강제.
- ▲ **v0.5 축 검증:** `tags.원천`·`tags.의뢰부서`가 각 축 목록에 없으면 `null`로 강제(주제는 검증 안 함).

---

## 6. 편집 방식 (v0.5 유지 + 태그 편집 추가)

### 6.1 룰 페이지 안 편집
룰 페이지의 각 필드(판단문·note·kind·summary·path·**태그**)가 마우스오버 → 편집.
- ▲ **v0.5 신규 편집 대상:** `tags.원천`(드롭다운, 원천 목록에서), `tags.의뢰부서`(드롭다운, 부서 목록에서), `tags.주제`(pills + 추가/제거), `contract_view`(원천=계약일 때만 폼).
- 저장 시 `stampAuthoring` + `history` 자동 stamp.
- 조용히(플래시 없음).

### 6.2 새 룰 만들기 = [지식 넣기]
상단 nav → 룰 페이지 새 룰 모드.
- ▲ **v0.5 필드 확장:** 판단(대·중·항목) + 원천 드롭다운 + 의뢰부서 드롭다운 + 주제 pills + (원천=계약이면) contract_view 폼 + 판단문 + kind + note + summary.
- AI 축 제안 버튼: 판단문 입력 후 → AI가 4축 모두 제안 → 사람이 accept/override.
- 저장 → 트리 push, 저작자·history 자동, `origin="새 룰"`.

### 6.3 검토 추출 4단계 (MoreDrawer, 파워유저용)
v0.4 유지. ▲ v0.5 프롬프트에 3축 목록 주입 + `contract_view` 자동 채우기.

### 6.4 계약 마이닝 대량 (MoreDrawer, 초기 부트스트랩)
v0.4 유지. ▲ v0.5 프롬프트에 3축 목록 주입. 원천은 "계약" 고정, `contract_view` 자동 채움.

### 6.5 인터뷰 채굴 (자리만)

---

## 7. 렌즈·찾기

### 7.1 검토 렌즈 (MoreDrawer, 나중)

### 7.2 AI 찾기 (채팅 버블 흐름, v0.4 유지)
- 우측 대화 UI. 사용자 질문 → AI 답 (근거 pill · hold 배너).
- 미탐 우선. 인용하지 창작 안 함.
- ▲ v0.5: 근거 pill에 원천·부서도 표시 가능(선택 · 조각 7f).

---

## 8. AI의 두 역할 (중심은 언제나 사람)

- **편집 도울 때:** AI가 축 제안, 사람이 확정, **그 사람이 저작자.**
- **찾을 때:** 사람이 묻고, AI가 트리에서 답하고, **저작자·원천을 밝힌다.**

---

## 9. md 내보내기 (사람+기계 한 장)

'위키 md 저장' → `codex_wiki_YYYY-MM-DD.md`.
- `#` 제목 + 요약(총 규칙 수 / 분포 / **원천별·부서별 카운트**).
- 판단별 그룹핑 (기본) → `##` 대분류 → `###` 중분류 → 룰.
- 각 룰 아래에 판단·강도·근거·**태그(원천/의뢰부서/주제)**·(있으면)contract_view·origin·status·저작자·출처·재검토 트리거.
- **말미 `## 기계 판독용 데이터`에 ```json으로 전체 룰 배열 + `codex_categories`.**

---

## 10. 화면 — v0.5 (v0.4 유지 + 축 토글 추가)

### 10.1 상단 바
- 로고 · **[판단 찾기]** · **[카테고리 설정]** · **[지식 넣기]** · 검색바 · ⚙ 설정 · [더보기]·[md 저장]·[비우기]·codex_tree count·모델.

### 10.2 3판 그리드
좌 25% 목차 / 중 50% 룰 페이지 / 우 25% AI 찾기.

### 10.3 목차 (좌) — ▲ v0.5 축 토글 추가

**상단 (신규):**
- 목차 라벨 + 규칙 수 + [카테고리 설정] 버튼 (기존).
- ★ **축 토글:** `[판단별] [계약서별] [부서별] [원천별]` — 기본 `판단별`.
- 검색바(옵션).

**본문:**
- 축 토글에 따라 그룹핑 완전히 달라짐.

  **판단별 (기본 · 집계 뷰)** — `path`로 그룹핑 (v0.4 그대로).
  - 같은 판단은 하나로 묶여 관철/양보 통계.
  - 대분류 헤더 sticky · 기본 접힘 · 강도 dot은 잎에만.
  - 미분류 맨 아래.

  **계약서별 (나열 뷰)** — `tags.원천="계약"`인 룰만, `contract_view.contract_group > contract_type > article` 순으로.
  - 같은 판단이 여러 계약에 있어도 각 계약 조항으로 따로 나열(집계 아님).
  - 계약 원천 룰 없으면: "아직 계약에서 추출한 판단이 없어요."

  **부서별** — `tags.의뢰부서`로 그룹핑.
  - 각 부서 아래 판단문 나열.
  - 부서=null인 룰은 "미지정" 섹션.

  **원천별** — `tags.원천`으로 그룹핑.
  - 계약/자문/회의/사고/규제/내부규정별 판단 나열.
  - 원천=null은 "미지정".

- **편집 자리 없음** (목차는 지도, 편집은 룰 페이지 안).

### 10.4 룰 페이지 (중) — v0.4 유지 + ▲ 태그 표시

v0.4 구조 그대로 + 신규 카드/필드:
- **태그 카드** — 원천 · 의뢰부서 · 주제 pills. 각각 마우스오버 편집.
- **contract_view 카드** — 원천=계약일 때만. 계약종류·계약군·조항·조항제목. 편집 가능.
- 나머지(breadcrumb·판단문·강도·요약·hold·재검토·관련 판단·[역사]) 유지.

### 10.5 AI 찾기 (우, v0.4 채팅 흐름 유지)
근거 pill에 원천·부서 표시 확장은 조각 7f 옵션.

### 10.6 [카테고리 설정] 모달 (▲ v0.5 3축 관리)
- **상단 탭: [판단] [원천] [의뢰부서]** — 각 축 CRUD.
- 각 탭 안:
  - 판단 탭: 대분류 · 중분류(subs) · 프리셋 "법무 기본" 시드 (v0.4 그대로).
  - 원천 탭: 단일 대분류 리스트 (subs 없음) · 프리셋 시드 (계약/자문/…).
  - 의뢰부서 탭: 단일 대분류 리스트 · 프리셋 시드 (법무/인사/…).
- 저장 → v2 형식으로 `codex_categories` 갱신 + 트리 재검증.

### 10.7 [지식 넣기] = 룰 페이지 새 룰 모드 (v0.4 + ▲ 3축)
- 판단 대·중·항목 (기존).
- ▲ **원천 드롭다운** (원천 목록에서 · 필수 or 선택).
- ▲ **의뢰부서 드롭다운** (부서 목록에서 · 선택).
- ▲ **주제 pills** (자유 추가, 선택).
- ▲ **contract_view 폼** — 원천="계약" 선택 시만 등장. 계약종류·계약군·조항·조항제목.
- 판단문 · kind · note · summary · [AI에 축 제안] · [심기].

### 10.8 편집 UX 규약 (v0.4 유지)

### 10.9 MoreDrawer (v0.4 유지 · 계약 마이닝·검토 추출·검토 렌즈·인터뷰)

---

## 11. 색·비주얼 규약

- 배경 `#E9EBE6` · 패널 `#F5F6F3` · 잉크 `#14171C`.
- 강도색: hard `#9E3524` · soft `#C8871F` · neu `#2FA97F` · hold `#8A9086`.
- 폰트: Pretendard + IBM Plex Mono. 테두리 2px.
- 코디(두더지·헤드램프): 친근한 존댓말, 은유 라벨 금지, "카테고리 설정"·"태그" 등 직관적 표현으로 통일.

---

## 12. 지금 범위 vs 나중 범위 (v0.5 재배열)

**지금 — 조각 7a~7f 순차:**
- **7a**: 스키마 다축 확장(`tags`·`contract_view`) + `codex_categories` v1→v2 마이그레이션 + 카테고리 3축 + `store.js` 다축 검증. 하위호환.
- **7b**: [카테고리 설정] 3축 탭 UI (판단·원천·의뢰부서). 각 축별 프리셋.
- **7c**: 위키 목차 상단 축 토글 + `groupByAxis` 재구성 (판단별 집계·계약서별 나열·부서별·원천별).
- **7d**: 룰 페이지 태그 카드 + contract_view 카드 + 편집.
- **7e**: 입구 프롬프트 3축 주입 + contract_view 자동 채움 (마이닝·검토 추출·path/축 제안). NewRuleForm에 3축 드롭다운 + contract_view 폼.
- **7f (선택)**: AI 찾기 근거 pill 다축 표시.

**선택 (되면 좋지만 데모 필수 아님):**
- 검토 추출 4단계 실체 (MoreDrawer)
- 검토 렌즈 실체
- md 내보내기에 다축·카테고리 반영

**나중 (자리만):**
- 인터뷰 채굴 · 파일 OCR·hwp · SSO 저작자 · Notion 통합
- 팀 유형별 프리셋 (인사·개발·마케팅) · path 이동 자유도
- AI 찾기 대화 지속 저장 · 팔로업 컨텍스트

> **좁게, 그러나 진짜로.** v0.5의 좁고 진짜는 **다축 태그 + 축 토글 + 3축 카테고리 관리 + 입구 프롬프트 확장.**

---

## 13. 완료 기준 (v0.5)

1. **다축 스키마** — `path`(판단 정본) + `tags`(원천·의뢰부서·주제) + `contract_view`(계약일 때). v0.4 룰 로드해도 폴백으로 빈 태그.
2. **카테고리 저장소 v2** — 3축(판단·원천·의뢰부서) 저장. v1 → v2 자동 마이그레이션.
3. **[카테고리 설정]** — 3축 탭. 각 축 CRUD + 프리셋 시드.
4. **위키 축 토글** — [판단별]/[계약서별]/[부서별]/[원천별] 목차 상단. 클릭 시 그룹핑 즉시 재계산(데이터 불변).
5. **판단별 = 집계**(같은 path 묶음 통계), **계약서별 = 나열**(원천=계약만, contract_group>type>article 순), 부서별·원천별 = 그룹핑.
6. **룰 페이지** — 태그 카드 + contract_view 카드. 태그 마우스오버 편집.
7. **[지식 넣기]** — 판단(3단) + 원천/의뢰부서 드롭다운 + 주제 pills + (계약이면) contract_view 폼.
8. **입구 프롬프트** — 계약 마이닝·검토 추출·AI 축 제안이 3축 목록 주입 + contract_view 자동 채움.
9. **자문·회의 판단** — 계약이 아닌 판단(예: "노조 자문 · 원천=자문 · 부서=인사")을 넣으면 판단별·부서별·원천별엔 뜨고 계약서별엔 안 뜬다.
10. **편집 액션마다** `history` 자동 append. 다축 편집도 (원천 변경/의뢰부서 변경/주제 추가 등).
11. `npm run build` 성공.

---

## 14. 불가침 요약

- **위키가 곧 앱이다.**
- **카테고리는 사람이 먼저 정한다** (판단·원천·의뢰부서 3축).
- ▲ **지식은 계약에서만 나오지 않는다.** 자문·회의·사고·규제도 원천. 룰은 다축 태그를 갖는다.
- ▲ **보기는 축을 바꿔가며.** 데이터는 하나, 축은 여럿.
- **목차는 지도, 편집은 룰 페이지 안에서만.**
- 편집 장벽은 낮게 (룰 페이지 안 EditableField로 5초).
- 코어 ↔ 편집 방식/렌즈 분리.
- 모든 편집 방식은 4장 스키마만 뱉는다.
- 트리는 뷰, 저장은 평면 배열.
- **주인 없는 판단은 트리에 없다.** authors·history·provenance는 필수 축.
- ▲ **닫힌 축은 사람이 목록 관리, 열린 축(주제)만 AI 자유.**
- AI는 초안, 사람이 확정.
- 데이터 반출·키 커밋 금지, 로컬 전용.
- 찾기·검토는 확실치 않으면 보류(미탐 우선).
- md 말미 JSON 블록 필수.
- **은유 라벨 금지.** 직관적 표현으로.

---

## 15. 이전 판 히스토리

**v0.1 — 채굴 도구.** AI가 계약 채굴해 트리에 쌓는 도구. 저작·출처 개념 없음.

**v0.2 — 저작자 축.** `authors`·`history`·`provenance` 신설. 검토 추출 4단계 신설. AI 찾기 신설.

**v0.3 — 위키 한 층.** 이분법 폐기, 위키가 곧 앱. "지식 넣기" 프레임 폐기, 인라인 편집. 검토 추출 격하. 저작자 자동 삽입. path 규약(4.5) 신설, AI 자동 제안.

**v0.4 — 지도 있는 위키.** taxonomy AI 자유 발명 폐기, `codex_categories`로 카테고리를 사람이 먼저 정함. 목차 옆 `+` 편집 폐기, 편집은 룰 페이지 안 EditableField로만. [지식 넣기] 상단 nav 승격. AI 찾기 채팅 버블 흐름. 룰 페이지에 breadcrumb·credit·요약·hold·재검토·관련판단 구조. `summary`·`related` 신규 필드.

**v0.5 — 다축 저장소.** 지식은 계약에서만 나오지 않는다. 룰이 `path`(판단 축) + `tags`(원천·의뢰부서·주제) + `contract_view`(계약일 때)를 갖는다. 닫힌 축 3(판단·원천·의뢰부서) + 열린 축 1(주제). 위키 목차 상단 축 토글로 같은 데이터를 다른 관점으로. 판단별=집계·계약서별=나열. `codex_categories` v2로 3축 관리. AI가 4축 모두 자동 제안. `path` 필드는 판단 축의 정본 저장 위치로 유지(하위호환·병합 앵커).

---

*v0.5 — 무게중심을 "다축 저장소"로 확장한 판. 이 문서가 헌법이다. 구조를 바꿔야 하면 이 문서를 먼저 고친다.*
