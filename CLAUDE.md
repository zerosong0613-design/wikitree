# CODEX 마스터 계획서 (CLAUDE.md) v0.4

> 팀의 판단을 **누가·언제·왜 정했는지까지 남는, 기계가 읽는 지식 계층**으로 쌓는 시스템.
> 사람이 읽는 문서이자, Claude Code가 매 세션 처음 읽는 **헌법**이다.
> 작업 전 이 문서를 먼저 읽는다. 구조 원칙과 스키마는 어떤 작업에서도 어기지 않는다.
> 구조를 바꿔야 하면 코드부터 고치지 말고 이 문서를 먼저 고친다.

---

## ★ 이번 판(v0.4)에서 새로 정해진 것

v0.3는 "위키가 곧 앱, 편집은 아무나 툭"이었다. v0.4는 무게중심을 다시 옮긴다 — **카테고리는 사람이 정하고 AI는 그 안에서만 채운다.**

v0.3까지 taxonomy를 AI가 자유 발명했다. 편해 보였지만 트리가 자랄수록 지도가 흔들려 찾기가 어려워졌다. 위키는 지도(카테고리)가 있어야 나뉘고 찾아진다(나무위키 원리). "편집 장벽 최소"는 유지하되 **편집의 자리를 명확히** 한다 — 목차는 순수 지도로, 편집은 룰 페이지 안에서만. 새 판단은 상단 [지식 넣기]로 명확한 진입점.

새로 들어온 것:

1. **카테고리는 사람이 먼저 정한다** — 대·중분류는 `codex_categories`(localStorage)에 사용자가 저장. AI·입구는 이 카테고리 안에서만 분류, 안 맞으면 "미분류". 카테고리 확장·수정도 사람. (3장 원칙 11 · 4.6장)
2. **목차는 지도, 편집은 룰 페이지 안에서만** — 목차의 `+` 인라인 편집(v0.3 조각3) 폐기. 목차는 검색·찾기용 지도로 순수. 편집은 룰 페이지 안 EditableField(판단문·note·kind)로만. (3장-9 재정의)
3. **새 룰 만들기 = 상단 [지식 넣기]** — 상단 nav 클릭 → 룰 페이지가 **새 룰 편집 모드**로 열림. 카테고리 드롭다운 + 판단문 + kind. MoreDrawer의 수동 폼 항목은 여기로 흡수·제거. (6.2 재정의)
4. **AI path 제안은 카테고리 제약 안에서만** — 새 대분류 발명 금지. 카테고리 부족 시 [카테고리 설정] 유도. (3장-10 재정의 · 4.5장)
5. **AI 찾기는 채팅 버블 흐름** — user/ai 대화 형태. 근거·저작자는 답 안에 pill로. 근거 없으면 회색 보류 배너. (7.2 재작성)
6. **목차 개편** — 기본 접힘, 대분류 헤더 sticky, 강도 dot은 잎(항목)에만, 미분류 맨 아래 고정. (10.3)
7. **룰 페이지 개편** — breadcrumb · credit(누가·어디서·언제 한 줄) · 요약 카드 · hold 안내 카드 · 재검토 카드 · 관련 판단 pills. (10.4)

이전 판(v0.1·v0.2·v0.3)의 요지는 문서 말미 **15장(히스토리)** 에 남긴다.

---

## 0. 이 문서의 사용법 (Claude Code에게)

- 매 작업 전 이 파일을 읽는다. 프로젝트 구조는 여기에만 고정돼 있다.
- **구조 원칙(3장)·룰 스키마(4장)는 불가침이다.** 편의로 새 구조·새 필드를 임의로 만들지 않는다. 필요하면 이 문서 수정을 먼저 제안한다.
- 한 번에 전체를 만들지 않는다. 작업은 별도 지시서로 조각 단위로 들어온다. 그 조각만 만들고 완료 기준을 확인하고 멈춘다.
- 기존 코드 구조를 모르겠으면 새로 짓지 말고 이 문서와 현재 파일을 먼저 읽는다.
- **현재 코드(v0.3 조각 1~5까지 반영)와 이 문서(v0.4)가 어긋난 지점은 v0.4가 정답이다.** 조각 6a~6e 지시서로 코드를 v0.4에 맞춘다. 특히 카테고리 저장소·목차 개편·룰 페이지 개편·[지식 넣기] 승격·입구 프롬프트 카테고리 제약·AI 찾기 채팅 UI는 아직 코드에 없다.

---

## 1. 이게 뭐고 왜 만드나

**문제.** 이 회사는 보안 때문에 외부 AI·클라우드·데이터 반출이 막혀 있다. 우리 데이터를 외부 모델에 넘기는 방식의 AX는 영원히 못 한다.

**유일한 답.** 그 제약 안에서 AX를 준비하는 길은 하나다 — 우리 판단을 기계 판독 형태로 우리 안에 쌓아두고, 어떤 AI가 들어오든 그걸 읽게 하는 것. 모델은 바뀐다. 바뀌지 않고 남는 건 우리 지식이다.

**위키 한 층 (v0.3 이후 유지).** "아래층=지식·위층=AI 인터페이스" 이분법은 v0.3에서 폐기했다. **위키가 지식 계층이자 곧 화면 그 자체.** AI는 그 안에서 편집을 돕고 검색을 대신하는 도우미.

**지도가 있는 위키 (v0.4).** 위키는 카테고리가 있어야 지도가 되고, 지도가 있어야 찾아진다. AI가 매번 카테고리를 발명하면 트리는 늘 흔들린다. **카테고리는 사람이 정한다 — 한 번, 먼저.** 이후 모든 AI 도우미는 이 카테고리 안에서만 판단을 분류·제안·답변한다.

**한 문장.** "우리가 만든 건 앱이 아니라 **지도 있는 위키**다. 사람이 퇴사해도 판단과 그 근거가 남고, 카테고리가 흔들리지 않아 찾아진다."

---

## 2. 무게중심 — 누가 쓰느냐

진짜 위키(위키피디아·나무위키)의 신뢰는 내용이 아니라 **편집 역사**에서 나온다. "누가, 언제, 왜 이렇게 썼는가"가 없으면 문서 더미다. 법무 판단은 특히 그렇다 — "이 배상 상한 방침은 2026년 3월 김변호사가 이 판례 때문에 정했다"가 판단 자체만큼 중요하다. 책임 소재가 있어야 기준이지, 없으면 참고자료다.

그래서 이 시스템의 중심은 AI가 아니라 **저작자**다. **주인 없는 판단은 트리에 넣지 않는다.**

**편집 장벽과 저작자 필수, 양립시키기.**
- 사용자는 **처음 편집 시 이름을 한 번**만 넣는다(설정의 "내 이름", localStorage).
- 이후 편집·수정·승인·삭제 등 **모든 액션마다 `authors`·`history`에 누가·언제·무엇을이 자동으로 박힌다.**
- 이름 없이 편집 시도 → 이름 입력창으로 조용히 유도(막지 않되 필수).
- 사용자는 로그·저작자 기록에 신경 쓰지 않는다. **투명 기록.** 룰 페이지의 `[역사]` 토글과 '최근 변경'에서 볼 수 있다(본문 읽기 화면엔 노출하지 않는다 — 10.4).

---

## 3. 구조 원칙 (불가침)

1. **코어와 편집/렌즈를 분리한다.** 코어(트리·스키마·저장·로그·md)는 편집 방식·렌즈와 독립. 편집 방식을 더하거나 고쳐도 **코어 코드는 안 바뀐다.**
2. **모든 편집은 같은 룰 스키마(4장)만 뱉는다.** 룰 페이지 편집이든, [지식 넣기]든, 검토 추출 4단계든, 트리에 꽂히는 건 항상 같은 모양.
3. **트리는 저장이 아니라 뷰다.** 룰은 평면 배열로 저장하고 `path`로 트리를 그린다. 사람은 나무로, 기계는 배열로 읽는다.
4. **도메인 중립.** 가지 이름을 코드에 하드코딩하지 않는다. `codex_categories`(사용자 저장)와 `path`(각 룰)에서 온다. "어느 팀에나"는 이 원칙과 원칙 11이 함께 실현한다.
5. **로컬 전용, 데이터 반출 금지.** 지식은 localStorage와 로컬 파일에만. 외부 호스팅·키 커밋 금지.
6. **원본은 안 남기되 출처는 남긴다.** 계약서 원본 파일은 저장하지 않는다. 그러나 "이 룰이 어느 계약에서 누가 뽑았다"는 출처 메타데이터(provenance)는 저장한다.
7. **AI는 초안, 사람이 확정.** AI는 판단을 추출·추천할 수 있으나, "일반 기준 vs 이 건만의 특수 예외"의 최종 판단과 트리 등재는 반드시 사람이 한다.
8. **위키가 곧 앱이다 (v0.3 신설).** "아래층 지식 + 위층 AI 인터페이스" 이분법 폐기. 위키가 지식이자 곧 화면. AI는 위키 안의 도우미(검색·편집 보조).
9. ✚ **편집 장벽은 낮게, 편집의 자리는 명확하게 (v0.3 신설 · v0.4 재정의).** 편집이 무거우면 위키가 못 자란다. **룰 페이지 안 EditableField로 5초 편집은 유지.** 다만 편집의 자리는 목차가 아니라 룰 페이지 내부다 — v0.3의 "목차 옆 `+` 툭" 방식은 목차의 지도성을 흐리므로 폐기. 새 룰 만들기는 상단 [지식 넣기]로 명확한 진입점.
10. ✚ **AI path 제안은 카테고리 안에서 (v0.3 신설 · v0.4 재정의).** 제안은 **`codex_categories` 안에서만.** 새 대분류 발명 금지 — 안 맞으면 [카테고리 설정]으로 사용자 유도. AI가 taxonomy 확장을 결정하지 않는다.
11. ★ **카테고리는 사람이 먼저 정한다 (v0.4 신설).** 대·중분류는 코드 하드코딩 아님(4번, 도메인 중립) · AI 자유 생성도 아님. **사용자가 `codex_categories`에 저장.** AI·모든 입구는 이 안에서만 분류. 안 맞으면 "미분류". 카테고리 확장·수정도 사람. **이것이 "정리를 위한 정리"와 "찾아지는 지도"를 가르는 선이며, 원칙 4(도메인 중립)와 충돌하지 않는다** — 하드코딩은 없지만 사용자 결정은 있다.

---

## 4. 공통 룰 스키마 (하나로 고정)

트리의 원자 단위. 모든 편집 방식이 이 형태로만 만들고, 모든 화면·내보내기가 이 형태를 읽는다. **필드를 임의로 늘리지 않는다.** (★ = v0.2 신규, ✚ = v0.3 정책 변경, ● = v0.4 정책 변경)

```json
{
  "id": "RULE-0001",
  "path": ["대분류", "중분류", "항목"],
  "judgment": "판단 한 문장",
  "kind": "hard | soft | neu | hold",
  "strength_label": "마지노선 | 협상 여지 | 신규·미확정 | 사람 확인",
  "held": 4,
  "total": 4,
  "badge": "관철 4/4",
  "note": "근거·양보 이력 한 줄",
  "summary": "본문 요약 (수 줄, 룰 페이지 요약 카드용, v0.4)",  // ● v0.4 신규 필드
  "origin": "룰 페이지 편집 | 새 룰 | 검토 추출 | 계약 마이닝(diff) | 인터뷰 채굴",  // ● v0.4: 값 재편
  "source": "제3조 등 근거 위치(선택, 없으면 null)",
  "status": "candidate | approved",

  "authors": [                                         // ★ 이 룰의 주인(현재 상태)
    {"name": "김변호사", "role": "편집", "at": "2026-08-02"}
  ],
  "history": [                                         // ★ 편집 역사(모든 액션 자동 누적)
    {"who": "김변호사", "action": "새 룰 등재", "at": "2026-08-02T14:03:12Z"}
  ],
  "provenance": {                                      // ★ 어디서 나왔나 (선택 — 손 입력은 null)
    "source_contract": "SK케미칼-한국콜마 위수탁계약 (2026-07)",
    "reviewed_by": "김변호사",
    "extracted_at": "2026-08-02",
    "source_article": "제3조",
    "extraction_method": "검토 추출 | 계약 마이닝 | 수동 | 인터뷰"
  },

  "review_trigger": {
    "keywords": ["배상", "손해", "cap", "한도"],
    "law_group": "상법 배상책임(선택)",
    "test": "입력에 배상 상한 조항이 있는가"
  },
  "renew_trigger": "상법 배상 조항 개정(선택)",
  "related": ["RULE-0007", "RULE-0011"],               // ● v0.4 신규: 관련 판단(룰 페이지 하단 pills)
  "created_at": ""
}
```

**필드 규칙**
- `kind`는 등장 빈도가 아니라 **판단의 성격**. hard=한 번도 양보 안 함, soft=양보 이력 있음, neu=표본 적음/신규, hold=갈리거나 기준 없음(억지 규칙화 금지).
- `authors`는 현재 주인, `history`는 전체 이력. **모든 편집 액션은 `history`에 자동 append.** "누가"가 빠진 로그는 로그가 아니다.
- `provenance`는 계약명·시점만. 계약 **내용**은 저장하지 않는다. 손 편집(룰 페이지·새 룰)은 `null` 허용.
- 채굴/입력 직후 `status`는 `candidate`. 사람이 승인하면 `approved`.
- ● **v0.4 신규 필드:**
  - `summary`: 룰 페이지 "요약" 카드용 본문 (수 줄). 선택. 없으면 note만 보임.
  - `related`: 관련 룰 id 배열. 룰 페이지 하단 관련 판단 pills.
- ● **v0.4 `origin` 값 재편:** "인라인 편집"·"수동 폼" 개념 통합 → **"룰 페이지 편집"**(기존 룰 필드 편집) · **"새 룰"**(상단 [지식 넣기]로 새로 등재). 기존 값은 그대로 통과(호환).

---

## 4.5 path 규약 (v0.3 신설 · v0.4 재작성)

path는 트리의 골격이다. **v0.4에서 사람이 미리 정한 `codex_categories`(4.6장) 안에서만 결정된다.** 원칙과 AI 제안 규약은 아래.

### 원칙 (팀·회사 무관하게 통함)
1. **성격으로 묶는다.** 판단이 걸리는 성격이 같으면 같은 path. 문서 종류·계약 종류가 달라도 같은 노드에 병합돼 두터워진다(5장 병합 규칙).
2. **문서 종류·거래처는 provenance에.** path에 넣지 마라.
3. **대분류 3~7개 유지.** 넘치면 축이 뭉개진 것.
4. **한 대분류에 hold가 절반 넘으면 재분류 신호.** 그 축이 실무 판단 성격과 안 맞다는 뜻.
5. **path는 이동 가능해야 한다** (미래 조각). taxonomy는 성장하며 변한다.
6. ● **path는 `codex_categories` 안에서만 (v0.4).** AI·입구가 자유 발명 금지. 안 맞으면 `path[0]="미분류"`, 사람이 나중에 정리하거나 [카테고리 설정]에서 카테고리를 늘린다.

### AI path 제안 (v0.4 재정의)

편집 방식(6.1~6.4)에서 판단문이 입력되면 AI가 **`codex_categories`를 컨텍스트로 받아** 후보 3개를 제안한다:
- **반드시 기존 대분류·중분류 중에서** path[0]·path[1]을 고른다.
- 항목(path[2])만 판단문의 성격으로 새로 만든다.
- 어디에도 안 맞으면 답에 "이 판단은 기존 카테고리에 안 맞아요. [카테고리 설정]에서 새 축을 만드세요"라고 안내.
- 새 대분류·중분류 발명 절대 금지 (원칙 10·11).
- 사용자는 제안을 accept 또는 override. override는 그대로 저장돼 다음 제안에 반영.

**이 기능은 원칙 11의 실현** — 카테고리가 흔들리지 않으면서도 항목 채우기는 자동화.

### 시작 발판은 [카테고리 설정] 프리셋으로 (v0.4)
v0.3의 "법무팀 시작 발판(6분류)"은 이제 **[카테고리 설정] 모달의 "법무 기본 카테고리 불러오기" 프리셋**으로 실체화(4.6). 시드일 뿐 편집·삭제·확장 가능(원칙 11).

---

## 4.6 카테고리 저장소 (v0.4 신설)

**localStorage 키 `codex_categories`.** 형식:

```json
{
  "version": 1,
  "categories": [
    { "name": "정보통제", "subs": ["비밀유지", "목적 외 이용", "데이터 활용"] },
    { "name": "책임·분쟁", "subs": ["손해배상", "면책", "관할"] },
    { "name": "절차·기한", "subs": ["통지", "갱신"] },
    { "name": "규제 대응", "subs": ["개인정보", "공정거래"] },
    { "name": "계약 일반", "subs": ["서명", "수정"] },
    { "name": "분쟁 해결", "subs": ["중재", "화해"] }
  ]
}
```

**규칙:**
- 코드에 하드코딩하지 않는다. 이 파일은 **사용자 소유.** AI·입구는 **읽기만.**
- 카테고리 추가·수정·삭제·순서변경은 [카테고리 설정] 화면에서 사람이 한다.
- **미분류**는 특수 카테고리 (저장소엔 명시 안 함, 코어에서 자동 처리). 카테고리에 안 맞는 룰이 여기 들어감. 목차 맨 아래 고정.
- **프리셋 "법무 기본 카테고리 불러오기"** — 위 6분류를 시드로 심는 버튼. 이후 편집 가능. 프리셋은 예시일 뿐 하드코딩 아님(원칙 4 준수).

**AI 프롬프트 개조 (필수):**
모든 AI 호출(계약 마이닝·검토 추출·AI path 제안)이 프롬프트에 `codex_categories`를 주입하고:
> "path의 [대분류, 중분류]는 반드시 아래 카테고리에서 고른다. 새 카테고리를 만들지 마라. 어디에도 맞지 않으면 대분류를 '미분류'로 두고 이유를 note에 남긴다."

**응답 검증 (코어 강제):**
반환된 룰의 `path[0]`이 `codex_categories`에 없으면 코드에서 강제로 `path[0]="미분류"`로 교정. AI가 원칙을 어겨도 코어가 막는다.

**빈 상태:**
- `codex_categories` 없으면 위키 첫 화면: **"먼저 카테고리를 정하세요"** → [카테고리 설정] 유도.
- 카테고리는 있는데 룰이 없으면: **"카테고리는 준비됐어요. [지식 넣기]에서 판단을 채우세요."**

---

## 5. 저장·병합·로그 (코어)

- localStorage 키 `codex_tree`에 룰 **평면 배열**로 저장. 새로고침해도 유지.
- **병합:** 같은 `path`의 룰이 있으면 새로 만들지 말고 병합 — `held/total` 합산, `note`·`badge`·`strength_label` 최신 갱신, **`history`·`authors`는 누적**(덮어쓰지 않는다). 없으면 추가.
- 가지(대분류·중분류)는 저장하지 않는다. `path`에서 생성. (v0.4: path의 대·중분류는 `codex_categories`에서 유효성 확인.)
- **편집 로그 자동화.** 모든 편집이 저장 시 `history`에 `{who, action, at}` 한 줄을 자동 append. 액션 어휘 최소 고정: `새 룰 등재`·`판단문 수정`·`path 이동`·`kind 변경`·`note 수정`·`summary 수정`·`승인`·`삭제`·`검토 추출로 등재`·`계약 마이닝으로 등재`. 사용자는 이 기록을 직접 쓰지 않는다.
- ● **v0.4 미분류 처리:** `path[0]`이 `codex_categories`의 어느 대분류에도 없으면 코어가 강제로 `["미분류", ...(원 path 유지)]`로 저장. 사람이 나중에 카테고리를 넓히거나 룰을 옮긴다.
- "최근 변경" 화면은 모든 룰의 `history`를 시간순으로 모아 표시.

---

## 6. 편집 방식 (v0.4 재분류)

각 편집 방식은 코어 스키마(4장)를 반환하기만 한다. 방식을 더해도 코어는 안 바뀐다.

### 6.1 ● 룰 페이지 안 편집 (v0.4 재정의 · v0.3 조각3에서 자리 이동)
**룰 페이지의 필드 각각(판단문·note·kind·summary·강도)이 마우스오버 → 클릭 → 편집(EditableField).**
- path와 카테고리는 룰 페이지에서 이동 가능(대·중분류 드롭다운, `codex_categories`에서만 선택). — 조각 6b에서 실체.
- 저장 시 `stampAuthoring`으로 저작자·history 자동. 액션: 판단문 수정 / note 수정 / summary 수정 / kind 변경 / path 이동.
- 저장은 **조용히** (본문 flash 없음, 새 값이 그 자리에 렌더되는 게 성공 신호).
- `origin`은 원래 값 유지 (편집이 origin을 안 바꿈).

### 6.2 ● 새 룰 만들기 = [지식 넣기] (v0.4 재정의)
**상단 nav의 [지식 넣기] 클릭 → 룰 페이지가 "새 룰 편집 모드"로 열림.**
- 필드: **카테고리 대·중분류 드롭다운**(codex_categories에서만, 필수) · 판단문(필수) · kind(soft 기본) · note(선택) · summary(선택).
- AI path 제안 버튼(조각 5 재활용): 판단문 입력 후 → AI가 카테고리 안에서 항목 제안 3개 → 클릭 accept.
- 저장 → 트리에 push, 저작자·history 자동, `origin="새 룰"`, `provenance=null`.
- 저장 성공 → 새 룰이 selectedRule로 세팅되고 룰 페이지가 방금 만든 룰을 보여줌.
- **카테고리가 아직 없으면** [지식 넣기] 클릭 시 [카테고리 설정] 모달 자동 open 유도 ("먼저 카테고리부터").

### 6.3 검토 추출 4단계 (MoreDrawer, 파워유저용)
v0.2·v0.3 그대로. **v0.4에서 프롬프트에 `codex_categories` 주입 필수.** 반환 룰의 path 검증.

### 6.4 계약 마이닝 대량 (MoreDrawer, 초기 부트스트랩)
v0.3 그대로. **v0.4에서 프롬프트에 `codex_categories` 주입 필수.** 반환 룰의 path 검증.

### 6.5 인터뷰 채굴 (MoreDrawer 자리만, 안 만듦)

**MoreDrawer 정리 (v0.4):** 수동 폼(v0.3 6.2) 항목은 6.2 새 룰 만들기로 흡수·제거. MoreDrawer엔 계약 마이닝·검토 추출·검토 렌즈·인터뷰만 남음.

---

## 7. 렌즈·찾기 — 지식을 꺼내는 방법

### 7.1 검토 렌즈 (입력을 비추기)
새 입력(계약 조항·법령 변경)을 트리에 비춰 **위반/해당/보류/무관** 판정. MoreDrawer에서 접근. (조각 5+ 예정)

### 7.2 ● AI 찾기 (v0.4 채팅 버블 흐름으로 재작성)

**우측 패널은 대화 UI** — 사용자 질문 버블 → AI 답 버블 순으로 흐른다.

**답 구조:**
- 답 문장 (존댓말, 1~3문장)
- 답 안에 **근거 pill** (강도색 dot + 룰 이름 + 저작자 credit "김변호사, 3월"). 클릭 → 중앙 룰 페이지 전환.
- 근거 없으면 회색 **"확정 기준 없음 · 사람 확인 필요"** 배너 (hold 배너).

**Cody 하단 상주:** 두더지 아이콘 + 상태별 안내 한 줄 ("근거가 있는 것만 답해요. 없으면 없다고 말할게요 🔦" 등).

**미탐 우선 원칙 (양보 불가):** 확실치 않으면 무관이 아니라 보류. 놓치는 게 틀리는 것보다 위험하다. AI는 인용하지 창작하지 않는다.

**대화 상태:** 세션 내에만 유지(새로고침 시 리셋). 지속 저장은 나중 조각.

---

## 8. AI의 두 역할 (중심은 언제나 사람)

- **편집 도울 때(4.5 path 제안 · 6.3·6.4·6.5):** AI가 묻거나 분석하고, 사람이 판단·선택하고, **그 사람이 저작자**가 된다.
- **찾을 때(7.2):** 사람이 묻고, AI가 트리에서 답하고, **저작자를 밝힌다.**

편집 도울 때도 사람이 주인, 꺼낼 때도 사람이 신뢰의 출처.

---

## 9. md 내보내기 (사람+기계 한 장)

'위키 md 저장' → `codex_wiki_YYYY-MM-DD.md`.
- `#` 제목 + 요약(총 규칙 수 / 분포) → `##` 대분류 → `###` 중분류(다수 hold면 ⚠) → 룰은 `- **[HARD] 판단문** \`관철 4/4\`` 아래에 근거·origin·status·저작자·출처·재검토 트리거를 하위 항목으로.
- **문서 말미 `## 기계 판독용 데이터`에 ```json 으로 전체 룰 배열 + `codex_categories` 함께.** 사람은 위를, AI는 이 블록을 읽는다.
- 이 파일이 "앱이 없어져도 위키의 판단·카테고리·책임 소재는 남는다"의 물증.

---

## 10. 화면 — v0.4 대개편

### 10.1 상단 바
- 로고 · **[판단 찾기]**(기본 활성, 클릭 시 새 룰 모드에서 나옴) · **[카테고리 설정]**(모달 열기) · **[지식 넣기]**(룰 페이지 새 룰 모드) · 우측: 검색바 · ⚙ 설정 · codex_tree count · 모델

### 10.2 3판 그리드
좌 25% 목차 / 중 50% 룰 페이지 / 우 25% AI 찾기. 데스크톱 데모 우선(반응형 나중).

### 10.3 목차 (좌) — v0.4 개편
- **상단:** "목차" 라벨 + 규칙 수. 판단 검색 인풋(즉시 필터는 옵션). [카테고리 설정] 버튼.
- **본문:**
  - **기본 접힘.** 처음엔 대분류 줄만 (예: 6줄). 각 줄 앞 ▸, 클릭 시 항목 펼침.
  - 대분류 줄: 이름 + 규칙 수 + (다수 hold이면) ⚠. **강도색 dot은 안 붙임.** 열림 시 검은 배경 강조.
  - 항목 줄: 강도색 dot(hold는 점선 원) + 판단문. 선택된 룰은 amber 배경.
  - 대분류 헤더 sticky.
  - **편집 자리 없음.** `+` 버튼·인라인 입력창 모두 제거.
- **하단 (미분류, 조건부):** 항상 맨 아래 고정. 없으면 안 보임.
- 카테고리가 아직 없으면 좌 패널이 "카테고리가 아직 없습니다 · 먼저 판단을 분류할 큰 틀을 정해주세요" + [카테고리 설정] 유도 카드.

### 10.4 룰 페이지 (중) — v0.4 개편
- **Breadcrumb:** 판단 찾기 › 대분류 › 중분류 › (항목 short). 각 조각 클릭 가능.
- **제목** (h1, 큰 글씨) — EditableField(판단문).
- **Credit 한 줄:** "김변호사가 · SK케미칼–한국콜마 위수탁계약에서 · 2026-08-02 추출" — 판단의 신뢰 앵커. 저작자·provenance에서 합성.
- **강도 pill** + **badge** (관철 30/30) + **badge note** ("양보 0 — 서명본 30건 전부 이 선을 지켰습니다.").
- **요약 카드** (border box, "요약" 라벨) — `summary` 필드. EditableField.
- **Hold 안내 카드** (조건부) — kind가 hold이면 "팀 판단이 갈린 자리 · 사람 확인 필요" + hold note + [토론 열기] [담당자 지정] 버튼 자리 (자리만, 실동작은 나중).
- **재검토 카드** — 재검토 트리거 표시 + [재검토 요청] 버튼.
- **관련 판단 pills** — `related` 배열의 룰들. 클릭 → 그 룰로 전환.
- **[역사] 토글** (상단 우측 자리 유지) — 저작·이력·출처·메타 접힘.
- **새 룰 모드** (조각 6c): [지식 넣기] 클릭 시 이 페이지가 새 룰 편집 상태로 진입. 카테고리 대·중 드롭다운 + 판단문 + kind + note/summary. AI path 제안 버튼. 저장.

### 10.5 AI 찾기 (우) — v0.4 채팅 흐름
- 상단: "AI 찾기" + 서브 "우리 판단에서만 답을 찾습니다"
- 인풋 (판단 검색 + ↵)
- **대화 리스트** (스크롤): user 버블 오른쪽 / ai 버블 왼쪽 (border style 다르게)
- AI 버블 안: 답 문장 + **근거 pill** (dot + 이름 + credit, 클릭 시 룰 페이지) + (조건부) 회색 hold 배너
- **하단 Cody** 두더지 + 안내 한 줄

### 10.6 [카테고리 설정] 모달
- 제목 "카테고리 설정" + 부제 "판단을 분류할 큰 틀"
- 안내 카드: "먼저 카테고리를 정하세요. 앞으로 모든 판단은 이 카테고리 안에 분류됩니다."
- **비었을 때** 프리셋 카드: "법무 기본 카테고리 불러오기" 버튼 + 6개 이름 미리보기
- **채워졌을 때** 대분류 리스트:
  - 각 카드: 번호 · 이름 · [이름 편집] [순서 ↑↓] [삭제] 버튼 · 중분류 pills + [+ 중분류] 버튼
  - 상단 [+ 대분류 추가] 버튼
- 삭제 경고: 그 카테고리에 룰 N개가 있으면 "미분류로 옮길지" 확인.
- 하단: Cody + [저장하고 닫기] 버튼.

### 10.7 [지식 넣기] = 룰 페이지 새 룰 모드
- 상단 nav [지식 넣기] 클릭 → 룰 페이지 자리에 새 룰 폼 렌더
- 필드: **대분류 드롭다운**(codex_categories에서) · **중분류 드롭다운**(대분류 선택 시 그 아래 subs만) · **판단문** · **kind 칩** · note/summary
- AI path 제안 버튼: 판단문 입력 후 → 카테고리 안에서 항목 3개 제안 → 클릭
- [심기] 버튼 → 저장 → 그 룰로 전환
- **카테고리 없으면** [카테고리 설정] 모달 자동 open + 안내.

### 10.8 편집 UX 규약
- 첫 편집 시 이름 없으면 Settings 자동 open.
- 편집 저장은 조용히 (본문 flash 없음).
- 모든 편집 액션은 `history` 자동 append (5장).
- 카테고리 없이 [지식 넣기] 시도 → [카테고리 설정] 유도.

### 10.9 MoreDrawer (v0.4 축소)
"…더보기" 라이트박스는 유지하되 항목 재편:
- ~~수동 폼~~ (v0.4에서 상단 [지식 넣기]로 흡수·제거)
- 검토 추출 4단계 (준비 중)
- 계약 마이닝 대량
- 검토 렌즈 (준비 중)
- 인터뷰 채굴 (준비 중)

---

## 11. 색·비주얼 규약 (모든 화면 공통)

- 배경 `#E9EBE6` · 패널 `#F5F6F3` · 잉크 `#14171C`.
- 강도색: hard `#9E3524` · soft `#C8871F` · neu `#2FA97F` · hold `#8A9086`(+점선·반투명).
- 폰트: 본문 Pretendard, 숫자·라벨 IBM Plex Mono. 테두리 2px.
- 코디(두더지·헤드램프): 위키를 가꾸는 안내자. 친근한 존댓말, 말풍선당 이모지 최대 1개, 권위자 톤 금지("모르면 저도 잘 모르겠어요").
- **표현 원칙 (v0.4 신설):** 라벨·문구는 직관적으로. 은유(서랍·뼈대·나무 등)를 라벨에 쓰지 않는다. 사람이 한 번 보고 바로 아는 말만. 분류 틀 기능은 "카테고리 설정"으로 통일.

---

## 12. 지금 범위 vs 나중 범위 (v0.4 재배열)

**지금 — 조각 6a~6e 순차:**
- **6a**: 카테고리 저장소 + [카테고리 설정] 모달 + 프리셋 + 목차 개편 (기본접힘·sticky·dot 잎에만·미분류) + TocPanel `+` 삭제 + InlineNewRule 폐기.
- **6b**: 룰 페이지 개편 (breadcrumb·credit·요약카드·hold안내·재검토·관련판단) + summary·related 필드 반영 + path 이동(카테고리 드롭다운).
- **6c**: [지식 넣기] 상단 nav → 룰 페이지 새 룰 모드 + MoreDrawer 수동 폼 항목 제거.
- **6e**: 입구(계약 마이닝·path 제안) 프롬프트 카테고리 제약 + 응답 검증(카테고리 밖이면 "미분류").
- **6d**: AI 찾기 채팅 버블 UI 개편.

**선택 (되면 좋지만 데모 필수 아님):**
- 검토 추출 4단계 실체, 검토 렌즈 (MoreDrawer 안)

**나중 (자리만 표시, 안 만듦):**
- 인터뷰 채굴, 규정 마이닝, 파일 OCR·hwp, SSO 저작자, Notion·공유 저장소.
- path 이동 자유도 (지금은 카테고리 드롭다운으로만, 룰 페이지에서).
- 팀 유형별 시작 발판 부록 (인사·개발·마케팅).
- AI 찾기 대화 이력 지속 저장 · 팔로업 질문.

> 다 만들려다 아무것도 안 도는 게 최악. **좁게, 그러나 진짜로.** v0.4의 좁고 진짜는 **지도(카테고리) 있는 위키 + 편집은 룰 페이지 안 + AI 찾기 대화.**

---

## 13. 완료 기준 (v0.4)

1. **[카테고리 설정]** — 대·중분류를 정하면 `codex_categories` 저장, 목차에 빈 카테고리로 보인다. 프리셋 "법무 기본" 시드 가능, 편집·삭제 가능.
2. **목차** — 기본 접힘, 대분류 헤더 sticky, 강도 dot은 잎에만, 미분류 맨 아래. **`+` 편집 자리 없음.**
3. **룰 페이지** — breadcrumb · credit · 요약 카드 · (hold 시) hold 안내 · 재검토 · 관련 판단 pills. 판단문·note·kind·summary는 마우스오버 편집. path는 카테고리 드롭다운으로 이동.
4. **[지식 넣기]** (상단 nav) → 룰 페이지 새 룰 모드. 카테고리 드롭다운 필수(카테고리 밖 선택 불가) · 판단문 필수 · 저장 → 트리 등재. 카테고리 없으면 [카테고리 설정] 유도.
5. **입구 프롬프트 카테고리 제약** — 계약 마이닝·AI path 제안이 카테고리 안에서만 분류. 밖이면 코어가 "미분류"로 강제.
6. **AI 찾기 채팅** — 대화 버블 흐름. 근거 pill(색 dot + 이름 + credit) 답 안. 근거 없으면 회색 hold 배너. 근거 클릭 → 룰 페이지 전환.
7. **편집 액션마다** `authors`·`history` 자동 append. 이름 없이 편집 → Settings 자동 open.
8. **같은 path 재입력 시 병합** — history·authors 누적.
9. **표현 규범** — 화면 어디에도 은유 라벨 없음. "카테고리 설정"으로 통일.
10. `npm run build` 성공.

---

## 14. 불가침 요약 (한눈에)

- **위키가 곧 앱이다.** 위키 안에서 편집·검색·AI 대화 상주.
- ● **카테고리는 사람이 먼저 정한다.** AI는 그 안에서만 채운다. 새 대분류 발명 금지. (v0.4)
- ● **목차는 지도, 편집은 룰 페이지 안에서만.** 목차 옆 `+` 폐기. 새 룰은 상단 [지식 넣기]. (v0.4)
- 편집 장벽은 낮게 (룰 페이지 안 EditableField로 5초).
- 코어 ↔ 편집 방식/렌즈 분리. 코어는 편집 방식 추가에도 안 바뀐다.
- 모든 편집 방식은 4장 스키마만 뱉는다. `kind`(강도)는 빈도와 다르다.
- 트리는 뷰, 저장은 평면 배열, 가지는 `path`에서 생성(코드 하드코딩 금지).
- **주인 없는 판단은 트리에 없다.** authors·history·provenance는 필수 축.
- 원본은 안 남기되 출처는 남긴다.
- AI는 초안, 사람이 확정.
- 데이터 반출·키 커밋 금지, 로컬 전용.
- 찾기·검토는 확실치 않으면 보류(미탐 우선). AI는 인용하지 창작 안 한다.
- md 말미 JSON 블록 필수 (트리 + `codex_categories`).
- **은유 라벨 금지.** 직관적 문구로.

---

## 15. 이전 판 히스토리

**v0.1 — 채굴 도구.** "AI가 계약을 채굴해 트리에 쌓는 도구." 코어(스키마·트리·저장·병합·md)와 계약 마이닝·수동 입력 입구, [위키트리] 화면이 뼈대. 저작·출처 개념 없음.

**v0.2 — 저작자 축.** 무게중심을 "누가 썼는가"로 이동. `authors`·`history`·`provenance` 신설, 검토 추출 4단계 입구 신설(핵심), AI 찾기 신설, 위키 3단 화면. 스키마 골격은 v0.4까지 그대로 계승.

**v0.3 — 위키 한 층.** "지식 계층 + AI 인터페이스" 이분법 폐기, **위키가 곧 앱.** "지식 넣기" 프레임 폐기, 편집은 위키 안 곳곳에서 인라인(목차 옆 `+`). 검토 추출 격하. 편집 장벽 최소 원칙 신설, 저작자 자동 삽입 + 편집일 자동 기록. path 규약(4.5장)과 AI 자동 제안 신설. v0.3에서 도입한 목차 옆 `+` 인라인은 실제 써보니 목차의 지도성을 흐리는 문제가 있어 v0.4에서 위치 이동.

**v0.4 — 지도가 있는 위키.** taxonomy를 AI가 자유 발명하는 방식 폐기. 카테고리(대·중분류)를 사람이 먼저 정하고 `codex_categories`에 저장, AI·입구는 그 안에서만 분류·제안·답변. 카테고리 밖이면 "미분류" 강제. 목차 옆 `+` 편집 폐기 — 목차는 순수 지도(기본 접힘·sticky·dot 잎에만·미분류 하단). 편집은 룰 페이지 안 EditableField로만. 새 룰 만들기는 상단 [지식 넣기]로 승격 → 룰 페이지가 새 룰 모드로 진입. AI 찾기 채팅 버블 흐름 재작성. 룰 페이지에 breadcrumb·credit·요약카드·hold안내·재검토·관련판단 구조 도입. 스키마에 `summary`·`related` 신규 필드. 표현 원칙(은유 금지, "카테고리 설정" 통일) 신설.

---

*v0.4 — 무게중심을 "카테고리는 사람이 정하고 AI는 그 안에서만 채운다"로 옮긴 판. 이 문서가 헌법이다. 구조를 바꿔야 하면 이 문서를 먼저 고친다.*
