# CODEX 마스터 계획서 (CLAUDE.md)

> 팀의 판단을 **기계가 읽는 지식 계층**으로 쌓는 시스템.
> 사람이 읽는 문서이자, Claude Code가 매 세션 처음 읽는 **헌법**이다.
> 새 작업을 시작하기 전에 이 문서를 먼저 읽는다. 여기 적힌 구조 원칙과 스키마는 어떤 작업에서도 어기지 않는다.
> 특정 기능을 어떻게 만들지 헷갈리면, 그 결정이 이 문서의 목적(1장)에 맞는지부터 본다.

---

## 0. 이 문서의 사용법 (Claude Code에게)

- 매 작업 전 이 파일을 읽는다. 이 프로젝트의 구조는 여기에만 고정돼 있다.
- **구조 원칙(2장)과 룰 스키마(3장)는 불가침이다.** 편의를 위해 새 구조·새 필드를 임의로 만들지 않는다. 필요하면 먼저 이 문서를 고치자고 제안한다.
- 한 번에 전체를 만들지 않는다. 작업은 별도 지시서(예: `작업지시서_YYYY-MM-DD.md`)로 조각 단위로 들어온다. 그 조각만 만들고, 완료 기준을 확인하고 멈춘다.
- 기존에 만든 코드의 구조를 모르겠으면 새로 짓지 말고 이 문서와 현재 파일을 먼저 읽는다.

---

## 1. 이게 뭐고 왜 만드나

**문제.** 이 회사는 보안 때문에 외부 AI 서비스·클라우드·데이터 반출이 막혀 있다. 남들처럼 우리 데이터를 외부 모델에 넘기는 방식의 AX는 영원히 못 한다.

**유일한 답.** 그 제약 안에서 AX를 준비하는 길은 하나다 — **우리 판단을 기계 판독 형태로 우리 안에 쌓아두고, 어떤 AI가 들어오든(로컬·온프레미스) 그걸 읽게 하는 것.** 모델은 계속 바뀌고 좋아진다. 바뀌지 않고 남는 건 우리 지식이다.

**그래서 만드는 것은 앱이 아니라 두 겹이다.**
- **아래층 — 지식 계층(위키트리 + md).** 영구적, 우리 소유, 어떤 툴에도 안 묶임. 밖으로 안 나간다.
- **위층 — AI 인터페이스(넣기·검토 화면).** 소모품. 오늘은 Claude, 내일은 사내 모델. 갈아끼워도 아래층은 그대로.

**한 문장.** "우리가 만든 건 특정 앱이 아니라, 앱이 바뀌어도 남는 지식 계층이다."

---

## 2. 구조 원칙 (불가침)

이 다섯 개가 무너지면 프로젝트가 무너진다. 어떤 작업에서도 어기지 않는다.

1. **코어와 입구/렌즈를 분리한다.** 코어(트리·스키마·저장·md)는 입구·렌즈와 독립이다. 입구나 렌즈를 추가·수정해도 **코어 코드는 바뀌지 않아야** 한다.
2. **모든 입구는 같은 룰 스키마(3장)만 뱉는다.** 계약 마이닝이든 수동 입력이든, 트리에 꽂히는 건 항상 동일한 모양이다. 입구는 "무엇을 읽고 어떻게 캐느냐"만 다르고, 출력 규격은 하나다.
3. **트리는 저장이 아니라 뷰다.** 룰은 평면 배열로 저장하고, 각 룰의 `path`로 트리를 그려낸다. 사람은 나무로 보고, 기계는 배열로 질의한다.
4. **도메인 중립.** 대분류·중분류(가지)를 하드코딩하지 않는다. `path`에서 동적으로 생성한다. 법무팀은 "계약/규제"를, 다른 팀은 자기 분류를 쓴다. "어느 팀에나"는 이 원칙으로 실현된다.
5. **로컬 전용, 데이터 반출 금지.** 지식은 브라우저(localStorage)와 로컬 파일에만 산다. 계약서 원본은 저장하지 않는다(추출 텍스트와 룰만 남긴다). 외부 호스팅하지 않는다. API 키·토큰을 코드에 하드코딩·커밋하지 않는다.

---

## 3. 공통 룰 스키마 (하나로 고정)

트리의 원자 단위. 모든 입구가 이 형태로만 만들고, 모든 화면·내보내기가 이 형태를 읽는다. **필드를 임의로 늘리지 않는다.**

```json
{
  "id": "RULE-0001",
  "path": ["대분류", "중분류", "항목"],
  "judgment": "판단 한 문장",
  "kind": "hard | soft | neu | hold",
  "strength_label": "마지노선 | 협상 여지 | 신규·미확정 | 사람 확인",
  "held": 4,
  "total": 4,
  "badge": "관철 4/4",
  "note": "근거·양보 이력 한 줄",
  "origin": "수동 입력 | 계약서 마이닝(diff) | 사례 역추출 | 인터뷰 채굴",
  "source": "제7조 등 근거 위치(선택, 없으면 null)",
  "status": "candidate | approved",
  "review_trigger": {
    "keywords": ["배상", "손해", "cap", "한도"],
    "law_group": "상법 배상책임(선택, 규제형 노드만)",
    "test": "입력에 배상 상한 조항이 있는가"
  },
  "renew_trigger": "상법 배상 조항 개정(선택, RADAR 연결점)",
  "created_at": ""
}
```

**필드 규칙**
- `kind`는 등장 빈도가 아니라 **판단의 성격**이다. 절대 혼동하지 않는다.
  - `hard`(마지노선): 우리 표준안이 한 번도 양보되지 않음.
  - `soft`(협상 여지): 표준안을 제시하나 양보 이력이 있음. 양보 이력이 곧 협상 카드라는 증거다.
  - `neu`(신규·미확정): 표본이 적거나 최근 등장한 쟁점.
  - `hold`(사람 확인): 우리안과 상대안이 엇비슷하게 갈리거나, 표준에 조항 자체가 없어 제각각. **억지로 규칙화하지 말고 갈렸다고 정직하게 표시한다.**
- `held/total`은 미터용. 값이 없으면 null 허용.
- `review_trigger`는 이 노드가 "검토 기준"으로도 작동하기 위한 신호. `keywords`는 1차 빠른 매칭, `test`는 애매할 때 LLM이 판정하는 문장.
- 채굴/입력 직후 `status`는 `candidate`. 사람이 승인하면 `approved`. approved만 최종 지식으로 취급한다(내보내기엔 둘 다 상태 표기해 포함).

---

## 4. 저장·병합 규칙 (코어)

- localStorage 키 `codex_tree`에 룰 **평면 배열**로 저장. 새로고침해도 유지.
- **병합:** 같은 `path`의 룰이 이미 있으면 새로 만들지 말고 병합한다 — `held/total` 합산, `note`·`badge`·`strength_label` 최신값 갱신. 없으면 추가. → 같은 대상을 다시 넣어도 중복 가지가 안 생기고 숫자만 커진다.
- 가지(대분류·중분류)는 저장하지 않는다. `path`에서 그때그때 생성된다.

---

## 5. 입구 — 지식을 넣는 방법 (플러그인)

각 입구는 코어가 정한 스키마(3장)를 반환하기만 한다. 입구를 더해도 코어는 안 바뀐다.

### 5.1 수동 입력 (가장 단순한 입구)
폼: `path`(대분류/중분류/항목) + 판단문 + `kind` + note → 스키마로 만들어 코어에 주입. 마이닝 없이도 트리가 살아있게 하는 기본 입구.

### 5.2 계약서 마이닝 (대량 자동 입구)
표준안 + 서명본(붙여넣기) → Anthropic 호출 → 룰 배열. `origin`은 `계약서 마이닝(diff)`, `path`는 예: `["계약 판단","책임·분쟁","배상 상한"]`. 결과는 `candidate`로 주입. 채굴 프롬프트는 아래 고정.

```
당신은 기업 법무의 '계약 판단 채굴기'다. 아래 서명된 계약들을 읽고, 이 팀이 반복하는 판단을 규칙으로 뽑는다. 지어내지 말고 실제 텍스트에서 반복되는 것만 뽑는다.

[강도 판정 — 등장 빈도와 혼동 금지]
- 마지노선(hard): 우리 표준안이 한 번도 양보되지 않은 조항.
- 협상 여지(soft): 표준안을 제시하나 양보한 이력이 있는 조항. 양보 이력이 곧 협상 카드라는 증거다.
- 신규·미확정(neu): 표본이 적거나 최근에야 등장한 쟁점.
- 사람 확인(hold): 우리안과 상대안이 엇비슷하게 갈리거나, 표준에 조항이 없어 제각각인 경우. 억지로 규칙화하지 말고 갈렸다고 정직하게 표시한다.

[트리 경로] 각 룰에 path를 매긴다: [대분류, 중분류, 항목]. 성격으로 묶는다(정보통제 / 절차·기한 / 책임·분쟁 등).

3장의 공통 룰 스키마 형태의 JSON 배열만 출력한다. 마크다운·설명 없이 JSON 하나만.

[서명된 계약들] {{서명본}}
[표준안] {{있으면 표준안, 없으면 "없음"}}
```

### 5.3 향후 입구 (지금 만들지 않음, 자리만 표시)
규정 마이닝 / 자문 Q&A 마이닝 / 인터뷰 채굴. '지식 넣기'에 '준비 중'으로 비활성 노출 — 같은 스키마로 꽂힐 자리임을 보여준다(확장성 시연).

---

## 6. 렌즈 — 쌓인 지식으로 비추는 방법

트리 노드는 두 역할을 동시에 한다: **판단 기록**이자 **검토 기준**. 검토 = 새 입력을 노드들의 `review_trigger`에 대보고 걸리는 노드를 찾아 판정하는 것. 활용을 여러 개 만들지 않는다 — 이 렌즈 하나가 "저장된 지식이 활용된다"의 증명이다.

**미탐 우선 원칙(양보 불가).** 확실치 않으면 '무관'이 아니라 '보류'를 택한다. 트리에 기준이 없는 관련 입력은 반드시 보류로 사람에게 올린다. **놓치는 것이 틀리는 것보다 위험하다.** (RADAR 기준서와 같은 철학.)

구현은 오늘 작업지시서 참조.

---

## 7. md 내보내기 (핵심 산출물, 사람+기계 한 장)

'위키트리 md 저장' → `codex_wikitree_YYYY-MM-DD.md` 다운로드.
- `#` 제목 + 요약(총 규칙 수 / hard·soft·hold 분포) → `##` 대분류 → `###` 중분류(다수 hold면 옆에 ⚠) → 룰은 `- **[HARD] 판단문** \`관철 4/4\`` 아래 근거·origin·status·재검토 트리거를 하위 항목으로.
- **문서 말미 `## 기계 판독용 데이터`에 ```json 으로 전체 룰 배열(스키마 그대로).** 사람은 위를, AI는 이 블록을 읽는다.
- 이 파일이 "앱이 없어져도 지식은 남는다"의 물증이다.

---

## 8. 화면 구조

상단 탭 세 개 + 설정.
- **[지식 넣기]** — 입구 선택(수동 입력 / 계약서 마이닝 / 향후 입구 비활성). 계약 마이닝은 표준안·서명본 입력 + 채굴 현장(코디 로딩) + 관철/양보 배지 + hold 점선 큐.
- **[위키트리]** — codex_tree를 path로 그룹핑한 접이식 트리. 대분류>중분류>룰 카드. 다수 hold 가지엔 ⚠ + "표준 미확정". md 저장 버튼.
- **[검토하기]** — 렌즈. 입력을 붙여넣으면 걸린 노드 하이라이트 + 판정(위반/해당/보류).
- **설정(⚙)** — API 키 입력, 모델 선택. 키 없으면 마이닝·검토 비활성.

---

## 9. 색·비주얼 규약 (세 화면 공통)

세 화면이 한 제품으로 묶여야 "플랫폼"이 믿긴다. 색·폰트·코디는 어디서나 동일.
- 배경 `#E9EBE6` · 패널 `#F5F6F3` · 잉크 `#14171C`.
- 강도색: hard `#9E3524` · soft `#C8871F` · neu `#2FA97F` · hold `#8A9086`(+점선·반투명).
- 폰트: 본문 Pretendard, 숫자·라벨 IBM Plex Mono. 테두리 2px.
- 코디(두더지·헤드램프): 나무를 가꾸는 안내자. 친근한 존댓말, 말풍선당 이모지 최대 1개, 절대 권위자 톤 금지("모르면 저도 잘 모르겠어요").

---

## 10. 지금 범위 vs 나중 범위

**지금(대회 기간에 실제로 돌린다):**
- 코어: 트리 저장·병합·표시, md 내보내기.
- 입구: 수동 입력 + 계약서 마이닝.
- 렌즈: 검토 하나(한 케이스 깔끔하게).

**나중(자리만 표시, 만들지 않음):**
- 다른 입구(규정·자문·인터뷰), 파일 드롭·OCR·hwp, Notion/공유 저장소, 백엔드.

> 다 만들려다 아무것도 안 도는 게 최악이다. **좁게, 그러나 진짜로.** 보여주는 건 좁고 진짜, 그리는 비전은 넓게(말로).

---

## 11. 완료 기준 (뭐가 되면 이 프로젝트가 "된다")

1. 수동 입력·계약 마이닝 어느 쪽으로든 룰이 트리에 실제로 쌓이고, 새로고침해도 남는다.
2. 새 `path`를 쓰면 가지가 자동 생성된다(가지 하드코딩 아님).
3. 같은 대상 재입력/재채굴 시 중복이 아니라 병합(카운트만 증가).
4. 다수 hold 가지에 ⚠가 뜬다.
5. md 저장 시 헤딩 트리 + 말미 JSON 블록이 든 파일이 실제로 떨어진다.
6. [검토하기]에 입력을 넣으면 걸린 노드가 판정되고, **트리에 없는 쟁점은 '무관'이 아니라 '보류'로** 잡힌다.

---

## 12. 불가침 요약 (한눈에)

- 코어 ↔ 입구/렌즈 분리, 코어는 입구 추가에도 안 바뀐다.
- 모든 입구는 3장 스키마만 뱉는다.
- `kind`(강도)는 빈도와 다르다.
- 트리는 뷰, 저장은 평면 배열, 가지는 `path`에서 생성(하드코딩 금지).
- 데이터 반출 금지, 원본 저장 금지, 키 커밋 금지, 로컬 전용.
- 검토는 확실치 않으면 보류(미탐 우선).
- md 말미 JSON 블록은 반드시 있어야 한다(사람+기계 한 장).

---

*v0.1 — 이 문서가 헌법이다. 구조를 바꿔야 하면 코드부터 고치지 말고 이 문서를 먼저 고친다.*
