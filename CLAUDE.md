# CODEX 마스터 계획서 (CLAUDE.md) v0.3

> 팀의 판단을 **누가·언제·왜 정했는지까지 남는, 기계가 읽는 지식 계층**으로 쌓는 시스템.
> 사람이 읽는 문서이자, Claude Code가 매 세션 처음 읽는 **헌법**이다.
> 작업 전 이 문서를 먼저 읽는다. 구조 원칙과 스키마는 어떤 작업에서도 어기지 않는다.
> 구조를 바꿔야 하면 코드부터 고치지 말고 이 문서를 먼저 고친다.

---

## ★ 이번 판(v0.3)에서 새로 정해진 것

v0.2는 "저작자 축과 검토 추출을 핵심으로" 세운 판이었다. v0.3는 무게중심을 다시 옮긴다 — **위키 그 자체로.**

v0.2까지의 프레임은 조용히 "계약 검토 요약 도구"에 가까웠다. 판단을 넣는 사람이 늘 있어야 돌아가고, 넣는 방식은 무거웠다(4단계). 그러나 이 프로젝트의 목표는 **위키**다. 회사마다 법무팀 안에는 지식 넣기를 좋아하는 사람도 있고 아닌 사람도 있다. 좋아하는 소수만 성실히 채워야 돌아가는 도구는 위키가 아니다. 위키의 성질은 하나 — **아무나 지나가다 한 줄 툭 던져도 자란다.** 새로 들어온 것:

1. **위키가 곧 앱이다** — "지식 계층(아래) + AI 인터페이스(위)" 이분법 폐기. 위키가 지식이자 곧 화면. AI는 위키 안에서 편집과 검색을 돕는 조수로 격하. (1장·3장·10장)
2. **편집 장벽 최소 원칙 신설** — "지식 넣기" 프레임 폐기. 편집은 위키 페이지 곳곳에서 열린다. 위키 노드 옆 `+`를 누르면 **한 문장 + 엔터**로 편집이 끝나야 한다. path·kind·저작자는 컨텍스트·설정에서 자동. (3장·6장·10장)
3. **입구 → "편집 방식"으로 개칭 + 재분류** — 무거움 순으로: 인라인 편집(기본) / 수동 폼 / 검토 추출(파워유저) / 계약 마이닝(초기 부트스트랩). **검토 추출은 v0.2의 핵심이었으나 v0.3에서 격하** — 이걸 즐기는 사람에게만 쓸모 있는 옵션이라 기본이 될 수 없다. (6장)
4. **저작자 자동 삽입 + 편집일 자동 기록** — 편집 장벽을 낮추는 대가로 저작자 필수 원칙은 오히려 강화한다. 편집 한 건마다 `authors`·`history`에 **누가·언제·무엇을**이 자동으로 박힌다. 사용자 부담은 첫 편집 시 이름 한 번뿐. (2장·5장·10장)
5. **위키가 앱 셸** — 탭 3개 구조([지식 넣기]·[위키]·[검토하기]) 폐기. 위키 화면 안에 편집 인라인·AI 찾기 상주. 무거운 편집·렌즈는 상단 "…더보기"로 격리. (10장)
6. **path 규약 + AI 자동 제안** — taxonomy(대분류·중분류)를 문서 종류(계약·규정·자문)로 나누지 않고 **판단의 성격**으로 묶는다(같은 판단은 병합돼 두터워짐). 축은 팀이 정하되(도메인 중립), **시작 발판(법무 예시)과 AI path 자동 제안**으로 첫 사용자의 진입 장벽을 없앤다. taxonomy는 사용자 override로 자기 회사 색으로 진화. (3장 원칙 10 · 4.5장)

---

## 0. 이 문서의 사용법 (Claude Code에게)

- 매 작업 전 이 파일을 읽는다. 프로젝트 구조는 여기에만 고정돼 있다.
- **구조 원칙(3장)·룰 스키마(4장)는 불가침이다.** 편의로 새 구조·새 필드를 임의로 만들지 않는다. 필요하면 이 문서 수정을 먼저 제안한다.
- 한 번에 전체를 만들지 않는다. 작업은 별도 지시서로 조각 단위로 들어온다. 그 조각만 만들고 완료 기준을 확인하고 멈춘다.
- 기존 코드 구조를 모르겠으면 새로 짓지 말고 이 문서와 현재 파일을 먼저 읽는다.
- **현재 코드(v0.2 지시서까지 반영)와 이 문서(v0.3)가 어긋난 지점은 v0.3가 정답이다.** 다음 작업 지시서로 코드를 v0.3에 맞춘다. 특히 탭 구조·편집 프레임·인라인 편집·저작자 자동화는 아직 코드에 없다.

---

## 1. 이게 뭐고 왜 만드나

**문제.** 이 회사는 보안 때문에 외부 AI·클라우드·데이터 반출이 막혀 있다. 우리 데이터를 외부 모델에 넘기는 방식의 AX는 영원히 못 한다.

**유일한 답.** 그 제약 안에서 AX를 준비하는 길은 하나다 — 우리 판단을 기계 판독 형태로 우리 안에 쌓아두고, 어떤 AI가 들어오든 그걸 읽게 하는 것. 모델은 바뀐다. 바뀌지 않고 남는 건 우리 지식이다.

**★ 위키 한 층 (v0.3).** v0.2까지는 "아래층=지식·위층=AI 인터페이스" 이분법이었다. 개념은 옳았지만 실제 서사는 "AI가 계약 뽑는 파이프라인" 쪽으로 계속 쏠렸다. v0.3에서 이분법을 폐기한다. **위키가 지식 계층이자 곧 화면 그 자체.** AI는 그 안에서 편집을 돕고 검색을 대신하는 도우미다. 앱을 바꿔도 남는 것은 위키의 콘텐츠(+ md 내보내기).

**한 문장.** "우리가 만든 건 앱이 아니라 **위키**다. 사람이 퇴사해도 판단과 그 근거가 남는다."

---

## 2. 무게중심 — 누가 쓰느냐

진짜 위키(위키피디아·나무위키)의 신뢰는 내용이 아니라 **편집 역사**에서 나온다. "누가, 언제, 왜 이렇게 썼는가"가 없으면 문서 더미다. 법무 판단은 특히 그렇다 — "이 배상 상한 방침은 2026년 3월 김변호사가 이 판례 때문에 정했다"가 판단 자체만큼 중요하다. 책임 소재가 있어야 기준이지, 없으면 참고자료다.

그래서 이 시스템의 중심은 AI가 아니라 **저작자**다. **주인 없는 판단은 트리에 넣지 않는다.**

**★ 편집 장벽과 저작자 필수, 양립시키기 (v0.3).** v0.3에서 편집 장벽을 낮추는데(3장-9), 그렇다고 저작자를 뺄 순 없다 — 오히려 자동화로 강화한다.
- 사용자는 **처음 편집 시 이름을 한 번**만 넣는다(설정의 "내 이름", localStorage).
- 이후 편집·수정·승인·삭제 등 **모든 액션마다 `authors`·`history`에 누가·언제·무엇을이 자동으로 박힌다.**
- 이름 없이 편집 시도 → 이름 입력창으로 조용히 유도(막지 않되 필수). 이게 편집 장벽의 유일한 예외다.
- 사용자는 로그·저작자 기록에 신경 쓰지 않는다. **투명 기록.** 룰 페이지의 `[역사]` 토글과 '최근 변경'에서 볼 수 있다(본문 읽기 화면엔 노출하지 않는다 — 10.1).

---

## 3. 구조 원칙 (불가침)

1. **코어와 편집/렌즈를 분리한다.** 코어(트리·스키마·저장·로그·md)는 편집 방식·렌즈와 독립. 편집 방식을 더하거나 고쳐도 **코어 코드는 안 바뀐다.**
2. **모든 편집은 같은 룰 스키마(4장)만 뱉는다.** 인라인 한 줄이든, 폼이든, 검토 추출 4단계든, 트리에 꽂히는 건 항상 같은 모양.
3. **트리는 저장이 아니라 뷰다.** 룰은 평면 배열로 저장하고 `path`로 트리를 그린다. 사람은 나무로, 기계는 배열로 읽는다.
4. **도메인 중립.** 가지(대분류·중분류)를 하드코딩하지 않는다. `path`에서 동적 생성. "어느 팀에나"는 이 원칙으로 실현된다.
5. **로컬 전용, 데이터 반출 금지.** 지식은 localStorage와 로컬 파일에만. 외부 호스팅·키 커밋 금지.
6. **원본은 안 남기되 출처는 남긴다.** 계약서 원본 파일은 저장하지 않는다(보안 부채). 그러나 "이 룰이 어느 계약에서 누가 뽑았다"는 출처 메타데이터(provenance)는 저장한다(자산).
7. **AI는 초안, 사람이 확정.** AI는 판단을 추출·추천할 수 있으나, "일반 기준 vs 이 건만의 특수 예외"의 최종 판단과 트리 등재는 반드시 사람이 한다.
8. ★ **위키가 곧 앱이다 (v0.3 신설).** "아래층 지식 + 위층 AI 인터페이스" 이분법 폐기. 위키가 지식이자 곧 화면. AI는 위키 안의 도우미(검색·편집 보조). 이 시스템에서 "지식 넣기 앱"·"AI 채굴 도구" 같은 별도 프레임은 사라진다.
9. ★ **편집 장벽은 최대한 낮게 (v0.3 신설).** 위키의 성질은 "아무나 지나가다 툭 던져도 자란다"이다. 기본 편집(6.1 인라인)은 **한 문장 + 엔터**로 끝난다. path·kind·저작자는 컨텍스트·설정·자동에서 채운다. 무거운 워크플로우(검토 추출 4단계 등)는 원하는 사람만 쓰는 옵션이지 기본이 아니다. **회사마다 지식 넣기를 좋아하는 사람과 아닌 사람이 있고, 아닌 사람에게도 열려 있어야 위키다.**
10. ★ **path는 성격으로 묶고, AI가 제안한다 (v0.3 신설).** taxonomy(대분류·중분류)는 문서 종류(계약·규정·자문·회의)로 나누지 않고 **판단이 걸리는 성격**으로 묶는다 — 같은 판단이 여러 상황에서 나오면 한 노드에 병합돼 두터워지는 게 위키의 이득이다(5장 병합). 축은 팀이 정하되(4번, 도메인 중립), **시작 발판과 AI 자동 제안**으로 첫 사용자의 진입 장벽을 없앤다(4.5장). 문서 종류·계약 종류·거래처 등 맥락은 `provenance`에.

---

## 4. 공통 룰 스키마 (하나로 고정)

트리의 원자 단위. 모든 편집 방식이 이 형태로만 만들고, 모든 화면·내보내기가 이 형태를 읽는다. **필드를 임의로 늘리지 않는다.** (★ = v0.2 신규, ✚ = v0.3 정책 변경)

```json
{
  "id": "RULE-0001",
  "path": ["대분류", "중분류", "항목"],
  "judgment": "판단 한 문장",
  "kind": "hard | soft | neu | hold",
  "strength_label": "마지노선 | 협상 여지 | 신규·미확정 | 사람 확인",
  "held": 4,
  "total": 4,
  "badge": "관철 4/4",
  "note": "근거·양보 이력 한 줄",
  "origin": "인라인 편집 | 수동 폼 | 검토 추출 | 계약 마이닝(diff) | 인터뷰 채굴",  // ✚ v0.3: 값 재편
  "source": "제3조 등 근거 위치(선택, 없으면 null)",
  "status": "candidate | approved",

  "authors": [                                         // ★ 이 룰의 주인(현재 상태)
    {"name": "김변호사", "role": "편집", "at": "2026-08-02"}
  ],
  "history": [                                         // ★ 편집 역사(모든 액션 자동 누적) ✚ v0.3 자동화 강화
    {"who": "김변호사", "action": "인라인 편집으로 등재", "at": "2026-08-02T14:03:12Z"},
    {"who": "이변호사", "action": "판단문 수정", "at": "2026-08-03T09:12:41Z"}
  ],
  "provenance": {                                      // ★ 어디서 나왔나(✚ v0.3: 선택 — 인라인·폼은 null)
    "source_contract": "SK케미칼-한국콜마 위수탁계약 (2026-07)",
    "reviewed_by": "김변호사",
    "extracted_at": "2026-08-02",
    "source_article": "제3조",
    "extraction_method": "검토 추출 | 계약 마이닝 | 수동 | 인터뷰"
  },

  "review_trigger": {
    "keywords": ["배상", "손해", "cap", "한도"],
    "law_group": "상법 배상책임(선택)",
    "test": "입력에 배상 상한 조항이 있는가"
  },
  "renew_trigger": "상법 배상 조항 개정(선택, RADAR 연결점)",
  "created_at": ""
}
```

**필드 규칙**
- `kind`는 등장 빈도가 아니라 **판단의 성격**이다. hard=한 번도 양보 안 함, soft=양보 이력 있음(양보가 곧 협상 카드), neu=표본 적음/신규, hold=갈리거나 기준 없음(억지 규칙화 금지, 정직하게 보류).
- `authors`는 "누가 주인인가"(현재), `history`는 "어떻게 왔나"(전체 이력). ✚ **v0.3에서 모든 편집·수정·승인·삭제 액션은 `history`에 자동 append.** 사용자가 직접 로그를 쓰지 않는다. "누가"가 빠진 로그는 로그가 아니다.
- `provenance.source_contract`는 계약명·시점만. 계약 **내용**은 저장하지 않는다. ✚ **v0.3: 인라인 편집(6.1)·수동 폼(6.2)은 `provenance=null` 허용.** 계약 검토에서 온 게 아니면 출처가 없는 게 정직하다. 검토 추출(6.3)·계약 마이닝(6.4)은 여전히 필수.
- 채굴/입력 직후 `status`는 `candidate`. 사람이 승인하면 `approved`이고 그 사람이 `authors`에 "승인"으로 붙는다. approved만 최종 지식으로 취급(내보내기엔 둘 다 상태 표기해 포함).
- ✚ **v0.3: `origin` 값 재편.** v0.2의 "수동 입력"은 "수동 폼"으로 개칭, 새로 "인라인 편집"이 추가된다. 기존 룰은 마이그레이션(수동 입력 → 수동 폼)하거나 그대로 둔다(호환 유지).

---

## 4.5 ★ path 규약 (v0.3 신설)

path는 트리의 골격이다. 아무렇게나 정하면 위키가 아니라 서랍 더미가 된다. 규약은 아래 다섯이면 충분하고, 축의 실제 내용은 팀이 정한다(3장-4·10).

### 원칙 (팀·회사 무관하게 통함)
1. **성격으로 묶는다.** 판단이 걸리는 성격이 같으면 같은 path. 문서 종류·계약 종류가 달라도 같은 노드에 병합돼 두터워진다(5장 병합 규칙과 정확히 부합).
2. **문서 종류·거래처는 provenance에.** path에 넣지 마라. "이 판단이 어느 계약에서 나왔다"는 판단의 성격이 아니라 판단의 이력이다.
3. **대분류 3~7개 유지.** 넘치면 축이 뭉개진 것 — 통합할 수 있는지 본다.
4. **한 대분류에 hold가 절반 넘으면 재분류 신호.** 그 축이 실무의 판단 성격과 안 맞다는 뜻. 세분화하거나 축 이름을 다시.
5. **path는 이동 가능해야 한다** (미래 조각). taxonomy는 성장하며 변한다.

### AI path 제안 (v0.3 필수 기능 · 조각 5로 실체화)

어떤 편집 방식(6.1~6.4)이든 판단문이 입력되면 AI가 트리 현재 상태를 근거로 path 3개까지 제안한다:
- 기존 대분류에 잘 맞으면 그 안으로 (예: `책임·분쟁 > 배상 > 상한`).
- 기존과 안 맞으면 새 대분류를 제안 + 왜 새로 필요한지 한 줄.
- 사람이 accept 또는 override. **AI 제안은 강제 아니다.**
- 사용자의 override 패턴은 다음 제안에 자연스레 반영된다(같은 트리 상태를 참조하므로).

이 기능이 3장-10의 실현 — 시작 발판(아래)에서 시작해 회사·팀 색으로 진화하는 유일한 메커니즘. AI 제안 없으면 taxonomy는 첫 결정에 굳어져 진화 못 함.

### 시작 발판 — 법무팀 예시 (강제 아님)

아래 6분류는 여러 회사 법무팀에서 자주 나타나는 축이다. **그대로 쓰라는 게 아니라**, 첫 판단 몇 개를 얹을 발판이다. 실무에서 판단이 쌓이며 자기 팀 색으로 진화해야 한다 — AI 제안이 그 진화의 엔진.

- **책임·분쟁** — 배상, 면책, 위험 배분, 분쟁 해결, 준거법
- **정보·기밀** — 비밀유지, 개인정보, 데이터·AI 학습, 공개 의무
- **자산·거래** — 대금·정산, IP·라이선싱, 산출물 귀속
- **절차·형식** — 통지, 승인, 기한, 계약 형식
- **관계 유지** — 해지, 갱신, 이전, 인수합병
- **신흥 이슈** — AI·크립토·데이터거버넌스 등 축이 아직 안 잡힌 것들

**회사·조직별로 자연스레 갈리는 지점 (참고):**
- 소송 비중 큰 팀 → **송무·자문** 축이 별도 대분류
- 스타트업 인하우스 → **투자·사업제휴** 축이 대분류
- 금융·의료 등 규제산업 → **규제·컴플라이언스** 축이 대분류
- 크로스보더 많음 → **관할·준거법** 축이 대분류

핵심은 이름이 아니라 **판단이 걸리는 성격으로 묶기.** 위 이름을 그대로 쓸 필요 없다. 같은 회사 법무팀 안에서도 팀·시대에 따라 다르다.

### 다른 팀 유형은?

법무 외 팀(인사·개발·마케팅 등)은 이 문서에 시작 발판을 두지 않는다. 원칙(위 5개)만 지키면 축은 스스로 세울 수 있고, AI 제안이 축을 진화시킨다. 필요 시 나중에 팀 유형별 발판을 부록으로 추가.

---

## 5. 저장·병합·로그 (코어)

- localStorage 키 `codex_tree`에 룰 **평면 배열**로 저장. 새로고침해도 유지.
- **병합:** 같은 `path`의 룰이 있으면 새로 만들지 말고 병합 — `held/total` 합산, `note`·`badge`·`strength_label` 최신 갱신, **`history`·`authors`는 누적(덮어쓰지 않는다).** 없으면 추가.
  - 병합의 값: 계약 마이닝 룰에 검토 추출·인라인 편집이 합쳐지면 "통계적으로도 그렇고 김변호사도 그렇게 판단했고 이변호사가 한 줄 더 확인함"으로 근거가 두터워진다.
- 가지(대분류·중분류)는 저장하지 않는다. `path`에서 생성.
- ✚ **편집 로그 자동화 (v0.3).** 모든 편집 방식(6.1~6.5)이 저장 시 **`history`에 `{who, action, at}` 한 줄을 자동 append**한다. 액션 어휘는 최소 고정: `인라인 편집으로 등재`·`판단문 수정`·`path 이동`·`kind 변경`·`승인`·`삭제`·`검토 추출로 등재`·`계약 마이닝으로 등재`. 사용자는 이 기록을 직접 쓰지 않는다.
- "최근 변경" 화면은 모든 룰의 `history`를 시간순으로 모아 표시. 각 항목에 **누가·무엇을·언제**가 반드시 나온다.

---

## 6. ★ 편집 방식 (v0.3에서 "입구"에서 개칭)

각 편집 방식은 코어 스키마(4장)를 반환하기만 한다. 방식을 더해도 코어는 안 바뀐다. **무거움 순으로 아래처럼.** (✚ = v0.3에서 위상이 바뀐 것)

### 6.1 ✚ 인라인 편집 (기본 · v0.3 신설)
**위키 페이지에서 노드 옆 `+`를 누르면 한 문장 입력창이 뜨고, 엔터로 끝난다.**
- **자동 채움:** `path`는 클릭한 위치에서 파생(예: "책임·분쟁 > 배상" 아래에서 누르면 그 두 단계까지 자동, 세 번째 단계는 판단문 앞머리에서 자동 추출하거나 빈 값). `kind`는 기본 `soft`(옆 칩 클릭 한 번으로 hard/neu/hold 변경 가능). `authors`는 설정의 내 이름 자동. `origin`=`인라인 편집`, `provenance`=null.
- **입력 필드는 판단문 하나뿐.** note·근거·held/total·강도 세부는 나중에 룰 페이지에서 다듬을 수 있다(위키피디아식 사후 편집).
- **왜 기본인가.** 위키의 성질은 진입장벽 제로다. 지식 넣는 걸 즐기는 사람이든 아니든, 회의 중 "이건 트리에 있어야 한다" 싶을 때 5초 안에 끝나야 한다. 3장-9의 실현.

### 6.2 수동 폼 (약간 신경 쓴 편집 · v0.2 "수동 입력"에서 개칭)
현행 폼 방식. 인라인보다 필드가 몇 개 더 있다 — `path` 3단·판단문·`kind`·note·held/total. **인라인이 부족할 때, 룰을 처음부터 신중히 세팅하고 싶을 때.** `origin`=`수동 폼`, `authors`=[내 이름], `provenance`=null. "…더보기"에서 접근(자주 쓰면 사이드로 승격 옵션).

### 6.3 ✚ 검토 추출 4단계 (파워유저용 · v0.3에서 격하)
**v0.2에서 이 시스템의 핵심 신규였으나, v0.3에서 "여러 편집 방식 중 하나"로 격하한다.** 이유: 이 방식을 좋아하는 실무자도 있지만, 매일 이 4단계를 밟기엔 무거운 워크플로우다. 편집을 즐기는 사람에게만 쓸모 있는 옵션이면 기본이 될 수 없다(3장-9).
- 여전히 지원. **"…더보기"에서 접근.**
- 프로세스는 그대로: (1) 검토한 계약 넣기 (2) AI 조항별 추출(판정 아님, "이렇게 판단하셨네요") (3) 사람이 조항별 넣기/수정/빼기(빼기 이유 필수) (4) provenance·authors 붙여서 저장.
- 이걸로 들어온 룰은 `provenance`가 채워진다(인라인·폼과 다른 지점 — 계약 검토를 실제로 하고 뽑은 판단이라 출처가 있다).
- `origin`=`검토 추출`.

### 6.4 계약 마이닝 대량 (초기 부트스트랩용)
과거 서명본 여러 건 → diff → "우리는 통계적으로 이렇게 해왔다". **트리를 처음 채울 때 한 번 돌리는 용도.** 평상시 편집 방식이 아니다. "…더보기"에서 접근. 표준안 대비 관철/양보로 `kind` 판정. `origin`=`계약 마이닝(diff)`, `authors`=[초안:AI](승인은 사람이).

### 6.5 인터뷰 채굴 (자리만, 지금 안 만듦)
AI가 구체적 상황을 묻고("배상 무제한 요구받으면?") 사람이 답하면 AI가 룰로 정규화, 사람이 확인해 넣는다. 문서에 없는 암묵지용. `origin`=`인터뷰 채굴`, `authors`=[대화한 사람]. "…더보기"에 '준비 중' 비활성.

---

## 7. 렌즈·찾기 — 지식을 꺼내는 방법

트리 노드는 **판단 기록**이자 **검토 기준**이다. 꺼내는 두 방법 모두 "트리를 근거로 LLM이 답한다"는 같은 엔진.

### 7.1 검토 렌즈 (입력을 비추기)
새 입력(계약 조항·법령 변경)을 트리에 비춰 **위반/해당/보류/무관** 판정. 걸린 노드를 트리에서 하이라이트. **"…더보기"에서 접근.**

### 7.2 AI 찾기 (질문을 비추기) — 위키에 상주
사람이 묻고("우리 배상 방침 뭐야?") AI가 트리를 근거로 정리해 답한다. **위키 화면 우측에 상주** — 검색바와 함께 정문 역할. 답에는 반드시:
- **근거 룰 링크** — 클릭하면 그 룰 페이지로.
- **저작자** — "이 룰은 김변호사가 3월에 정함". AI는 전달자, 신뢰의 출처는 사람.
- 트리에 근거가 없으면 "확정 기준 없음(보류)"이라고 정직하게. **인용하지 창작하지 않는다.**

### ★ 미탐 우선 원칙 (양보 불가, 두 방법 공통)
확실치 않으면 무관이 아니라 보류. 트리에 기준 없는 관련 입력/질문은 반드시 보류로 사람에게 올린다. **놓치는 것이 틀리는 것보다 위험하다.**

---

## 8. AI의 두 역할 (중심은 언제나 사람)

같은 대화형 AI가 두 방향으로 작동한다.
- **편집을 도울 때(6.3·6.4·6.5):** AI가 묻거나 분석하고, 사람이 판단·선택하고, **그 사람이 저작자**가 된다. 인라인·폼(6.1·6.2)은 AI 개입 없이 사람이 곧장 쓴다.
- **찾을 때(7.2):** 사람이 묻고, AI가 트리에서 답하고, **저작자를 밝힌다.**

편집 도울 때도 사람이 주인, 꺼낼 때도 사람이 신뢰의 출처. "누가 쓰느냐"(2장)가 구조로 실현되는 지점.

---

## 9. md 내보내기 (사람+기계 한 장, + 저작·출처)

'위키 md 저장' → `codex_wiki_YYYY-MM-DD.md`.
- `#` 제목 + 요약(총 규칙 수 / 분포) → `##` 대분류 → `###` 중분류(다수 hold면 ⚠) → 룰은 `- **[HARD] 판단문** \`관철 4/4\`` 아래에 근거·origin·status·**저작자·출처**·재검토 트리거를 하위 항목으로.
- **문서 말미 `## 기계 판독용 데이터`에 ```json 으로 전체 룰 배열(스키마 그대로).** 사람은 위를, AI는 이 블록을 읽는다.
- 이 파일이 "앱이 없어져도 위키의 판단과 그 책임 소재는 남는다"의 물증이다.

---

## 10. ★ 화면 — 위키가 곧 앱 (v0.3 전면 재작성)

**v0.2 3탭 구조([지식 넣기]·[위키]·[검토하기]) 폐기.** 위키 화면이 앱 셸이다. 위키 안에서 편집·검색·AI 대화가 상주하고, 무거운 유틸(수동 폼·검토 추출·계약 마이닝·검토 렌즈)은 상단 "…더보기"로 격리.

### 10.1 위키 화면 (앱 셸)
- **상단 바:** 로고 · **검색바(중앙, 정문)** · "…더보기" · ⚙ 설정.
- **좌 (25%) — 목차.** `path` 기반 접이식 트리. 항목마다 강도색 dot, 다수 hold 가지엔 ⚠. **각 노드 옆에 `+` 버튼(6.1 인라인 편집).** 대분류·중분류 옆에도 `+` — 그 위치에 바로 판단 하나가 붙는다.
- **중 (50%) — 룰 페이지.** ✚ **본문은 판단 그 자체(콘텐츠)만 흐른다:** 판단 → 강도 → 관철/양보 → 근거(note) → hold면 왜 갈렸는지 → 재검토 트리거. **저작·이력·출처(누가·언제·어디서)는 본문에 박지 않고 상단 `[역사]` 토글 뒤로 숨긴다** — 나무위키/위키피디아의 `[편집]`·역사와 같은 방식. 2장은 판단의 '기록'을 요구하지 '본문 노출'을 요구하지 않는다. 누가·언제는 반드시 기록하되 읽기 화면을 어지럽히지 않는다. 페이지 하단에 `[+ 이 문단에 한 줄 더]`(같은 path 인라인 추가). 각 필드 마우스오버 시 인라인 편집, 저장 시 `history`에 자동 기록.
- **우 (25%) — AI 찾기(7.2).** 항상 상주. 질문 → 근거 룰 링크 + 저작자. 클릭 시 중앙이 그 룰 페이지로 전환.

### 10.2 상단 검색바 (정문)
- 키워드 즉시 필터(목차에 반영) + 엔터 시 AI 찾기로 승격. **법령정보센터·위키피디아처럼 검색이 정문.**

### 10.3 "…더보기" 유틸
평상시 안 쓰는 무거운 편집·렌즈. 5개:
- 수동 폼(6.2) — 자주 쓰면 사이드로 승격 옵션.
- 검토 추출 4단계(6.3).
- 계약 마이닝 대량(6.4).
- 검토 렌즈(7.1).
- 인터뷰 채굴(6.5, 준비 중).

### 10.4 ⚙ 설정
- API 키, 모델, **내 이름**(저작자 자동 삽입용, 편집 필수 조건 유일).

### 10.5 편집 UX 규약 (3장-9의 실현)
- **첫 편집 시** 이름 없으면 이름 입력창 한 번 → 이후 조용.
- **인라인 편집 폼은 필드 하나(판단문).** 다른 값은 컨텍스트·설정·기본값에서. path 세부는 저장 후 룰 페이지에서 조정.
- **모든 편집·수정·삭제·승인 액션은 `history`에 자동 기록** (5장). 사용자가 로그를 신경 쓰지 않는다(투명 기록). 로그는 룰 페이지의 `[역사]` 토글과 '최근 변경'에서 본다 — 본문(읽기 화면)엔 노출하지 않는다.
- 편집 후 성공 표시는 짧게(플래시 1초). 흐름을 방해하지 않는다.

---

## 11. 색·비주얼 규약 (모든 화면 공통)

- 배경 `#E9EBE6` · 패널 `#F5F6F3` · 잉크 `#14171C`.
- 강도색: hard `#9E3524` · soft `#C8871F` · neu `#2FA97F` · hold `#8A9086`(+점선·반투명).
- 폰트: 본문 Pretendard, 숫자·라벨 IBM Plex Mono. 테두리 2px.
- 코디(두더지·헤드램프): 위키를 가꾸는 안내자. 친근한 존댓말, 말풍선당 이모지 최대 1개, 권위자 톤 금지("모르면 저도 잘 모르겠어요").

---

## 12. 지금 범위 vs 나중 범위 (v0.3 재배열)

**지금 (대회 기간에 실제로 돌린다):**
- 코어: 트리 저장·병합·로그, md 내보내기.
- 스키마 확장: `authors`·`history`·`provenance`(선택). 편집 액션마다 자동 append.
- 편집 방식: ✚ **인라인 편집(6.1) — 새 기본**, 수동 폼(6.2, 기존 "수동 입력" 개칭).
- 화면: **위키 셸(10.1) + 검색바(10.2) + AI 찾기(7.2) 상주.** "…더보기"는 자리만 있어도 됨.
- 설정: 내 이름.
- ✚ **AI path 제안(4.5 · 조각 5)** — 인라인·폼·추출 편집 시 판단문 → AI가 트리 근거로 path 3개 제안 → 사람이 accept/override. taxonomy가 자기 팀 색으로 진화하는 엔진. **없으면 taxonomy는 첫 결정에 굳어져 위키가 못 자란다.**

**선택 (되면 좋지만 데모 필수는 아님):**
- 검토 추출 4단계(6.3) — 파워유저용, "…더보기"에 실제 구현. (v0.2 지시서 결과물을 격하해 재배치.)
- 검토 렌즈(7.1).
- 계약 마이닝 대량(6.4) — 이미 구현돼 있으니 "…더보기"에 격리만.

**나중 (자리만 표시, 안 만듦):**
- 인터뷰 채굴(6.5), 규정 마이닝, 파일 OCR·hwp, SSO 저작자, Notion·공유 저장소.
- path 이동(재분류) UI — 4.5장 원칙 5.
- 팀 유형별 시작 발판 부록(인사·개발·마케팅 등).

> 다 만들려다 아무것도 안 도는 게 최악. **좁게, 그러나 진짜로.** v0.3의 좁고 진짜는 **위키 셸 + 인라인 편집 + 저작자 자동 + AI 찾기 + AI path 제안.** 이 다섯이 되면 "위키가 곧 앱, 아무나 툭, taxonomy가 자란다"가 보인다.

---

## 13. 완료 기준 (뭐가 되면 "된다") — v0.3

1. ✚ **위키 화면이 앱 정문이다.** 열면 목차/룰 페이지/AI 찾기가 한 화면에 있다. [지식 넣기] 같은 별도 탭은 없다.
2. ✚ **인라인 편집이 5초 안에 끝난다.** 노드 옆 `+` → 판단 한 문장 → 엔터 → 트리에 박힘. path·kind·저작자 자동. 새로고침해도 남는다.
3. ✚ **편집 액션마다 `authors`·`history`에 누가·언제·무엇이 자동으로 박힌다.** 사용자는 신경 쓰지 않는다. 룰 페이지·최근 변경에 그냥 보인다.
4. **이름 없이 편집 시도 → 이름 입력 한 번으로 유도, 이후 자동.** 저작자 없는 룰은 없다.
5. **같은 path 재입력 시 병합** — history·authors 누적(덮어쓰지 않음).
6. **AI 찾기:** 질문 → 트리 근거 답 + 근거 룰 링크 + 저작자. 기준 없으면 '보류'.
7. **md 내보내기:** 헤딩 트리 + 말미 JSON 블록 + 저작·출처 포함.
8. ✚ **AI path 제안(4.5 · 조각 5):** 인라인·폼 편집에서 판단문 입력 → AI가 트리 현재 상태 근거로 path 3개 제안 → 사용자가 클릭(accept)·수정(override). override는 그대로 저장돼 다음 제안에 반영. 시작 발판만 있고 override 반복이 taxonomy를 진화시킨다.
9. (선택) 검토 추출 4단계·검토 렌즈·계약 마이닝 — "…더보기"에서 접근 가능, 결과 룰은 위키 트리에 그대로 합류.

---

## 14. 불가침 요약 (한눈에)

- ✚ **위키가 곧 앱이다.** "지식 넣기" 별도 프레임 없다. 위키 안에서 편집·검색·AI 대화 상주.
- ✚ **편집 장벽은 최대한 낮게.** 기본은 인라인 한 줄. 무거운 워크플로우(검토 추출 등)는 파워유저 옵션.
- 코어 ↔ 편집 방식/렌즈 분리. 코어는 편집 방식 추가에도 안 바뀐다.
- 모든 편집 방식은 4장 스키마만 뱉는다. `kind`(강도)는 빈도와 다르다.
- 트리는 뷰, 저장은 평면 배열, 가지는 `path`에서 생성(하드코딩 금지).
- ✚ **path는 성격으로 묶는다** (문서 종류가 아니라 · 4.5장). 축은 팀이 정하되 시작 발판 + AI 자동 제안으로 진입 장벽 없이 시작해 자기 팀 색으로 진화.
- **주인 없는 판단은 트리에 없다.** authors·history·provenance는 필수 축. ✚ 저작자 자동 삽입, 편집 액션마다 history 자동 append로 강화.
- 원본은 안 남기되 출처는 남긴다. ✚ 인라인·폼은 provenance=null 허용.
- AI는 초안, 일반/특수 판단과 등재는 사람.
- 데이터 반출·키 커밋 금지, 로컬 전용.
- 찾기·검토는 확실치 않으면 보류(미탐 우선). AI는 인용하지 창작 안 한다.
- md 말미 JSON 블록은 반드시(사람+기계 한 장).

---

## 15. 이전 판 히스토리

**v0.1 — 채굴 도구.** "AI가 계약을 채굴해 트리에 쌓는 도구." 코어(스키마·트리·저장·병합·md)와 계약 마이닝·수동 입력 입구, [위키트리] 화면이 뼈대. 저작·출처 개념 없음. 지금 코드(`codex-mining-tool`)의 골격이 여기서 왔다.

**v0.2 — 저작자 축.** 무게중심을 "누가 썼는가"로 이동. `authors`·`history`·`provenance` 신설, **검토 추출 4단계 입구를 핵심 신규로** 세움, AI 찾기 신설, 위키 3단 화면(목차 + 룰 페이지 + AI 대화). 스키마 골격은 v0.3까지 그대로 계승. 실제 코드에는 v0.2 스키마·검토 추출·AI 찾기가 아직 반영되지 않은 상태에서 v0.3로 넘어감.

**v0.3 — 위키 한 층.** "지식 계층 + AI 인터페이스" 이분법 폐기, **위키가 곧 앱.** "지식 넣기" 프레임 폐기, 편집은 위키 안 곳곳에서 **인라인**. 검토 추출은 여전히 지원되나 파워유저 옵션으로 격하. **편집 장벽 최소 원칙 신설**, 저작자 자동 삽입 + 편집일 자동 기록으로 필수성과 낮은 장벽을 양립. ✚ **path 규약(4.5장)과 AI 자동 제안**을 신설 — taxonomy가 시작 발판에서 회사·팀 색으로 진화하는 엔진 확보. 코어 스키마·저장·병합·md 규약은 v0.2 그대로 계승, 값(`origin`) 재편.

---

*v0.3 — 무게중심을 "위키가 곧 앱, 편집은 아무나 툭"으로 옮긴 판. 이 문서가 헌법이다. 구조를 바꿔야 하면 이 문서를 먼저 고친다.*
