# RADAR 판단 기준서 v0.1

> 법령 변경이 우리 회사의 **무엇을** 건드리는지 판정하는 규칙.
> 사람이 읽는 문서이자, 에이전트 프롬프트의 원본이다.
> 조문 번호는 이 문서에 넣지 않는다 — 조문은 개정될 때마다 바뀌고, 판정은 법령·제도 단위로 충분하다.

---

## 1. 자산 대장 스키마

매칭의 대상이 되는 우리 쪽 목록. SharePoint 리스트 1개로 시작한다.

| 필드 | 설명 | 예시 |
|---|---|---|
| 자산ID | 고유번호 | AST-0001 |
| 자산유형 | 계약 / 제품 / 인허가 / 사내규정 | 인허가 |
| 자산명 | | (제품·계약명) |
| 소관부서 | 변경 시 통지받을 부서 | |
| 근거 법령군 | 이 자산이 딛고 선 법 | (아래 2.1) |
| 규제태그 | 매칭 키 (복수) | 유해화학물질, 취급시설, 표시 |
| 기한 | 갱신·만료·재신고일 | |
| 담당자 | | |

**초기 구축 범위: 대표 50건.** 전수는 필요 없다. 자산유형 4종이 모두 들어가고, 심각도 '상'이 나올 만한 건이 최소 5건 포함되면 검증에 충분하다.

### 1.1 규제태그 사전

태그는 자유 입력이 아니라 **닫힌 목록**이어야 매칭이 된다. 법령 조문 용어가 아니라 **현장에서 쓰는 말**로 만든다.

- 예: `유해화학물질` `취급시설` `표시·포장` `수입신고` `임상` `제조판매` `개인정보` `하도급` `안전보건`
- 규칙: 태그 1개당 자산이 3건 미만이면 너무 잘게 쪼갠 것 → 상위 태그로 합친다.

---

## 2. 규제 변경의 유형 분류

**이 문서의 핵심 축.** 변경의 성격에 따라 어느 자산이 걸리는지가 갈린다.

| 코드 | 유형 | 성격 | 주로 걸리는 자산 |
|---|---|---|---|
| **A** | 의무 신설·강화 | 안 하던 걸 해야 함 | 인허가, 사내규정 |
| **B** | 기준 수치 변경 | 한도·농도·기한 조정 | 제품, 인허가 |
| **C** | 적용 대상 확대 | 무관하던 대상이 규제권에 편입 | **전 유형** |
| **D** | 절차·서식 변경 | 방법만 바뀜 | 사내규정 |
| **E** | 제재·책임 강화 | 위반 효과가 커짐 | 계약 |

### 2.1 유형별 주의점

- **C가 가장 위험하다.** 지금까지 태그가 안 붙어 있던 자산이 갑자기 규제 대상이 되는 유형이라, 태그 매칭만으로는 **구조적으로 놓친다**. C로 분류되면 태그 일치 여부와 무관하게 사람 확인 큐로 보낸다.
- **E는 법령이 아니라 계약을 본다.** 제재가 세지면 우리 계약의 배상·면책·해지 조항이 실질적으로 불리해진다. 법령 소관부서에 알릴 게 아니라 법무가 계약을 다시 꺼내야 하는 케이스.
- **D는 대량으로 쏟아진다.** 여기서 알림 피로가 생기므로 기본값을 '주간 묶음 통지'로 둔다.

---

## 3. 심각도 판정

두 축의 곱으로 정한다. 감으로 정하지 않는다.

**축1 — 위반 효과**
- 3점: 사업 중단, 인허가 취소, 형사처벌
- 2점: 과태료, 시정명령, 회수·폐기
- 1점: 절차 위반, 경고

**축2 — 시행까지 남은 기간**
- 3점: 90일 이내 (또는 이미 시행)
- 2점: 90~180일
- 1점: 180일 초과 또는 미정

| 합계 | 심각도 | 처리 |
|---|---|---|
| 5~6 | **상** | 즉시 개별 통지 + 법무 동시 통지 |
| 3~4 | **중** | 주간 리포트 상단 |
| 2 | **하** | 주간 리포트 묶음 |

---

## 4. 매칭 절차

```
[법령 변경 감지]
      │
 1차  ├─ 법령군 필터 ─────── 우리 자산의 근거 법령군에 없으면 → 종료(로그 남김)
      │
 2차  ├─ 태그 매칭 ────────── 규제태그 교집합 있으면 → 후보
      │
 3차  ├─ LLM 판정 ─────────── 태그 불일치라도 문언상 관련 있으면 → 후보로 승격
      │                        (유형 C 대응. 이 단계가 미탐을 막는 유일한 장치)
      │
      └─ 심각도 산정 → 라우팅
```

### 4.1 미탐 방지 규칙 (양보 불가)

1. 유형 C로 분류된 건은 **무조건** 사람 확인 큐에 올린다.
2. 위반 효과 3점이면 태그 불일치여도 후보로 남긴다.
3. 1차 필터에서 걸러진 건도 **로그를 남긴다.** 나중에 놓친 게 발견되면 어디서 떨어졌는지 추적해야 규칙을 고칠 수 있다.

> 법무에서는 오탐보다 미탐이 치명적이다. 알림이 좀 많은 건 참을 수 있지만, 놓친 규제는 못 돌린다.

### 4.2 판정 보류

근거가 부족하면 억지로 매칭하지 않는다. `판정보류` + 보류 사유를 남기고 사람 확인 큐로 보낸다. **모르는 걸 모른다고 하는 게 이 시스템의 신뢰도다.**

---

## 5. 라우팅 기본값

| 자산유형 | 1차 수신 | 법무 동시 통지 |
|---|---|---|
| 인허가 | 소관부서 | 심각도 상 |
| 제품 | 소관부서 + 품질 | 심각도 상 |
| 계약 | 법무 | 항상 |
| 사내규정 | 소관부서 | 유형 A |

---

## 6. 출력 스키마

에이전트 → 통지 플로우 사이의 계약. 이 형식이 고정돼야 자동화가 붙는다.

```json
{
  "detected_at": "2026-08-05",
  "source": "법령명 / 개정 공포일 / 시행일",
  "change_type": "A|B|C|D|E",
  "summary": "무엇이 어떻게 바뀌는지 2문장",
  "matched_assets": [
    {
      "asset_id": "AST-0001",
      "asset_name": "",
      "match_basis": "tag|llm|manual",
      "impact": "이 자산에 무슨 일이 생기는지 1문장",
      "severity": "상|중|하",
      "severity_score": {"effect": 3, "timing": 2},
      "owner_dept": ""
    }
  ],
  "unresolved": [
    {"reason": "판정보류 사유", "needs_human": true}
  ],
  "action_deadline": "2026-11-01"
}
```

---

## 7. 평가셋

발표에 넣을 숫자는 여기서 나온다.

- **정답 20건**: 과거 1년치 실제 개정 중 20건을 골라, 어느 자산이 걸렸어야 했는지 사람이 먼저 답을 적어둔다.
- **1순위 지표: 미탐률** (걸렸어야 했는데 안 걸린 비율). 정확도가 아니다.
- **2순위: 리드타임** — 공포일 → 담당부서 통지까지. 현재 방식과 비교한 수치가 SUPEX 항목의 핵심 근거가 된다.

---

## 8. 아직 비어 있는 칸

이 문서에서 회사 실제 정보가 필요한 부분. 여기를 채우는 게 실질 작업이고, 남이 못 베끼는 부분이다.

- [ ] **2.1 법령군 목록** — 우리 사업이 딛고 선 법 (그린케미칼/라이프사이언스 각각)
- [ ] **1.1 규제태그 사전** — 20~30개 내외
- [ ] **5 라우팅 부서 실명**
- [ ] **7 평가셋 20건 정답**

---

*v0.1 — 예선 제출용 초안. 실제 매칭 3건 시연이 붙으면 구현 가능성 항목은 충분하다.*
