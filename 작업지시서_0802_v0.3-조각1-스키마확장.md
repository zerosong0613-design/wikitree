# 작업지시서 v0.3 — 조각 1 : 코어 스키마 확장 + 저작자 자동 (2026-08-02)

> 먼저 `CLAUDE.md v0.3`을 읽는다. 이 지시서는 그 위에 얹는 **첫 조각**이다.
> v0.3 마이그레이션은 5조각으로 나뉜다(조각 1~5). 이건 **조각 1: 코어 기반.**
> 화면은 건드리지 않는다. 오늘 만드는 건 뒤 조각들이 딛고 설 **코어 확장**이다.

---

## 0. 시작 전 전제 확인

- [ ] `CLAUDE.md v0.3`을 읽었다(특히 2장·3장 8·9·4장·5장·10.5).
- [ ] 현재 코드: 트리가 `codex_tree`에 쌓이고 새로고침해도 남는다.
- [ ] 현재 코드: 룰이 v0.1 스키마(authors/history/provenance 없음)로 저장돼 있다.
- [ ] 지금 존재하는 입구 두 개(`entries/MiningEntry.jsx`·`entries/ManualEntry.jsx`)와 코어(`core/schema.js`·`core/store.js`)의 위치를 안다.

전제가 되어 있으면, **화면 코드는 건드리지 말고** 스키마·store·설정만 확장한다. 화면(App.jsx·WikitreeTab.jsx·KnowledgeTab.jsx)은 조각 2에서 뒤엎으니 지금은 손대지 않는다.

---

## 1. 목표 (한 문장)

모든 편집 방식이 저장하는 순간, **누가·언제·무엇을**이 룰의 `authors`·`history`에 자동으로 박혀 트리에 들어간다 — v0.3의 "편집 장벽 최소 + 저작자 필수"(3장-9, 2장) 원칙을 코어에서 실현한다.

---

## 2. 오늘 범위 (이것만)

- **스키마**: `normalizeRule`에 `authors`·`history`·`provenance` 필드 추가. 없는 값은 안전한 폴백(빈 배열/null).
- **`origin` 값 재편** (CLAUDE.md 4장 ✚): "수동 입력" → "수동 폼"(개칭), 새 값 "인라인 편집" 허용. 기존 저장 값과 호환.
- **store.js merge 강화**: 같은 path 병합 시 `history`·`authors` **누적**(덮어쓰지 않음).
- **편집 헬퍼 신설** (`core/authoring.js`): 내 이름 읽기, `authors`·`history` 자동 stamp, 이름 없을 때 이벤트 트리거.
- **Settings에 내 이름 필드**: localStorage `codex_author_name`.
- **기존 두 입구 미세 수정**: `MiningEntry`·`ManualEntry`가 저장 시 이 헬퍼를 통과하도록. `origin` 값 정리("수동 입력" → "수동 폼", "계약서 마이닝(diff)" → "계약 마이닝(diff)" 유지).

**오늘 안 하는 것 (조각 2~5의 몫):**
- App.jsx 탭 개편, 위키 셸, "…더보기" 드로어 — **조각 2**.
- 인라인 편집 UI, 이름 없을 때 뜨는 모달, 룰 페이지 마우스오버 편집 — **조각 3**.
- AI 찾기 우측 패널 — **조각 4**.
- 검토 추출 4단계, 검토 렌즈 — **조각 5**.
- 기존 v0.1 룰의 마이그레이션 스크립트 — 하지 않는다. 폴백만으로 호환.

---

## 3. 스키마 — 저작·이력·출처 3필드

CLAUDE.md 4장의 필드를 `normalizeRule`에 추가한다. 기존 v0.1 스키마 룰과 **완전 호환**(없으면 폴백).

```json
"authors": [{"name": "김변호사", "role": "편집|수정|승인|삭제|초안", "at": "2026-08-02T14:03:12Z"}],
"history": [{"who": "김변호사", "action": "인라인 편집으로 등재|판단문 수정|승인|...", "at": "2026-08-02T14:03:12Z"}],
"provenance": null 또는 {
  "source_contract": "계약명 (시점)",
  "reviewed_by": "검토자",
  "extracted_at": "YYYY-MM-DD",
  "source_article": "제3조",
  "extraction_method": "검토 추출 | 계약 마이닝 | 수동 | 인터뷰"
}
```

**필드 규칙(4장 그대로):**
- `authors`는 현재 주인(중복 이름은 최신 role로 갱신), `history`는 시간순 전체 이력(절대 압축·삭제 없음).
- `provenance`는 **선택**. 인라인 편집·수동 폼은 `null`. 검토 추출·계약 마이닝은 채워짐.
- `origin` 허용 값: `"인라인 편집" | "수동 폼" | "검토 추출" | "계약 마이닝(diff)" | "인터뷰 채굴"`. 다른 값은 그대로 통과(외부 데이터 호환), 검증만 하지 않음.

**폴백(v0.1 호환):**
- `authors` 없으면 `[]`, `history` 없으면 `[]`, `provenance` 없으면 `null`.
- 기존 룰이 오류 없이 로드돼야 한다.

---

## 4. store.js — 병합 시 누적

`merge()` 함수 강화 — 같은 `path` 룰이 이미 있으면:
- **누적**: `history`는 두 배열을 concat(중복 제거 없음 — 편집 시각까지 같이 저장되면 서로 다른 액션이라 봐도 됨), `authors`는 이름 단위로 dedupe하되 role은 최신 값 유지(같은 사람이 초안→편집→승인 순으로 진행하면 최신 role로).
- **덮어쓰지 않음**: v0.2 원칙 그대로(4장·5장). 기존 `held/total` 합산, `note`·`badge`·`strength_label` 최신 갱신, `review_trigger.keywords` 합집합, `status`는 한쪽이라도 approved면 approved — 이건 유지.
- `provenance`는 병합 대상 아님(각 편집 이력은 `history`가 잡음). 새 값이 있으면 최신 유지, 없으면 기존 유지.

---

## 5. `core/authoring.js` — 편집 헬퍼 (신설)

편집 UI가 무엇이든, 저장 직전에 이걸 통과하면 저작자·history가 자동으로 박힌다.

**함수:**

```js
// localStorage 키
export const AUTHOR_KEY = "codex_author_name";

// 현재 편집자 이름 (없으면 "")
export function getAuthorName() { ... }
export function setAuthorName(name) { ... }

// 룰에 저작자·history를 자동 stamp해서 반환. action은 5장의 어휘 중 하나.
// - 이 룰이 처음 등재되는 경우: authors에 {name, role: 초기 role, at: now} 추가, history에 append.
// - 이 룰이 이미 있는 룰의 수정인 경우(같은 path 병합): 병합은 store가 하니, 여기선 그냥 stamp만.
export function stampAuthoring(rule, action) {
  const name = getAuthorName();
  if (!name) throw new NoAuthorError();
  const at = new Date().toISOString();
  const role = actionToRole(action); // "인라인 편집으로 등재" → "편집", "판단문 수정" → "수정", "승인" → "승인" 등
  return {
    ...rule,
    authors: [...(rule.authors || []), { name, role, at }],
    history: [...(rule.history || []), { who: name, action, at }],
  };
}

// 이름 없음을 UI가 잡을 수 있게 에러 클래스로 분리.
export class NoAuthorError extends Error {
  constructor() { super("저작자 이름이 필요합니다."); this.name = "NoAuthorError"; }
}

// 액션 어휘 → role 매핑(5장 액션 어휘 최소 고정)
const ACTION_ROLE = {
  "인라인 편집으로 등재": "편집",
  "수동 폼으로 등재": "편집",
  "검토 추출로 등재": "검토·추출",
  "계약 마이닝으로 등재": "초안",
  "판단문 수정": "수정",
  "path 이동": "수정",
  "kind 변경": "수정",
  "승인": "승인",
  "삭제": "삭제",
};
function actionToRole(action) { return ACTION_ROLE[action] || "편집"; }
```

**핵심:** 어떤 편집 UI든 저장 전 `stampAuthoring(rule, "인라인 편집으로 등재")` 같은 한 줄만 통과시키면 v0.3 원칙이 지켜진다. UI는 저작자 로직을 몰라도 된다.

---

## 6. Settings — 내 이름 필드

`components/Settings.jsx`에 필드 하나 추가. API 키와 나란히.

- 라벨: **"내 이름 (저작자)"**
- placeholder: "예: 김변호사"
- 저장 버튼과 함께 처리(API 키 저장 로직 옆에).
- 이름이 저장된 상태를 표시(마스킹 없이 그대로 — 자기 이름은 자기가 본다).
- 이름 삭제 버튼도 함께(선택).
- localStorage 키 `codex_author_name`.

설정 안내 문구 한 줄 추가:
> 편집 시 자동으로 룰의 저작자·이력에 이 이름이 박힙니다. 로컬에만 저장돼요.

---

## 7. 기존 두 입구 미세 수정

**UI는 안 바꾼다.** 저장 경로만 `stampAuthoring` 통과.

### 7.1 `entries/ManualEntry.jsx`
- `origin` 값: `"수동 입력"` → **`"수동 폼"`**.
- `addRules([rule])` 직전에 `stampAuthoring(rule, "수동 폼으로 등재")` 호출.
- `NoAuthorError` 잡히면 flash에 "이름을 먼저 설정(⚙)에서 넣어주세요"(모달 없이 안내만 — 모달은 조각 3).

### 7.2 `entries/MiningEntry.jsx`
- `origin` 값 그대로: `"계약 마이닝(diff)"` (기존 "계약서 마이닝(diff)"에서 미세 정리 — 헌법 4장 정한 값).
- 채굴 결과 각 룰에 `stampAuthoring(rule, "계약 마이닝으로 등재")` 통과. 계약 마이닝은 AI 초안이라 role="초안"으로 매핑됨.
- 이름 없으면 마찬가지로 flash 안내(설정 열기 유도).

**주의:** 계약 마이닝은 AI가 뽑은 것이므로 저작자를 "계약 마이닝(AI)"으로 넣을지, 사용자 본인 이름을 넣을지 애매하다. v0.3 원칙(2장 — 주인 없는 판단은 트리에 없다)에 따라 **사용자 본인 이름 + role="초안"** 으로 통일. "이 사람이 이 마이닝을 돌렸다"는 의미. 이후 승인은 조각 3에서.

---

## 8. 완료 기준

1. `normalizeRule`에 `authors`·`history`·`provenance`가 생기고, v0.1 룰(이 3필드 없음)이 오류 없이 로드된다.
2. Settings에 내 이름 필드가 생기고 저장·삭제된다. localStorage `codex_author_name`에 남는다.
3. 이름을 넣은 상태에서 **수동 폼**으로 룰 추가 → 그 룰의 `authors`에 내 이름 + role="편집", `history`에 `{who: 내이름, action: "수동 폼으로 등재", at: ISO}`가 자동으로 박힌다.
4. 같은 path로 **한 번 더** 추가 → `history`가 두 줄로 누적, `authors`도 누적(같은 이름은 dedupe·최신 role).
5. 이름 없이 저장 시도 → flash로 "이름을 먼저 설정(⚙)" 안내가 뜨고 저장은 되지 않는다.
6. 계약 마이닝을 돌리면 각 룰에 내 이름 + role="초안"이 박히고, `origin`="계약 마이닝(diff)"이다.
7. **화면(App·WikitreeTab·KnowledgeTab)은 하나도 안 바꿨다.** 이건 조각 2·3에서.

---

## 9. 끝나고 확인할 것 (나에게)

- **4번(누적)** 을 집요하게 본다. 같은 path에 두 번째 편집이 들어왔을 때 `history`가 **덮어써지면** 코어 원칙(5장) 위반이다. 반드시 concat.
- **5번(이름 없음)** — 지금은 flash로만 안내. 모달·자동 유도 UI는 조각 3에서 붙이지만, 지금 저장이 조용히 실패하면 안 된다(에러가 UI로 전해져야).
- `core/authoring.js` 단독 파일로 뽑아둔 게 맞다. 다음 조각(2·3)에서 이 함수만 import하면 인라인 편집·룰 페이지 편집이 저작자 로직을 다시 짜지 않는다. **재사용 가능성이 이 조각의 값이다.**
- 이 조각이 서면 v0.3의 다른 것들이 쌓일 수 있다: 위키 셸(조각 2), 인라인 편집(조각 3), AI 찾기(조각 4). 조각 1의 실패는 곧 4개 조각의 실패다.

---

## 10. 다음 조각 예고 (여기 만들지 않음)

**조각 2 — 위키 셸 재구조화.**
- App.jsx 3탭 폐기 → 위키 셸(좌 25% 목차 / 중 50% 룰 페이지 / 우 25% AI 찾기 자리) + 상단 검색바 + "…더보기" 드로어.
- 기존 `KnowledgeTab`·`MiningEntry`·`ManualEntry`는 "…더보기" 안에서 접근하게 재배치. UI 코드 자체는 대부분 그대로, 위치만 이동.
- 룰 클릭 → 중앙 룰 페이지에 저작·이력이 실제로 보인다(오늘 만든 데이터가 여기서 처음 눈에 띈다).

이 조각 1이 완료되면 조각 2를 다음 지시서로 발행한다.
