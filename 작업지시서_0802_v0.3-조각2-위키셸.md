# 작업지시서 v0.3 — 조각 2 : 위키 셸 재구조화 (2026-08-02)

> 먼저 `CLAUDE.md v0.3`과 조각1 지시서를 읽는다. 이 지시서는 그 위에 얹는 **두 번째 조각**이다.
> 오늘 만드는 건 **화면 전체 재구조화** — 3탭을 폐기하고 위키가 앱이 된다.
> 조각1이 자동으로 박은 저작·이력이 **룰 페이지에서 처음으로 눈에 보이는** 순간이다.

---

## 0. 시작 전 전제 확인

- [ ] `CLAUDE.md v0.3` 특히 **10장(화면), 3장 8·9(원칙), 2장(저작자)** 을 읽었다.
- [ ] 조각1 완료: `authors`·`history`·`provenance`가 실제로 저장된다. 이름 저장한 상태에서 룰 한두 개 이상 트리에 있어야 룰 페이지 데모가 된다.
- [ ] 조각1의 파일 위치를 안다: `core/authoring.js`·`core/schema.js`·`entries/ManualEntry.jsx`·`entries/MiningEntry.jsx`.

전제가 되어 있으면, **코어(`core/*`)와 조각1이 만든 것(`authoring.js`·`stampAuthoring`·저작 스키마)은 건드리지 않는다.** 오늘은 화면·구조만.

---

## 1. 목표 (한 문장)

탭 3개(`[지식 넣기]`·`[위키트리]`·`[검토하기]`)를 폐기하고 **위키를 앱 셸**로 만든다 — 좌: 목차 / 중: 룰 페이지(★조각1의 저작·이력이 실제로 표시된다) / 우: AI 찾기 자리 / 상단: 검색바 + "…더보기". 편집·마이닝·렌즈 같은 무거운 것은 "…더보기" 안으로 격리 (10장·12장).

---

## 2. 오늘 범위 (이것만)

- **App.jsx 재작성**: 3탭 폐기, 위키 셸이 유일한 화면. 상단 바(로고·검색바·…더보기·⚙ 설정).
- **WikiShell 신설**: 좌 25% · 중 50% · 우 25% 3판 그리드.
- **TocPanel 신설**: 좌측 목차 (기존 `WikitreeTab`의 toc 로직 흡수). 검색 필터 적용, 리프 클릭 → 선택 상태.
- **RulePage 신설**: 중앙 룰 페이지. 판단·강도·path·note·★**저작(authors)·이력(history)**·재검토 트리거·origin·status. 저작·이력 표시가 **이 조각의 시각적 핵심**.
- **AiFindPanel 신설**: 우측 자리만. 조각4에서 실체.
- **SearchBar 신설**: 상단 중앙. 키워드 즉시 필터(목차에 반영). 엔터 시 AI 찾기 승격은 조각4 몫, **지금은 필터만.**
- **MoreDrawer 신설**: "…더보기" 클릭 → 우측 슬라이드 패널. 5개 항목:
  - 수동 폼 (6.2, `ManualEntry` 재사용)
  - 계약 마이닝 (6.4, `MiningEntry` 재사용)
  - 검토 추출 4단계 (6.3, "준비 중 — 조각5")
  - 검토 렌즈 (7.1, "준비 중 — 조각5")
  - 인터뷰 채굴 (6.5, "준비 중 — 나중")
- **CSS**: 3판 그리드·상단 바·드로어. 기존 스타일 최대한 재사용, 새 클래스만 최소.
- **폐지**: `views/KnowledgeTab.jsx`·`views/WikitreeTab.jsx` — MoreDrawer와 WikiShell로 흡수. 참조 끊기고 제거.

**오늘 안 하는 것 (조각 3·4·5의 몫):**
- 인라인 편집(+ 버튼): 조각3.
- 룰 페이지 필드 마우스오버 편집: 조각3.
- AI 찾기 실체(질문 → 트리 근거 답): 조각4.
- 검토 추출 4단계 실체: 조각5.
- 검토 렌즈 실체: 조각5.
- md 저장 버튼 위치 재조정 — 상단 바 우측 어딘가에 유지만.
- 반응형(모바일). 데스크톱 데모용 3판 고정.

---

## 3. 파일 정리

**신설 (총 6개):**
- `views/WikiShell.jsx` — 앱 셸의 뼈대. 3판 그리드·선택 상태·검색 상태.
- `views/TocPanel.jsx` — 좌측 목차. 대분류·중분류·리프. 강도 dot·다수 hold ⚠.
- `views/RulePage.jsx` — 중앙 룰 페이지. **저작·이력 렌더가 시각적 핵심.**
- `views/AiFindPanel.jsx` — 우측 자리. "조각4에서 실체" 안내 + 코디 미니 로고.
- `views/MoreDrawer.jsx` — 우측 슬라이드 or 모달 드로어.
- `components/SearchBar.jsx` — 상단 중앙 검색바.

**수정:**
- `App.jsx` — 대개편(탭 상태·`KnowledgeTab`·`WikitreeTab` 참조 제거, `WikiShell`·`MoreDrawer` 렌더).
- `styles-core.css`(또는 `styles.css`) — 3판 그리드·상단 바·드로어·저작·이력 리스트.

**폐지 (파일 삭제):**
- `views/KnowledgeTab.jsx` — MoreDrawer 안에 입구 두 개(`ManualEntry`·`MiningEntry`)가 직접 배치되므로 이 컴포넌트는 불필요.
- `views/WikitreeTab.jsx` — 목차 로직은 `TocPanel`이, 사이드(대표·최근·사각지대·evidence 밴드)는 이 조각에서 사라진다(필요 시 조각4·5에서 룰 페이지·AI 찾기 안으로 재편입). 지금은 단순화 우선.

---

## 4. WikiShell 구조

```
<div class="wiki-shell">
  [TopBar]
    - 로고 · 브랜드명 · 서브
    - [SearchBar] (중앙 flex-grow)
    - [+] 더보기 버튼 (MoreDrawer open)
    - [⚙] Settings 버튼
    - codex_tree N개 · MODEL 표시(기존 UI 이관)
  [WikiBody — 3판 grid 25% 50% 25%]
    [TocPanel]
    [RulePage]
    [AiFindPanel]
</div>
[MoreDrawer] (open 시 우측에서 슬라이드)
[Settings] (모달, 기존 유지)
```

**상호작용:**
- `TocPanel`의 리프 클릭 → `WikiShell` state의 `selectedRuleId` 업데이트 → `RulePage`가 그 룰을 보여줌.
- 검색어 입력 → `TocPanel` 필터(WikiShell state의 `query` 공유).
- 초기 `selectedRuleId`: 트리에 룰 있으면 첫 번째 hard 룰 or 첫 룰, 없으면 null.
- 룰이 하나도 없으면 `RulePage`가 "…더보기에서 첫 판단을 심어보세요" 안내(빈 상태).

---

## 5. TocPanel 상세

- Props: `{ rules, query, selectedRuleId, onSelect }`
- 기존 `WikitreeTab`의 `groupByPath(rules)` 재사용 (`core/schema.js`).
- 대분류 접이식(기본 열림). 각 리프에 강도 dot(hold는 점선 원), 판단문.
- 다수 hold 가지엔 `⚠` 배지 (기존 로직).
- 검색어(`query`) 있으면 판단문 또는 path 문자열에 포함된 것만 표시. 매칭되는 리프의 부모 대분류는 자동 열림.
- 리프 클릭 → `onSelect(rule.id)`. 선택된 리프는 `.selected` 클래스로 강조.
- **인라인 편집 `+` 자리는 지금 만들지 않는다(조각3의 몫).** 다만 렌더 구조를 리프별 컨테이너로 짜서 조각3이 `+` 붙이기 쉽게.
- 목차 상단에 총 규칙 수·가지 수·강도 분포 바(기존 dist-bar 축약본) 유지.

---

## 6. RulePage 상세 (★ 이 조각의 시각적 핵심)

- Props: `{ rule }` (null이면 빈 상태 안내).
- 빈 상태: "왼쪽 목차에서 룰을 선택하세요" + 코디 안내 한 줄.
- 룰 있을 때 렌더 (위→아래 순서):

  1. **판단** (큰 글씨, 24px+, 잉크색)
  2. **강도 라벨** + **badge**(관철 N/M) — 기존 강도색 pill
  3. **path** (breadcrumb: 대분류 > 중분류 > 항목)
  4. **note** (근거·양보 이력 한 줄) — 있으면
  5. **★ 저작(authors)** — "이 룰의 주인들"
     - 카드 형식: `{name} · {role} · {at 사람이 읽기 좋게}`
     - 없으면 `⚠ 저작자 없음` (v0.1 룰에서만 발생)
  6. **★ 이력(history)** — "편집 이력, 최근 순"
     - 리스트: `{at} · {who} — {action}` (역순, 최근 위)
     - 없으면 표시 생략
  7. **provenance** — 있으면 (검토 추출·계약 마이닝으로 온 룰만): 계약명·검토자·발췌 일자·조항·방법.
  8. **재검토 트리거** — `review_trigger.keywords` 있으면 chip 나열, `test` 있으면 밑에 한 줄.
  9. **renew_trigger** — 있으면.
  10. **origin · status** — 하단 mono 라벨.
  11. **hold이면** "왜 갈렸는지" 안내 (팀 판단이 갈린 조항, 사각지대) — 기존 문구 재활용.

- **필드 편집은 조각3.** 지금은 read-only 렌더링만.
- 시각적으로 "위키피디아 문서 페이지" 느낌 — 여백 넉넉하게, 저작·이력이 확실히 눈에 띄게.

---

## 7. AiFindPanel (자리만)

- 상단: "AI 찾기" 라벨 + "조각 4에서 실체" 안내.
- 중앙: 미니 코디(WikitreeTab의 MiniMole 재사용).
- 하단: "여기서 트리를 근거로 질문에 답합니다 (조각4)". 
- 지금은 상호작용 없음. UI 자리만.

---

## 8. SearchBar (상단 중앙)

- Props: `{ query, onChange }`.
- placeholder: `검색... (엔터: AI 찾기 — 조각4)`.
- 키 입력 → `onChange(v)` → WikiShell state 갱신 → TocPanel 필터.
- 엔터 시 지금은 no-op(placeholder에 안내만). 조각4에서 실체.

---

## 9. MoreDrawer

- Props: `{ open, onClose, apiKey, onInjected, onOpenSettings }`.
- open 시 우측에서 슬라이드 패널 (width 480px 정도) or 모달 오버레이.
- 안에 세로 아코디언 or 상단 탭 5개:
  1. **수동 폼** → `<ManualEntry onInjected={onInjected} />` (기존 그대로).
  2. **계약 마이닝** → `<MiningEntry apiKey={apiKey} onInjected={onInjected} />`. API 키 없으면 배너.
  3. **검토 추출 4단계** — disabled 안내 카드, "준비 중 — 조각 5".
  4. **검토 렌즈** — disabled 안내 카드, "준비 중 — 조각 5".
  5. **인터뷰 채굴** — disabled 안내 카드, "준비 중 — 나중".
- 우상단에 닫기 버튼(×).
- 추가·마이닝 후 트리 갱신되면 WikiShell의 `rules`도 갱신됨(App의 `tree` state 갱신을 통해).

---

## 10. CSS 규약

기존 `styles-core.css`·`styles.css` 최대한 재사용. 새 클래스는 최소로:

- `.wiki-shell` — flex column, 100vh.
- `.top-bar` — 기존 `.topnav` 재활용 가능, 검색바 자리만 추가.
- `.search-bar` — flex-grow, 중앙 배치.
- `.wiki-body` — grid `25% 50% 25%`, gap 12px.
- `.toc-panel` · `.rule-page` · `.ai-find-panel` — 각 판에 배경(panel 색) + 내부 스크롤.
- `.rule-page` 내부:
  - `.rp-judgment` (큰 글씨)
  - `.rp-authors` · `.rp-history` — 카드/리스트 스타일 (기존 `.change-row` 참고)
- `.more-drawer` — position fixed, right 0, top 0, height 100vh, width 480px, transform으로 열림/닫힘.
- `.more-drawer-backdrop` — 반투명 오버레이.

색·폰트·테두리는 11장 규약 그대로.

---

## 11. 완료 기준

1. 앱을 열면 **위키 셸 하나**가 뜬다. 상단 탭 [지식 넣기]·[위키트리]·[검토하기] 없다.
2. 좌 25% 목차, 중 50% 룰 페이지, 우 25% AI 찾기 자리가 실제로 3판이다.
3. 목차의 리프 클릭 → 중앙 룰 페이지에 그 룰이 표시.
4. ★ **룰 페이지에 조각1이 stamp한 `authors`(주인)와 `history`(이력)가 눈에 보인다.** 이 조각의 시각적 목표.
5. 상단 검색바에 키워드 넣으면 목차가 즉시 필터된다.
6. "…더보기" 클릭 → 드로어가 열리고 5개 항목이 있다. **수동 폼·계약 마이닝은 실제로 동작**(기존 UX 그대로), 나머지 3개는 "준비 중" 안내.
7. `KnowledgeTab.jsx`·`WikitreeTab.jsx` 파일이 삭제됐거나 참조가 남지 않았다.
8. **코어(`core/*`)와 조각1 결과물(`authoring.js`·저작 스키마)은 하나도 안 바꿨다.** 구조 원칙 1 위반 없음.
9. `npm run build` 성공.

---

## 12. 끝나고 확인할 것 (나에게)

- **4번(저작·이력 표시)** 을 집요하게 본다. 조각1이 자동으로 박은 데이터가 여기서 처음 인간의 눈으로 보인다. 이게 예쁘게 나오지 않으면 조각1의 값이 안 살아난다. 카드·리스트 형식, 이름·시각이 명확한지.
- 3판 그리드가 실제로 3판인가. 좁은 창에서 깨지더라도 데스크톱(1280px+)에서 잘 나오면 OK.
- **조각3(인라인 편집)이 얹힐 자리** — TocPanel의 리프 렌더 구조가 리프별 개별 컨테이너인가(조각3이 `+` 버튼을 리프 옆에 붙일 수 있어야). RulePage의 필드도 개별 요소로 나뉘어 있어 조각3의 마우스오버 편집 hook이 쉬운가.
- MoreDrawer 안의 `ManualEntry`·`MiningEntry`가 원래처럼 동작하는가. 조각1의 저작·이력 stamp도 유지되는가(수동 폼으로 룰 추가 후 룰 페이지에서 저작·이력이 그대로 보이는지).
- 이 조각이 끝나면 v0.3의 골격이 실제로 선다: 위키가 앱이다. 남은 건 인라인 편집(조각3)과 AI 찾기(조각4).

---

## 13. 다음 조각 예고 (여기 만들지 않음)

**조각 3 — 인라인 편집.**
- TocPanel 리프·대분류 옆 `+` 버튼 → 인라인 입력창 → 판단 한 문장 → 엔터 → `stampAuthoring(rule, "인라인 편집으로 등재")` 통과 → 트리 갱신.
- RulePage 필드 마우스오버 → 인라인 편집 (판단문·note·kind). 저장 시 `stampAuthoring(rule, "판단문 수정")` 등 액션별로.
- 이름 없을 때 뜨는 모달 (Settings 열도록 유도).
- 이 조각이 서면 v0.3의 핵심 UX("아무나 지나가다 툭")가 실체가 된다.
