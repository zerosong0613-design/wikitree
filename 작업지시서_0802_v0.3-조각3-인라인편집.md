# 작업지시서 v0.3 — 조각 3 : 인라인 편집 (2026-08-02)

> 먼저 `CLAUDE.md v0.3`(특히 **3장-9, 6.1, 10.1, 10.5**)과 조각 1·2 지시서를 읽는다.
> 이 조각이 v0.3의 **핵심 UX**다 — "위키가 곧 앱, 편집은 아무나 툭"이 실체가 된다.
> 사용자 방향(v0.3 10.1 개정): 저장·편집은 **조용히**. 본문은 콘텐츠, 저작·이력은 `[역사]` 뒤로.

---

## 0. 시작 전 전제 확인

- [ ] 조각 1 완료: `core/authoring.js`·`stampAuthoring`·저작 스키마 살아있음. Settings에 "내 이름" 필드 있음.
- [ ] 조각 2 완료: 위키 셸(WikiShell), TocPanel, RulePage(판단→강도→note→트리거만 본문, `[역사]` 뒤로 저작·이력 숨김), MoreDrawer 라이트박스.
- [ ] 이름 저장 + 트리에 룰 2~3개 이상 (인라인 편집·필드 편집 데모용).
- [ ] `codex_tree`에 조각 1이 stamp한 `authors`·`history`가 실제 저장돼 있음.

전제가 되어 있으면, **코어(`core/schema.js`)와 저작 로직(`core/authoring.js`)은 건드리지 않는다.** `core/store.js`에는 `updateRule` 하나만 추가한다.

---

## 1. 목표 (한 문장)

목차 옆 `+` → 판단 한 문장 → 엔터로 **5초 안에** 룰이 트리에 박힌다. 룰 페이지의 판단문·note·kind는 **마우스 대면 인라인 편집** — 위키피디아식 사후 편집. 모든 저장은 **조용히**(본문 강조·플래시 없음), 저작·이력만 자동으로 `[역사]`에 쌓인다. 이름 없으면 Settings로 자동 유도.

---

## 2. 오늘 범위 (이것만)

- **`core/store.js`에 `updateRule(id, patchedRule)` 신설** — 같은 id 룰을 통째로 교체(경로 병합 아님). `authors`·`history`는 caller가 `stampAuthoring`으로 이미 넣어둔 상태여야.
- **TocPanel에 `+` 3자리:**
  1. **목차 헤더의 `+ 새 판단`** — path 전부 사용자 입력.
  2. **대분류(major) 옆 `+`** — path[0] 자동 채움, 판단문만.
  3. **리프 옆 `+ 이 옆에`** — 같은 부모 path(path[0], path[1]) 자동, 판단문만. (같은 path 재입력이면 병합, 다른 세 번째 단계면 형제로 추가)
- **`views/inline/InlineNewRule.jsx` 신설** — 판단문 하나 + kind 칩(soft 기본) + 엔터로 저장 · Esc로 취소. `stampAuthoring(rule, "인라인 편집으로 등재")` 통과.
- **`views/inline/EditableField.jsx` 신설** — RulePage의 판단문·note를 감싸는 hover→click→edit 컴포넌트. `stampAuthoring` 통과.
- **`views/inline/KindPicker.jsx` 신설** — 4개 kind 칩. 인라인 새 룰 · 룰 페이지 강도 변경 양쪽에서 재사용.
- **RulePage 필드 편집:** 판단문(EditableField) · note(EditableField) · 강도 pill 클릭 → KindPicker 팝오버 → 즉시 저장.
- **이름 없을 때: Settings 자동 열기** — 인라인 편집 · 필드 편집 어디서든 `NoAuthorError` 시 App의 `onRequireAuthor()` 호출 → Settings 모달 open. 인라인 입력창은 그대로 유지(이름 저장 후 재시도 가능).
- **저장 UX는 조용히** (10.5): 본문에 flash·강조·"방금 편집됨" 표시 **없음**. 편집 완료 = 편집 모드 나가고 새 값이 그 자리에 렌더. 저작·이력은 `[역사]`에서만 확인.

**오늘 안 하는 것:**
- kind 변경 확인 다이얼로그 (즉시 적용).
- path 이동(대분류·중분류 재분류) — 나중. 지금은 판단문·note·kind만.
- 삭제 버튼 — 나중.
- AI 찾기 실체(조각 4).
- 검토 추출 4단계 실체(조각 5).

---

## 3. 파일 정리

**신설 (3개):**
- `views/inline/InlineNewRule.jsx` — `+` 클릭 시 뜨는 새 룰 입력창.
- `views/inline/EditableField.jsx` — 판단문·note 편집. `<div hover→click>` → `<textarea/input>` → 엔터/blur 저장, Esc 취소.
- `views/inline/KindPicker.jsx` — 4칩 kind 선택 (인라인·필드 양쪽 재사용).

**수정:**
- `core/store.js` — `updateRule(id, patchedRule)` 추가.
- `views/TocPanel.jsx` — `+` 3자리 + InlineNewRule 렌더 (열림 상태·자동 path 인자 관리).
- `views/RulePage.jsx` — 판단문·note를 EditableField로, 강도 pill을 KindPicker 트리거로.
- `views/WikiShell.jsx` — `onTreeChange`(=setTree) · `onRequireAuthor`(설정 열기) 프롭 전달.
- `App.jsx` — `onRequireAuthor={() => setShowSettings(true)}` 를 WikiShell에 전달. (Settings는 이미 이름 필드 있음.)

**안 건드림:** `core/schema.js`·`core/authoring.js`·`core/mdExport.js`·`entries/*`·`MoreDrawer`·`AiFindPanel`·`SearchBar`·`Settings`.

---

## 4. 인라인 편집 UX 규약 (v0.3 10.5 실현)

- **저장 완료 시 본문에 강조·플래시·"방금 편집됨" 표시 없음.** 편집 모드에서 새 값을 그린 채로 나가면 그게 성공 신호다. 저작·이력은 `[역사]` 토글로만 노출(2장·10.1).
- **엔터 = 저장, Esc = 취소.** blur는 저장(일관성).
- **인라인 입력창은 판단문 하나뿐** (6.1). kind 칩은 인풋 아래 나란히, 기본 `soft`. path 세부는 컨텍스트에서 자동.
- **이름 없으면** `onRequireAuthor()` → Settings 자동 open. 인라인 입력창은 열린 채로 유지 → 이름 저장 후 사용자가 다시 엔터.
- **editable 필드는 hover 시 은은한 시각 신호** (예: 미묘한 밑줄 or 배경 hint), 클릭 시 편집 모드. 지나치게 눈에 띄지 않게.

---

## 5. `store.js` — `updateRule` (신설)

```js
// 기존 룰(같은 id)의 필드를 통째로 교체한다. 병합 아님.
// 호출자는 반드시 stampAuthoring으로 authors·history를 미리 stamp한 상태로 넘긴다.
export function updateRule(id, patchedRule) {
  const tree = loadTree();
  const idx = tree.findIndex((r) => r.id === id);
  if (idx === -1) return tree; // 룰이 사라졌으면 무시(경합 방지)
  const out = tree.map((r) => ({ ...r }));
  // path·id는 유지, 나머지는 patchedRule 값을 반영
  out[idx] = normalizeRule({ ...patchedRule, id, path: tree[idx].path, created_at: tree[idx].created_at }, idx);
  saveTree(out);
  return out;
}
```

- **`path`·`id`·`created_at`은 보존.** 판단문·note·kind 편집이므로 이들은 변하지 않는다.
- (path 이동은 오늘 범위 아님 — 미래 조각에서 별도 함수로.)
- `normalizeRule`을 다시 태워 방어적 정규화.

---

## 6. TocPanel — `+` 3자리

### 6.1 헤더 `+ 새 판단`
- `toc-panel-head` 옆에 작은 `+` 버튼.
- 클릭 → 목차 상단에 InlineNewRule 열림. 대분류·중분류·항목·판단문 모두 사용자가 입력.
- 저장 시 `path`는 사용자 입력 그대로(비면 `["미분류"]`).

### 6.2 대분류 옆 `+`
- 각 major(대분류) 헤더 우측에 `+`.
- 클릭 → 그 major 안에 InlineNewRule 열림. `path`는 `[major.name]` 자동, 판단문만.

### 6.3 리프 옆 `+`
- 각 리프 hover 시 우측에 `+` 등장.
- 클릭 → 그 리프 아래에 InlineNewRule 열림. `path`는 리프의 `path` **부모 경로**(`[path[0], path[1]]`) 자동.
- 같은 부모 밑에 형제 판단을 빠르게 추가.

### 6.4 상호작용
- 한 번에 하나의 InlineNewRule만 열림. 다른 `+` 클릭 시 이전 것 닫힘.
- InlineNewRule 자체 상태(judgment/kind)는 컴포넌트 내부 소유. 저장·취소 시 부모(TocPanel)가 닫음.

---

## 7. RulePage — 필드 편집

- **판단문**: `<h1 class="rp-judgment">` → `<EditableField multiline>` 으로 감쌈. 클릭 시 textarea로 전환, 엔터/blur 저장, Esc 취소. 저장 액션 = `"판단문 수정"`.
- **note**: `<p class="rp-note">` → `<EditableField>` (단일 줄). 저장 액션 = `"note 수정"`. note가 비어 있을 때는 `[+ 근거·양보 이력 한 줄]` 자리 표시자가 나타나 클릭 → 편집 모드.
- **강도 pill**(`.rp-strength`): 클릭 시 옆에 KindPicker 팝오버 → 4칩 중 하나 선택 → 즉시 저장. 액션 = `"kind 변경"`.
- 편집 성공 후: 상위 `onTreeChange(newTree)` 콜백으로 트리 갱신 → RulePage는 새 값이 자연스럽게 그려짐. flash 없음.
- **`[역사]` 토글이 열려 있으면** — 새 편집 후 리렌더되면서 자연스럽게 새 history entry가 위에 나타남. 별도 강조는 안 함(사용자가 스스로 확인).

---

## 8. `InlineNewRule.jsx` 구조

Props:
- `initialPath` (배열, 자동 채움)
- `onSave(rule)` — 부모가 `stampAuthoring` + `addRules`
- `onCancel()`
- `onRequireAuthor()` — 부모가 Settings 열기 트리거

Body:
```
[대분류][중분류][항목] (initialPath에 따라 채워짐; 초기값이 있으면 disabled or 비활성 스타일)
[판단 한 문장 입력창] (autoFocus, 엔터 = 저장, Esc = 취소)
[hard][soft (기본)][neu][hold] (KindPicker)
[저장] [취소]
```

- `initialPath.length === 0` (헤더 + 새 판단): 3필드 모두 편집 가능.
- `initialPath.length === 1` (대분류 옆): 대분류 채워짐·disabled. 중분류·항목 편집 가능.
- `initialPath.length === 2` (리프 옆): 대·중분류 채워짐·disabled. 항목 편집 가능.

저장 시 부모(TocPanel)가:
1. `path`·판단문·kind로 룰 만들고 `origin="인라인 편집"`.
2. `stampAuthoring(rule, "인라인 편집으로 등재")`.
3. `NoAuthorError` catch → `onRequireAuthor()`, InlineNewRule은 열린 채 유지.
4. 성공 → `addRules([stamped])` → `onTreeChange(newTree)` → InlineNewRule 닫음.

---

## 9. `EditableField.jsx` 구조

Props:
- `value` (현재 값)
- `multiline` (bool, textarea vs input)
- `placeholder` (비어 있을 때 자리 표시자)
- `className` (원본 스타일 유지)
- `onSave(newValue)` — 부모가 `stampAuthoring(patchedRule, action) → updateRule`
- `onRequireAuthor()`

동작:
- 기본: `<div>` 렌더 (원본과 시각적 동일 · hover 시 은은한 signal · 클릭 이벤트).
- 클릭 → 내부 state `editing = true` → `<textarea/input>` 렌더, 현재 값으로 초기화, autoFocus.
- 엔터(single-line) 또는 Ctrl+Enter(multiline) → 저장. blur도 저장.
- Esc → 취소(원 값 복원, editing = false).
- 값이 원본과 동일 → `onSave` 호출 안 함 (불필요 stamp 방지).
- 저장 성공/실패 판단은 부모의 `onSave` 반환값 or throw로. `NoAuthorError` 시 `onRequireAuthor()`, editing 상태 유지.

---

## 10. `KindPicker.jsx` 구조

Props:
- `value` (현재 kind)
- `onChange(newKind)` — 즉시 콜백

Body:
- 4개 칩: 마지노선 · 협상 여지 · 신규·미확정 · 사람 확인.
- 현재 값은 강조. 클릭 → `onChange(newKind)` 즉시.
- 기존 `styles-core.css`의 `.kind-btn` 재사용.

---

## 11. `App.jsx` · `WikiShell.jsx` 배선

**App.jsx:**
```jsx
<WikiShell
  rules={tree}
  query={query}
  selectedRuleId={selectedRuleId}
  onSelectRule={setSelectedRuleId}
  onTreeChange={setTree}
  onRequireAuthor={() => setShowSettings(true)}
/>
```

**WikiShell.jsx:** 두 프롭을 TocPanel·RulePage에 그대로 전달.

---

## 12. CSS 추가 (최소)

- `.toc-add-btn` — `+` 버튼 (majors·leaves 옆). 작고 조용한 스타일 (hover 시 강조).
- `.toc-leaf` hover 시 `.toc-add-btn` 등장(기본 hidden).
- `.inline-new-rule` — 컨테이너 (기존 `.entry-manual` 참고, 더 컴팩트).
- `.editable-field` — 기본 인라인, hover 시 은은한 밑줄 or 배경 hint.
- `.editable-field.editing` — 편집 모드 (input/textarea 스타일).
- `.kind-picker` — 팝오버(RulePage 강도 pill 옆).

`styles-core.css` 하단에 v0.3 인라인 편집 섹션으로 추가.

---

## 13. 완료 기준

1. **목차 헤더 `+ 새 판단`** 클릭 → InlineNewRule 열림 → path 3단·판단문 입력 → 엔터 → 트리에 박힘, 목차·룰 페이지 갱신.
2. **대분류 옆 `+`** 클릭 → 그 major의 path[0] 자동 채워진 InlineNewRule → 판단문+kind → 엔터 → 트리 갱신.
3. **리프 옆 `+`** hover 시 등장, 클릭 → 부모 path 자동 → 형제 판단 추가.
4. **룰 페이지 판단문 클릭** → 편집 모드 → 새 값 저장 → 페이지에 새 값 렌더. `[역사]` 토글 열면 새 `"판단문 수정"` 이력 위에 뜸.
5. **note 클릭** → 편집. 저장 시 `"note 수정"` 이력.
6. **강도 pill 클릭** → KindPicker 팝오버 → 다른 kind 선택 → 즉시 강도색 변경, `"kind 변경"` 이력.
7. **이름 없이 편집 시도** → Settings 자동 open, 이름 필드 자연 포커스. 이름 저장 · 모달 닫기 → 사용자가 다시 엔터 → 저장 성공.
8. **저장 후 본문에 flash·강조 없음** (조용히). 시각적 성공 신호 = 새 값이 그 자리에 뜬 것 뿐.
9. **코어(`schema.js`·`authoring.js`)는 안 바뀌었다.** store.js는 `updateRule` 한 함수만 추가.
10. `npm run build` 성공.

---

## 14. 끝나고 확인할 것 (나에게)

- **1·4번** 을 집요하게 본다. 이 두 개가 v0.3의 UX 요지 ("아무나 지나가다 툭"): 5초 안에 새 판단 등재, 룰 페이지에서 마우스 대고 편집. 이게 어색하면 실패.
- **저장의 조용함 (10.5)** — 어디에도 "저장됨!" 이런 뱃지 없어야. 사용자가 편집 안 하고 지나가는 사람이면 저장 뒤에 원래 화면을 그대로 보고 있어야.
- **`[역사]` 뒤로 이력이 실제로 쌓이는가.** 사용자가 판단문 → note → kind 세 번 편집하면 history에 3줄이 순서대로 나와야.
- **이름 유도** — Settings 자동 open이 방해가 되지 않는지. 인라인 입력창이 열린 채로 뒤에 유지되고 있어 이름 저장 후 자연스럽게 이어질 수 있어야.
- 조각 3이 서면 v0.3 12장 "지금 범위"의 핵심(위키 셸 + **인라인 편집** + AI 찾기)에서 두 번째가 완성. 조각 4(AI 찾기)만 얹으면 12장의 "지금"이 다 선다.

---

## 15. 다음 조각 예고 (여기 만들지 않음)

**조각 4 — AI 찾기 상주.** 우측 패널에 질문 입력 → Anthropic 호출 → 트리 근거로 답 + 근거 룰 링크 + 저작자. 확실치 않으면 '보류'. 클릭 시 중앙이 그 룰 페이지로 전환.
