# 작업지시서 v0.3 — 조각 4 : AI 찾기 상주 (2026-08-02)

> 먼저 `CLAUDE.md v0.3`(특히 **7.2, 7장 미탐 우선, 10.1 우측 · 10.2, 8장**)과 조각 1·2·3 지시서를 읽는다.
> 이 조각으로 v0.3 12장 "지금 범위" 3종(위키 셸 + 인라인 편집 + **AI 찾기**)의 마지막이 완성된다.
> 진짜 위키의 정문은 검색 — 그 검색을 AI가 받아 트리로 답한다.

---

## 0. 시작 전 전제 확인

- [ ] 조각 1·2·3 완료: 스키마·저작·인라인 편집이 살아있음. 룰 페이지 `[역사]`에 저작자가 실제로 박혀 있음.
- [ ] Settings에 API 키(Anthropic)와 내 이름이 저장됨.
- [ ] 트리에 서로 다른 대분류·판단 3~5개 이상 (AI가 실제 트리로 답하는 데모용).
- [ ] `codex-mining-tool/src/api.js`의 `mineRules` · `MODEL` · `parseModelJson` · `friendlyError` 함수 위치를 안다 (재사용).

전제가 되어 있으면, **코어·인라인 편집·룰 페이지는 건드리지 않는다.** 오늘은 우측 패널을 실체화하고 검색바 엔터를 승격시킨다.

---

## 1. 목표 (한 문장)

우측 AI 찾기 패널에 질문을 넣으면(또는 상단 검색바 엔터) Anthropic이 트리를 근거로 답한다 — **근거 룰 링크** 클릭 시 중앙 룰 페이지로 전환, **저작자**가 답 옆에 붙는다. 트리에 근거 없으면 정직하게 **"보류"**. AI는 인용하지 창작하지 않는다(7.2·미탐 우선).

---

## 2. 오늘 범위 (이것만)

- **`AiFindPanel.jsx` 실체화** — 자리에서 실제 대화창으로.
  - 질문 입력창 + 전송 버튼(또는 엔터).
  - API 키 없으면 배너로 Settings 열기 유도.
  - 트리 비어 있으면 "심어진 판단이 없어 답할 근거가 없습니다" 안내.
  - 답 렌더: 요지 문단 → 근거 룰 카드 리스트(각 카드 = 판단문 미리보기 + kind pill + 저작자 이름 + 클릭 시 룰 페이지 전환).
  - **verdict = "hold"** 인 경우: "확정 기준 없음(보류)" 회색 안내.
- **상단 검색바 엔터 → AI 찾기 승격 (10.2)** — 검색바에서 엔터 치면 그 텍스트가 AI 찾기 패널의 질문이 되어 즉시 실행.
- **`prompt.js`에 `buildAiFindPrompt(question, rules)` 신설** — 트리 룰의 최소 표현(id·path·judgment·kind·note·authors 이름만)을 컨텍스트로 넘김. 미탐 우선·인용 강제.
- **`api.js`에 `askAi({apiKey, prompt})` 신설** — `mineRules`와 동일 패턴(Anthropic API + JSON 파싱). `mineRules`는 그대로 유지.
- **`App.jsx` 배선** — `aiQuery`·`aiTrigger` state 신설, SearchBar/WikiShell에 프롭 전달.
- **`WikiShell.jsx`·`SearchBar.jsx` 배선** — 프롭 통과, 엔터 이벤트 소비.

**오늘 안 하는 것:**
- 답 문자열 스트리밍 — 지금은 한 번에 받아 표시.
- 대화 이력 저장/재조회 — 세션 동안 화면에만.
- 팔로업 질문(같은 세션 안 다중 턴) — 이번 답만.
- 자연어 근거 하이라이트를 룰 페이지에서 강조 — 클릭 이동만.
- 검토 렌즈(7.1) — 조각 5.

---

## 3. 파일 정리

**신설:**
- (필요 시) `views/AiFindPanel.jsx`는 이미 있으니 **완전 재작성**.

**수정:**
- `api.js` — `askAi({apiKey, prompt})` 함수 추가 (`mineRules`는 유지, 사실상 같은 호출 로직을 공용화하는 게 이상적이나 오늘은 병립).
- `prompt.js` — `buildAiFindPrompt(question, rules)` 함수 추가.
- `components/SearchBar.jsx` — `onSubmit(query)` 프롭 추가, 엔터 시 호출.
- `App.jsx` — `aiQuery`·`aiTrigger` state, SearchBar에 `onSubmit` 전달, WikiShell에 `apiKey`·`aiQuery`·`aiTrigger`·`onOpenSettings` 전달.
- `views/WikiShell.jsx` — 위 프롭들을 AiFindPanel에 통과.
- `styles-core.css` — AI 찾기 대화창·근거 카드·보류 배너 스타일.

**안 건드림:** `core/*`, `entries/*`, `TocPanel`, `RulePage`, `MoreDrawer`, `Settings`, `views/inline/*`.

---

## 4. 프롬프트 — `buildAiFindPrompt`

```
당신은 이 팀이 위키에 쌓아 온 판단 기준을 근거로 답하는 도우미다.
아래 [트리 룰]에 있는 판단만을 근거로 사용자 질문에 답한다.
없는 것을 지어내지 말라. 확실치 않으면 무관이 아니라 '보류'다 — 놓치는 게 틀리는 것보다 안전하다.

[출력 규칙]
- JSON만 출력. 마크다운·주석 없음.
- answer: 사용자 질문에 대한 요지 답변(1~3문장, 존댓말). 근거가 있으면 그 근거를 요약해 말한다.
- basis: 실제로 근거가 된 룰의 배열. rule_id·why(왜 이게 근거인가 한 줄).
  - basis가 비어 있으면 verdict는 반드시 "hold".
- verdict:
  - "answered": 트리 안에 명확한 근거가 있어 답한다.
  - "hold": 트리에 확정된 기준이 없거나 관련 룰들이 서로 갈린다. answer는 "확정 기준 없음"으로 시작한다.

[출력 형식]
{
  "answer": "...",
  "basis": [{ "rule_id": "RULE-XXXX", "why": "..." }],
  "verdict": "answered" | "hold"
}

[질문]
{{question}}

[트리 룰 — 근거로 쓸 수 있는 유일한 소스]
{{JSON: [{ id, path, judgment, kind, note, authors: [names], provenance?: source_contract }]}}
```

**주의:**
- 사용자 질문이 트리와 무관해도 트리에서 억지로 근거를 짜내지 말 것. 그럴 땐 `verdict=hold`.
- basis의 rule_id는 반드시 [트리 룰]에 존재하는 id여야 한다. 없는 id면 hold로.

---

## 5. `api.js` — `askAi` (신설, mineRules와 병립)

```js
// AI 찾기 호출. mineRules와 동일한 Anthropic 호출 + JSON 파싱.
// 차이: 이름·의도만. 공용화(callClaude)는 리팩터로 나중에.
export async function askAi({ apiKey, prompt }) {
  const res = await fetch("https://api.anthropic.com/v1/messages", {
    method: "POST",
    headers: {
      "x-api-key": apiKey,
      "anthropic-version": "2023-06-01",
      "content-type": "application/json",
      "anthropic-dangerous-direct-browser-access": "true",
    },
    body: JSON.stringify({
      model: MODEL,
      max_tokens: MAX_TOKENS,
      messages: [{ role: "user", content: prompt }],
    }),
  });
  if (!res.ok) {
    let detail = "";
    try { const j = await res.json(); detail = j?.error?.message || JSON.stringify(j); }
    catch { detail = await res.text().catch(() => ""); }
    throw new ApiError(res.status, detail);
  }
  const data = await res.json();
  const text = (data.content || []).filter(b => b?.type === "text").map(b => b.text).join("");
  try { return parseModelJson(text); }
  catch (e) {
    if (data.stop_reason === "max_tokens") throw new ParseError(text, true);
    throw e;
  }
}
```

- `MODEL`·`MAX_TOKENS`·`ApiError`·`ParseError`·`parseModelJson`·`friendlyError` 모두 재사용.
- 반환 파싱된 객체는 `{answer, basis, verdict}` 형태여야 한다(프롬프트에서 강제).

---

## 6. AiFindPanel 상세

### Props
- `apiKey` (없으면 배너)
- `rules` (트리 배열, 없으면 근거 부족 안내)
- `aiQuery` (검색바에서 승격된 질문, 옵션)
- `aiTrigger` (동일 질문을 재실행하기 위한 카운터/타임스탬프)
- `onSelectRule(id)` (근거 룰 클릭 시 중앙 전환)
- `onOpenSettings()` (API 키 없을 때 유도)

### 내부 state
- `question` (입력창 값 — aiQuery가 갱신되면 여기로 동기화)
- `status`: `idle | asking | answered | hold | error`
- `answer` (string)
- `basis` (array of `{rule_id, why}`)
- `error` ({title, msg} 또는 null)

### 흐름
1. 질문 입력 or 검색바에서 승격되어 옴 → 전송 버튼 or 엔터.
2. API 키 없으면 배너 표시하고 실행 안 함.
3. 트리 비어있으면 "근거 없음" 안내하고 실행 안 함.
4. `status="asking"` → `buildAiFindPrompt(question, rules)` → `askAi(...)` → 파싱.
5. `basis`의 rule_id가 실제 트리에 있는 것만 필터 (안전장치).
6. verdict에 따라 status 세팅.
7. 답 렌더.

### aiQuery 동기화 (useEffect)
- `aiQuery`가 truthy & `aiTrigger` 변화 → `setQuestion(aiQuery)` → 자동 전송.

### 렌더 (25% 폭 감안, 컴팩트)
- **헤더:** "AI 찾기" + 상태 표시(idle/asking/answered/hold).
- **질문 인풋** (multiline textarea 3줄) + [묻기] 버튼. 엔터로도 실행.
- **API 키 없음 배너** (있을 때만): "AI 찾기는 Anthropic API 키가 필요해요. ⚙ 설정"
- **트리 비어 있음 배너**: "심어진 판단이 없어 답할 근거가 없어요. 목차의 `+ 새 판단`으로 시작해보세요."
- **답 영역:**
  - `verdict=answered`: answer 문단 → "근거 N개" 라벨 → basis 카드 리스트.
  - `verdict=hold`: 회색 hold 배너 ("확정 기준 없음 — 보류") + answer 이유 + basis 있으면 관련 룰들.
  - 근거 카드 = 클릭 시 `onSelectRule(rule_id)`. 카드 안: 판단문(2줄까지 clamp) · kind pill · 저작자 이름 나열 · why 한 줄.

### 근거 룰 매핑
- `basis[].rule_id`를 트리 `rules`에서 룩업 → 없으면 skip.
- 저작자 표시: `rule.authors.map(a => a.name).join(", ")` 또는 최근 편집자 하나.

---

## 7. SearchBar 엔터 → AI 찾기 승격

- SearchBar에 `onSubmit(query)` 프롭 추가.
- 인풋에서 엔터 → `onSubmit(query)` 호출.
- App: `setAiQuery(query); setAiTrigger(t => t + 1);`.
- 이 검색바는 기존처럼 목차 필터도 계속 유지 (즉시 필터 + 엔터 시 AI 승격, 이중 용도).

---

## 8. 완료 기준

1. **우측 패널에 질문 입력창·전송 버튼**이 뜨고, 클릭 or 엔터 시 로딩(`asking`) → 결과.
2. **답에 answer 문단 + 근거 룰 카드**가 뜬다. 카드 클릭 시 중앙 룰 페이지가 그 룰로 전환.
3. **각 근거 카드에 저작자 이름**이 실제로 나타난다 (조각 1의 값이 여기서 신뢰의 출처로 쓰인다).
4. **트리에 없는 주제를 물으면 `verdict=hold`** → "확정 기준 없음(보류)" 배너.
5. **상단 검색바에서 엔터** → 우측 패널에 그 질문이 채워지고 즉시 실행.
6. **API 키 없이 질문** → 배너 + Settings 열기 링크.
7. **트리 비어 있는데 질문** → "근거 없음" 안내, API 호출 안 함.
8. **AI가 없는 rule_id를 반환**해도 안전 (필터로 걸러 skip).
9. 코어(`core/*`)·인라인 편집(`views/inline/*`)·룰 페이지·목차 코드 안 바뀜.
10. `npm run build` 성공.

---

## 9. 끝나고 확인할 것 (나에게)

- **2·3번**을 집요하게 본다. 답만 나오고 근거 룰 링크가 없으면 v0.3 8장 원칙("신뢰의 출처는 사람") 실패. 근거 클릭 → 룰 페이지 전환이 매끄러워야.
- **4번(hold)** — 트리에 명확히 없는 주제를 물었을 때 억지로 근거 짜내지 않고 정직하게 보류가 뜨는지. 프롬프트가 미탐 우선을 잘 지키는지.
- **5번(검색바 승격)** — 사용자가 목차 필터 목적으로 엔터를 눌렀는데 AI 찾기가 튀어나오면 놀랄 수 있음. placeholder에 "엔터: AI 찾기"로 이미 안내 중이므로 큰 문제는 아니나, 첫 세션 사용자 반응을 눈여겨.
- **아이덴티티** — 답 UI가 위키 셸의 톤(잉크·소프트 amber·mono 라벨)에 맞는지. 대화형 채팅 UI를 흉내내지 말고 "답+근거" 정돈된 카드로.
- 이 조각이 서면 v0.3 12장 "지금 범위" 3종이 다 선다. 이후 조각 5(검토 추출·검토 렌즈)는 파워유저용 옵션 — 데모 필수 아님.

---

## 10. 다음 조각 예고 (여기 만들지 않음)

**조각 5 (선택) — 무거운 유틸 실체화.** MoreDrawer에서 "준비 중"인 세 자리(검토 추출 4단계·검토 렌즈·인터뷰 채굴) 중 필요한 것부터. v0.3 12장에서 "지금 되면 좋지만 데모 필수 아님"으로 분류됨.
