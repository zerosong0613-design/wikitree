# 작업지시서 v0.3 — 조각 5 : AI path 자동 제안 (2026-08-02)

> 먼저 `CLAUDE.md v0.3`(특히 **3장 원칙 10, 4.5장, 12장·13장**)과 조각 1~4 지시서를 읽는다.
> 이 조각이 v0.3의 마지막 "지금 범위" 항목이다 — path 규약(4.5장)의 실체.
> **AI 제안이 없으면 taxonomy는 첫 결정에 굳어져 위키가 못 자란다.** 이 조각의 값.

---

## 0. 시작 전 전제 확인

- [ ] 조각 1~4 완료. 위키 셸·인라인 편집·AI 찾기 모두 살아있음.
- [ ] Settings에 Anthropic API 키 저장됨.
- [ ] 트리에 최소 5~10개 룰. (제안이 "기존 대분류에 맞음"과 "새 대분류 필요"를 실제로 구분하려면 기존 축이 몇 개 있어야 함.)
- [ ] `core/authoring.js`·`InlineNewRule.jsx`·`api.js`·`prompt.js`의 위치를 안다 (재사용·확장).

전제가 되어 있으면, **코어는 건드리지 않는다.** 오늘은 `InlineNewRule`에 AI 제안 단계를 붙이고, 프롬프트·API 함수를 추가한다.

---

## 1. 목표 (한 문장)

인라인 새 룰 입력창에서 판단문 한 문장을 쓰면 **[path 제안받기] 버튼**으로(또는 자동 debounce 후) AI가 **기존 트리를 근거로 path 후보 3개**를 제안한다 — 사람은 클릭 한 번으로 accept, 마음에 안 들면 직접 입력(override). override는 그대로 저장돼 다음 판단부터 그 방향으로 taxonomy가 진화한다.

---

## 2. 오늘 범위 (이것만)

- **`api.js`에 `suggestPath({apiKey, prompt})` 추가** — 기존 `askAi`와 동일 패턴(공용화는 리팩터로 나중).
- **`prompt.js`에 `buildPathSuggestPrompt(judgment, rules, initialPath)` 추가.** 기존 트리의 최소 표현(path·kind만) + 판단문 + (있으면) 초기 부분 경로.
- **`InlineNewRule.jsx` 확장:**
  - 판단문 아래에 **[✨ AI에 path 제안받기]** 버튼 (판단문 있고 API 키 있을 때만 활성).
  - 로딩 → 제안 3개 카드로 렌더(대·중·항목 미리보기 + 왜 이 자리인가 한 줄).
  - 카드 클릭 → path 3필드 자동 채움(fixed된 것 외에는 override 가능).
  - "새 대분류 제안"이면 카드에 배지 (기존 축과 다르다는 신호).
- **자동 트리거 (선택):** 판단문 입력 후 800ms debounce로 자동 실행 옵션. **기본은 수동 버튼**(사용자 흐름 방해 최소화).
- **에러/키 없음/트리 빈 경우:** 조용히 버튼 비활성 + 안내 tooltip.

**오늘 안 하는 것:**
- 수동 폼(6.2)·검토 추출(6.3) 편집 방식에도 확장 — 나중. 지금은 인라인만 (제일 자주 쓰는 편집이라 여기부터).
- RulePage에서 path 이동 UI — 4.5장 원칙 5, 미래.
- override 통계 저장 (몇 번 override됐는지 학습). 현재 트리 상태만 참조 → 자연스레 반영됨.

---

## 3. 파일 정리

**수정:**
- `api.js` — `suggestPath({apiKey, prompt})` 추가 (askAi 패턴 복제).
- `prompt.js` — `buildPathSuggestPrompt(judgment, rules, initialPath)` 추가.
- `views/inline/InlineNewRule.jsx` — 제안 버튼 + 결과 카드 리스트 렌더.
- `styles-core.css` — 제안 카드 스타일.

**안 건드림:** `core/*`, RulePage, TocPanel, MoreDrawer, AiFindPanel, EditableField, KindPicker, Settings.

---

## 4. 프롬프트 — `buildPathSuggestPrompt`

```
당신은 이 팀의 판단 위키 taxonomy 도우미다.
사용자가 새로 심으려는 판단문을 보고, 기존 트리에 어울리는 path 3개를 제안한다.

[출력 규칙]
- JSON만 출력. 마크다운·주석 없음.
- 3개의 제안. 각 제안은 path(대분류·중분류·항목 배열) + reason(왜 이 자리인가 한 줄) + is_new_major(bool, 기존에 없는 새 대분류이면 true).
- 기존 대분류에 잘 맞으면 그 안으로. 안 맞으면 새 대분류를 제안하되 왜 새로 필요한지 이유 필수.
- 판단의 "성격"으로 묶어라. 문서 종류·계약 종류로 나누지 마라 (그건 provenance에 남는다).
- initialPath가 주어졌으면 그 시작 경로를 존중하되(예: 대분류 고정) 그 안에서 다양한 위치를 제안.

[출력 형식]
{
  "suggestions": [
    { "path": ["대분류", "중분류", "항목"], "reason": "...", "is_new_major": false },
    { "path": ["대분류", "중분류", "항목"], "reason": "...", "is_new_major": false },
    { "path": ["대분류", "중분류", "항목"], "reason": "...", "is_new_major": true }
  ]
}

[새 판단]
${judgment}

[초기 경로 (있으면 존중)]
${initialPath.join(" > ") || "없음"}

[기존 트리의 축들 (성격으로 묶기 참고용)]
${existingTaxonomy}
```

**existingTaxonomy 구성:**
- 기존 트리의 대분류·중분류를 count와 함께 요약.
- 예: `책임·분쟁 (12) > 배상 (5), 면책 (3), 준거법 (4) | 정보·기밀 (8) > 비밀유지 (5), 개인정보 (3) | ...`
- 개별 룰의 판단문은 넣지 않음 (프롬프트 토큰 절약, 근거는 축만).

---

## 5. `api.js` — `suggestPath` (신설)

`askAi` 완전 동일 패턴. 이름·의도만 다름.

```js
export async function suggestPath({ apiKey, prompt }) {
  // askAi와 동일 body. 반환 파싱된 { suggestions: [...] }.
}
```

공용 `callClaude(apiKey, prompt)` 함수로 리팩터하고 싶어지지만 오늘은 병립(3개째 반복 인정하고 나중 정리).

---

## 6. `InlineNewRule.jsx` — 제안 흐름

기존 구조 위에 얹는다. 판단문 아래 · KindPicker 위에 제안 영역 추가:

```
[대분류][중분류][항목]  (initialPath에 따라 fixed 여부)
[판단 한 문장]
[✨ AI에 path 제안받기]  ← 판단문 & API 키 있을 때 활성
  ↓ 클릭
[제안 3개 카드 리스트 or 로딩]
[hard][soft][neu][hold]  KindPicker
[취소] [심기]
```

### 제안 카드 구조
```
| ★ 새 대분류 (배지, is_new_major=true인 경우만)
| 책임·분쟁 › 배상 › 상한
| → 배상 상한을 정한다는 성격이 '책임·분쟁' 축의 '배상' 아래에 자연스러움
```
- 카드 클릭 → path 3필드에 채움(대분류 fixed면 무시하고 나머지만 갱신).
- 사용자가 카드 무시하고 직접 타이핑도 가능(override) — path 필드는 계속 편집 가능.

### 상태
- `suggesting`: bool (API 호출 중)
- `suggestions`: array (결과) 또는 null

### 흐름
1. 판단문 입력 → 버튼 활성.
2. 버튼 클릭 → `suggesting=true` → `buildPathSuggestPrompt` → `suggestPath` → `suggestions=[...]` → `suggesting=false`.
3. 카드 3개 렌더. 클릭 시 path 자동 채움 (`setMajor` 등).
4. path 채워지면 카드 리스트 사라지고 심기 흐름 자연스레 이어짐(선택적으로 "다시 제안" 버튼 유지).

### 에러
- API 키 없음: 버튼 tooltip "설정에서 API 키를 넣어주세요", 비활성.
- 트리 빈 경우: 버튼 tooltip "기존 트리가 있어야 제안 가능", 비활성 (첫 판단은 사용자가 축 정하기).
- 호출 실패: 카드 리스트 자리에 회색 에러 한 줄. 재시도 버튼.

---

## 7. CSS 추가 (최소)

- `.inr-suggest-btn` — 판단문 아래 얇은 버튼. 활성/비활성 스타일.
- `.inr-suggestions` — 카드 리스트 컨테이너.
- `.inr-suggest-card` — 각 카드. hover 강조, 클릭 커서.
- `.inr-suggest-badge` — is_new_major 배지 (amber).
- `.inr-suggest-loading` — 작은 spinner or dot.

기존 색·폰트 규약 재사용.

---

## 8. 완료 기준

1. **인라인 새 판단 입력 → 판단문 쓴 뒤 [✨ AI에 path 제안받기] 버튼** 등장.
2. 클릭 → 잠깐 로딩 → **3개 카드**가 뜨고, 각 카드에 path(3단) + reason 한 줄 + (해당 시) "새 대분류" 배지.
3. **카드 클릭** → path 필드 자동 채움. 사용자가 원하면 그 위에서 다시 편집(override).
4. **엔터로 저장** → 트리에 박히고 목차/룰 페이지 갱신 (기존 인라인 흐름 그대로).
5. **API 키 없음·트리 빈 경우** → 버튼 tooltip 안내, 호출 안 함.
6. **AI가 없는 대분류를 제안**해도 문제없이 저장 가능 (새 대분류로 생성).
7. **override가 반영되는 진화**: 사용자가 특정 대분류로 override 계속하면 다음 새 판단 제안 시 그 대분류가 후보로 자주 등장 (트리 상태를 프롬프트에 넘기므로 자연스레).
8. 코어(`core/*`)·인라인 편집 다른 부분(EditableField, KindPicker)·기존 화면 안 바뀜.
9. `npm run build` 성공.

---

## 9. 끝나고 확인할 것 (나에게)

- **2·7번**을 집요하게 본다. 제안이 "성격으로 묶기"를 실제로 따르는지, 문서 종류로 나누지 않는지. 사용자가 override할 때마다 다음 제안이 그 방향으로 진화하는지.
- **첫 판단 시 진짜로 도움이 되는가.** 트리가 비어 있으면 제안 불가(정직히 안내), 3~4개 심은 뒤부터 제안 품질이 오르는지.
- **판단문 없이 버튼 눌러도 뭐가 나오는가.** 판단문 없이는 버튼 비활성 (심을 게 없으므로).
- 조각 5가 서면 v0.3 12장 "지금 범위" 다섯 항목 다 완성 — 위키 셸 + 인라인 편집 + 저작자 자동 + AI 찾기 + AI path 제안. **v0.3의 데모 완료.**

---

## 10. 다음 조각 예고 (여기 만들지 않음)

**조각 6+ (선택) — 나머지 12장 항목.**
- 수동 폼·검토 추출에도 AI path 제안 확장 (재사용).
- path 이동 UI(4.5장 원칙 5).
- 검토 추출 4단계 실체(6.3 · v0.2 지시서 결과물 격하 배치).
- 검토 렌즈(7.1).
- 팀 유형별 시작 발판 부록.
