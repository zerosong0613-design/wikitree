# 작업지시서 v0.4 — 조각 6a : 카테고리 저장소 + [카테고리 설정] + 목차 개편 + 인라인 편집 제거 (2026-08-02)

> 먼저 `CLAUDE.md v0.4`를 읽는다(**특히 3장 원칙 11, 4.6장, 10.3, 10.6, 12장**). v0.3까지의 리듬(조각별 실행)을 v0.4에서도 이어간다.
> 이 조각이 **v0.4의 뼈대**다 — 카테고리 저장소가 서야 6b(룰 페이지)·6c([지식 넣기])·6e(입구 카테고리 제약) 모두 얹힐 수 있다.

---

## 0. 시작 전 전제 확인

- [ ] `CLAUDE.md v0.4`를 읽었다 (원칙 9·10·11 재정의, 4.6장 신설, 10장 재작성).
- [ ] 조각 1~5 완료 상태: 스키마·저작자·위키 셸·AI 찾기·AI path 제안 모두 살아있음.
- [ ] 현재 코드에서 `views/inline/InlineNewRule.jsx`·`TocPanel`의 `+` 3자리·검색 필터 로직 위치를 안다.

전제가 되어 있으면, **코어(`core/schema.js`·`core/store.js`)는 카테고리 관련 최소 추가만.** 룰 페이지·AI 찾기·MoreDrawer는 오늘 안 건드림(6b·6c·6d).

---

## 1. 목표 (한 문장)

사용자가 [카테고리 설정] 모달에서 대·중분류를 정하면 `codex_categories`에 저장되고, 목차는 그 카테고리를 **지도**로만 보여준다 (기본 접힘·sticky·dot 잎에만·미분류 하단). **목차 안의 편집 자리(`+`)는 모두 제거** — 지도의 순수성을 되찾는다.

---

## 2. 오늘 범위 (이것만)

- **카테고리 저장소 신설:** `core/categories.js` (localStorage `codex_categories` 로드·저장·검증·프리셋).
- **[카테고리 설정] 모달** (`views/CategorySetup.jsx`): 프리셋 시드 · 대분류 리스트 · 이름 편집 · 순서 변경 · 삭제(룰 있으면 미분류로 이동 경고) · 중분류 pills · 저장.
- **상단 nav에 [카테고리 설정] 버튼** — 기존 `[…더보기]` 옆에 위치 (또는 별 자리, 10.1 참고).
- **TocPanel 개편:**
  - 기본 접힘(대분류만 보임), 대분류 헤더 sticky.
  - 강도 dot은 잎에만 (대분류엔 없음).
  - 미분류 맨 아래 고정 (있으면).
  - **`+` 편집 자리 3개(헤더·대분류·리프) 모두 제거.**
  - 카테고리 없으면 "카테고리가 아직 없습니다 · 먼저 판단을 분류할 큰 틀을 정해주세요" + [카테고리 설정] 유도 카드.
  - 카테고리 있는데 룰 없으면: 대분류들이 빈 상태로 보임 (규칙 수 0).
- **InlineNewRule 폐기:** `views/inline/InlineNewRule.jsx` 파일 삭제. TocPanel의 관련 state·핸들러도 정리.
- **KindPicker·EditableField는 유지** — 6b 룰 페이지에서 재사용.
- **코어에 미분류 강제 로직:** `store.js` merge에서 `path[0]`이 `codex_categories`에 없으면 강제로 `"미분류"`로 교체. 카테고리 없으면(초기) 이 검증 skip.

**오늘 안 하는 것 (6b·6c·6d·6e의 몫):**
- 룰 페이지 개편 (breadcrumb·credit·요약카드·hold안내·재검토·관련판단) — 6b.
- [지식 넣기] 상단 nav → 룰 페이지 새 룰 모드 — 6c.
- MoreDrawer 수동 폼 제거 — 6c.
- 입구 프롬프트에 카테고리 주입 — 6e.
- AI 찾기 채팅 UI 개편 — 6d.
- 스키마에 `summary`·`related` 추가 — 6b.

---

## 3. 파일 정리

**신설 (3개):**
- `core/categories.js` — 카테고리 저장소 (load/save/validate/preset).
- `views/CategorySetup.jsx` — 모달 UI.
- (선택) `components/CategorySetupTrigger.jsx` — 상단 nav 버튼용 (또는 App.jsx에 직접).

**수정:**
- `views/TocPanel.jsx` — `+` 3자리 제거, 기본 접힘, 미분류 하단, 강도 dot 잎에만.
- `views/WikiShell.jsx` — TocPanel에 `+` 관련 props 제거. 카테고리·미분류 관련 계산 유틸 필요 시.
- `App.jsx` — 상단 nav에 [카테고리 설정] 버튼. state·모달 open 관리. TocPanel에 `+` 관련 props 제거.
- `core/store.js` — merge에서 "미분류" 강제 로직 (categories 인자 받아 검증).
- `styles-core.css` — 카테고리 설정 모달 스타일 · 목차 sticky 헤더 · 미분류 카드.

**삭제:**
- `views/inline/InlineNewRule.jsx` — 폐기.
- `views/inline/` 폴더는 유지 (EditableField·KindPicker는 6b·6c에서 재사용).

**안 건드림:** RulePage(6b), AiFindPanel(6d), MoreDrawer(6c), Settings, entries/*, core/authoring.js, core/mdExport.js.

---

## 4. `core/categories.js` — 카테고리 저장소 (신설)

```js
// v0.4 조각 6a — 카테고리 저장소 (CLAUDE.md 4.6장).
// 사용자 소유. AI·입구는 이걸 읽기만. 하드코딩 아님(원칙 4·11).

export const CATEGORIES_KEY = "codex_categories";

// 프리셋 "법무 기본 카테고리" 시드. 편집·삭제 가능. 하드코딩 아님(사용자가 부를 때만 심음).
export const PRESET_LEGAL = [
  { name: "정보통제", subs: ["비밀유지", "목적 외 이용", "데이터 활용"] },
  { name: "책임·분쟁", subs: ["손해배상", "면책", "관할"] },
  { name: "절차·기한", subs: ["통지", "갱신"] },
  { name: "규제 대응", subs: ["개인정보", "공정거래"] },
  { name: "계약 일반", subs: ["서명", "수정"] },
  { name: "분쟁 해결", subs: ["중재", "화해"] },
];

export const UNCATEGORIZED = "미분류"; // 특수 대분류, 저장소엔 명시 안 함.

export function loadCategories() {
  try {
    const raw = localStorage.getItem(CATEGORIES_KEY);
    if (!raw) return { version: 1, categories: [] };
    const parsed = JSON.parse(raw);
    return normalizeCategoriesDoc(parsed);
  } catch {
    return { version: 1, categories: [] };
  }
}

export function saveCategories(doc) {
  const clean = normalizeCategoriesDoc(doc);
  localStorage.setItem(CATEGORIES_KEY, JSON.stringify(clean));
  return clean;
}

export function clearCategories() {
  localStorage.removeItem(CATEGORIES_KEY);
}

export function loadPreset() {
  return saveCategories({ version: 1, categories: PRESET_LEGAL.map((c) => ({ ...c, subs: [...c.subs] })) });
}

// path[0]이 카테고리에 있는지 확인. "미분류"는 항상 통과.
export function isMajorValid(major, doc) {
  if (major === UNCATEGORIZED) return true;
  return (doc.categories || []).some((c) => c.name === major);
}

// path[0]·path[1] 유효성 검증 → 안 맞으면 미분류 반환.
// 카테고리 없으면(빈 상태) 그대로 통과(초기 상태).
export function enforcePath(path, doc) {
  if (!doc || !doc.categories || doc.categories.length === 0) return path;
  const major = path?.[0];
  if (!major) return [UNCATEGORIZED];
  if (major === UNCATEGORIZED) return path;
  const majorNode = doc.categories.find((c) => c.name === major);
  if (!majorNode) return [UNCATEGORIZED, ...(path.slice(1) || [])];
  // 대분류 유효. 중분류도 확인 → 없으면 그대로 유지(부드럽게, 새 중분류 허용) or 잘라내기.
  // v0.4: 중분류도 카테고리 안이어야. 밖이면 대분류만 유지하고 중분류는 빈 값.
  const minor = path?.[1];
  if (minor && !majorNode.subs.includes(minor)) {
    return [major, "", ...(path.slice(2) || [])];
  }
  return path;
}

function normalizeCategoriesDoc(doc) {
  if (!doc || typeof doc !== "object") return { version: 1, categories: [] };
  const categories = Array.isArray(doc.categories) ? doc.categories : [];
  const clean = categories
    .map((c) => ({
      name: String(c?.name || "").trim(),
      subs: Array.isArray(c?.subs)
        ? Array.from(new Set(c.subs.map((s) => String(s || "").trim()).filter(Boolean)))
        : [],
    }))
    .filter((c) => c.name);
  return { version: 1, categories: clean };
}
```

---

## 5. `views/CategorySetup.jsx` — 설정 모달 (신설)

**Props:** `{ open, onClose, ruleCounts }` — `ruleCounts`는 `{ [majorName]: count }` (삭제 경고용, 부모가 계산).

**상태:**
- `doc` — 편집 중인 카테고리 문서 (open 시 loadCategories로 초기화).
- `editingName` — 어느 대분류 이름 편집 중인가.
- `newMinorFor` — 어느 대분류에 중분류 추가 중인가.

**UI 구조 (v0.4 10.6 그대로):**
```
[모달 오버레이]
  [제목 "카테고리 설정" + 부제 "판단을 분류할 큰 틀"] [✕]
  [안내 카드: "먼저 카테고리를 정하세요..."]
  
  { doc.categories 비었을 때 }
    [프리셋 카드]
      "아직 카테고리가 없습니다"
      [법무 기본 카테고리 불러오기] 버튼
      "정보통제 / 책임·분쟁 / ..." 미리보기
  
  { 카테고리 있을 때 }
    [대분류 헤더 "대분류" + [+ 대분류 추가]]
    [각 대분류 카드]
      번호 · 이름 (편집 시 input) · [이름 편집][순서↑↓][삭제]
      중분류 pills + [+ 중분류] 버튼
  
  [하단: Cody + [저장하고 닫기]]
```

**동작:**
- 프리셋 클릭 → `loadPreset()` → `doc` state 갱신.
- + 대분류 추가 → 이름 입력창 → 저장.
- 이름 편집 → inline input.
- 순서 ↑↓ → 배열 재정렬.
- 삭제 → `ruleCounts[name] > 0`이면 confirm ("이 카테고리 안 규칙 N개는 미분류로 옮깁니다. 계속?") · 미분류 이동은 6a에서는 다음 저장 시 코어 검증으로 자동 처리 (path[0]가 없어진 카테고리 → enforcePath로 "미분류"로).
- + 중분류 → 입력창 → subs에 추가.
- 저장하고 닫기 → `saveCategories(doc)` + `onClose()` + 트리도 재검증(App에서 setTree(loadTree())로 재로드).

---

## 6. `views/TocPanel.jsx` — 개편

**제거:**
- `+ 새 판단` 버튼 (헤더).
- 대분류 옆 `+` 버튼.
- 리프 hover 시 `+` 버튼.
- `openInline` state, `handleSaved`, InlineNewRule 렌더.
- `apiKey`, `treeRules`, `onTreeChange`, `onRequireAuthor` props (인라인 편집용).

**개편:**
- **기본 접힘:** `closed` state 초기값을 "모두 접힘"으로 (또는 세션 저장 유지).
- **대분류 헤더 sticky:** `.toc-major-row` 또는 `.toc-major-toggle`에 `position: sticky; top: 0; z-index: 2; background: ...`.
- **강도 dot은 잎에만:** 대분류 헤더에는 dot 없음(이미 그런 상태 확인). 규칙 수 라벨만.
- **미분류 하단 고정:** `groupByPath` 결과에서 "미분류" 카테고리를 배열 맨 뒤로 이동. 또는 렌더 시 분리.
- **선택된 leaf 배경**: `.toc-leaf.selected` 스타일 유지 (amber 톤이면 디자인 부합).
- **[카테고리 설정] 진입 버튼**: TocPanel 상단에 배치 (또는 상단 nav에도 병설).
- **빈 상태 안내**: `doc.categories.length === 0` → "카테고리가 아직 없습니다 · 먼저 판단을 분류할 큰 틀을 정해주세요" + [카테고리 설정] 버튼.

**Props (개편 후):** `{ rules, categoriesDoc, selectedRuleId, onSelect, onOpenCategorySetup }`.

**렌더 순서 in majors:**
1. `codex_categories`에 있는 순서대로 대분류 표시 (룰이 있든 없든).
2. `codex_categories`에 없는 대분류(예: "미분류" or 카테고리 삭제 잔재)는 맨 아래 별도 섹션 ("미분류").

**groupByPath 확장 or 새 함수:** 카테고리 순서를 반영하는 `groupByPathWithCategories(rules, categoriesDoc)` 유틸을 `core/schema.js` 또는 `core/categories.js`에 추가.

---

## 7. `App.jsx` 배선

- `useState`: `showCategorySetup` (boolean), `categoriesDoc` (loadCategories 초기값).
- 상단 nav에 **[카테고리 설정]** 버튼 추가 (기존 [+ 더보기] 옆). 클릭 → `setShowCategorySetup(true)`.
- `<CategorySetup open={showCategorySetup} onClose={...} ruleCounts={...} />` 렌더.
- 모달 닫힘 시 `setCategoriesDoc(loadCategories())` + `setTree(loadTree())` (트리도 재로드해 미분류 검증 반영).
- `WikiShell`에 `categoriesDoc`·`onOpenCategorySetup` 전달.
- **인라인 관련 props 정리:** WikiShell → TocPanel로 갔던 `apiKey`·`onTreeChange`·`onRequireAuthor` 프롭 중 목차용이던 것 정리 (룰 페이지·AI 찾기는 유지).

---

## 8. `core/store.js` — 미분류 강제 (추가)

`merge()`에 categories 인자를 받도록 확장하거나, `enforcePath`를 호출하는 래퍼 추가.

**옵션 A (권장): `addRules(incoming, categoriesDoc)` 시그니처 확장.**
```js
export function addRules(incoming, categoriesDoc = null) {
  // 각 raw에 enforcePath 적용 후 merge
  const guarded = (incoming || []).map((r) => {
    if (!categoriesDoc) return r;
    return { ...r, path: enforcePath(r.path, categoriesDoc) };
  });
  const merged = merge(loadTree(), guarded);
  saveTree(merged);
  return merged;
}
```

`updateRule`도 유사하게 categoriesDoc 인자 받아서 path 검증(사용자가 룰 페이지에서 path 이동 시 카테고리 밖으로 못 가게).

**caller 변경:** 지금은 `addRules([...])`만 호출. 6a에서는 caller가 아직 categoriesDoc을 안 넘겨도 backward compat (null이면 검증 skip). 6b·6c·6e에서 실제로 넘김.

---

## 9. CSS 추가 (styles-core.css)

- `.category-setup-modal` — 모달 (기존 .modal 스타일 재사용 가능, width 크게 900px).
- `.category-list` · `.category-card` · `.category-actions`.
- `.category-preset-card` — 프리셋 시드 카드.
- `.subs-pills` · `.sub-pill` + `.add-sub-btn`.
- `.toc-major-row.sticky` — sticky 헤더.
- `.toc-uncat-section` — 미분류 별도 스타일.
- `.toc-empty-cat` — 카테고리 없음 안내 카드.

---

## 10. 완료 기준

1. **[카테고리 설정]** 상단 nav 버튼 클릭 → 모달 열림. 비어있으면 프리셋 카드, 프리셋 클릭 시 6개 대분류 시드.
2. 대분류 추가·이름 편집·순서 변경·삭제 · 중분류 추가 · 저장하고 닫기 → `codex_categories` 저장.
3. 카테고리 있으면 목차에 대분류가 (규칙 0개라도) 순서대로 보임. 기본 접힘. 대분류 클릭 → 항목 펼침.
4. 대분류 헤더 sticky. 강도 dot은 항목(leaf)에만.
5. 목차 안 `+` 버튼·인라인 입력창 없음. `views/inline/InlineNewRule.jsx` 파일 삭제됨.
6. 카테고리 없으면 목차가 "카테고리가 아직 없습니다..." 안내로.
7. `path[0]`이 카테고리에 없는 룰은 목차 맨 아래 "미분류" 섹션에 모임. 새 룰 저장 시 코어가 검증(6b·6c·6e에서 활용).
8. `npm run build` 성공.
9. 코드에 대분류 이름 하드코딩 없음 (프리셋은 상수지만 시드일 뿐, 사용자가 편집·삭제 가능).

---

## 11. 끝나고 확인할 것 (나에게)

- **1·2번**을 집요하게 본다. 카테고리 설정이 실제로 codex_categories에 남고, 새로고침해도 유지되는지. 프리셋이 하드코딩 느낌 없이 편집되는지.
- **3·5번**: 목차가 순수 지도가 됐는지. `+` 자국이 완전히 없는지.
- 조각 6b·6c·6e에서 카테고리 인자를 addRules/updateRule에 어떻게 넘길지 시그니처가 명확한가.
- 이 조각이 서면 v0.4의 뼈대가 선다. 6b(룰 페이지)·6c(지식 넣기)·6d(AI 찾기)·6e(입구 카테고리)가 이 위에 얹힌다.

---

## 12. 다음 조각 예고 (여기 만들지 않음)

**조각 6b — 룰 페이지 개편.**
- 스키마에 `summary`·`related` 필드 추가.
- 룰 페이지 재작성: breadcrumb · credit 한 줄 · 요약 카드 · hold 안내 · 재검토 카드 · 관련 판단 pills.
- path 이동 UI (대·중분류 드롭다운, 카테고리 안에서만).
- 편집은 EditableField·KindPicker 재사용.
