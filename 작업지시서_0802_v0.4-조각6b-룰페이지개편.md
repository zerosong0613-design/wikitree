# 작업지시서 v0.4 — 조각 6b : 룰 페이지 개편 (2026-08-02)

> 먼저 `CLAUDE.md v0.4`(**특히 4장 스키마, 10.4, 6.1**)와 조각 6a 지시서를 읽는다.
> 6a에서 목차가 지도로 순수해졌으니, 이제 **편집·읽기의 중심을 룰 페이지 안으로** 옮긴다.
> 디자인 참고: `uploads/design_판단위키.dc.html`의 룰 페이지 중앙 스펙.

---

## 0. 시작 전 전제 확인

- [ ] `CLAUDE.md v0.4` 읽음. 특히 10.4(룰 페이지 재작성)·4장 스키마(신규 필드 `summary`·`related`).
- [ ] 조각 6a 완료: `codex_categories` 저장소, [카테고리 설정] 모달, 목차 지도화, `+` 제거.
- [ ] 트리에 룰 몇 개 심어둔 상태 (편집·표시 데모용).

전제가 되어 있으면, **코어는 스키마 확장만.** 목차·AI 찾기·모달·설정은 안 건드림.

---

## 1. 목표 (한 문장)

룰 페이지가 위키 페이지의 본격 형태를 갖춘다 — **breadcrumb · credit(누가·어디서·언제 한 줄) · 요약 카드 · hold 안내 · 재검토 · 관련 판단 pills** 순으로 흐르고, 판단문·note·kind·**summary**·**path**는 자리에서 마우스오버 편집. 새 필드 `summary`·`related`가 스키마에 자연스레 얹힌다.

---

## 2. 오늘 범위 (이것만)

- **스키마 확장:** `normalizeRule`에 `summary`(문자열, 기본 빈) + `related`(RULE id 배열, 기본 빈) 추가. v0.3 룰 하위호환.
- **RulePage 재작성:**
  - 상단 breadcrumb: `판단 찾기 › 대분류 › 중분류 › 항목(short)`.
  - 판단문 (h1, EditableField multiline).
  - **Credit 한 줄** (합성): `"김변호사가 · SK케미칼-한국콜마 위수탁계약에서 · 2026-08-02 추출"` (provenance 있으면 그걸 · 없으면 origin과 첫 author의 at).
  - 강도 pill (KindPicker 팝오버) + badge.
  - **요약 카드** (SUMMARY 라벨 · border box · EditableField multiline로 summary 편집).
  - **note** — EditableField (기존).
  - **hold 안내 카드** (조건부, kind === "hold"이면): "팀 판단이 갈린 자리 · 사람 확인 필요" + 원 note를 부연. [토론 열기] [담당자 지정] 버튼 자리 (실동작 없음, 자리만).
  - **재검토 카드**: `review_trigger.keywords`·`test`·`renew_trigger` 표시 + [재검토 요청] 버튼 자리 (실동작 없음).
  - **관련 판단 pills**: `rule.related`의 id들 → 트리에서 이름 lookup → 클릭 시 `onSelectRule` (read-only, 편집은 나중 조각).
  - **[역사] 토글** 유지 — 저작·이력·출처·메타 접힘 (기존 rp-history-drawer 그대로).
- **path 이동 UI (미니):**
  - breadcrumb 우측에 작은 **[카테고리 이동]** 버튼.
  - 클릭 → 인라인 편집 폼: 대분류 드롭다운(카테고리에서만) + 중분류 드롭다운(선택된 대의 subs) + 항목 텍스트 인풋 + [저장]/[취소].
  - 저장 → `updateRule(id, { ...rule, path: newPath }, categoriesDoc)`, action="path 이동".
  - 카테고리 밖 선택 불가 (드롭다운이 카테고리만 렌더).
- **updateRule 호출 시 `categoriesDoc` 전달** — path 검증 활성화 (이미 6a에서 시그니처 확장됨).
- **WikiShell → RulePage에 `categoriesDoc` 전달** (path 이동 드롭다운용) + `rules`(관련 판단 lookup용).

**오늘 안 하는 것 (6c·6d·6e의 몫):**
- 새 룰 만들기 = [지식 넣기] 상단 nav (조각 6c).
- MoreDrawer 수동 폼 제거 (조각 6c).
- 입구 프롬프트 카테고리 주입 (조각 6e).
- AI 찾기 채팅 UI (조각 6d).
- 관련 판단 편집 UI (미래 조각). 지금은 read-only 렌더.
- [토론 열기] · [담당자 지정] · [재검토 요청] 실동작 (자리만).

---

## 3. 파일 정리

**수정:**
- `core/schema.js` — normalizeRule에 `summary`·`related` 필드 추가.
- `views/RulePage.jsx` — 대개편 (위 스펙대로).
- `views/WikiShell.jsx` — RulePage에 `categoriesDoc`·`rules` 전달.
- `styles-core.css` — 새 카드 스타일 (credit, 요약 카드, hold 안내, 재검토 카드, 관련 판단 pills, path 이동 폼).

**안 건드림:** `core/store.js`·`core/authoring.js`·`core/categories.js`·TocPanel·CategorySetup·MoreDrawer·AiFindPanel·Settings·entries/*·SearchBar·EditableField·KindPicker.

---

## 4. 스키마 확장 (`core/schema.js`)

`normalizeRule` 반환 객체 안에 아래 두 필드 추가:

```js
// v0.4 신규 필드 (CLAUDE.md 4장)
summary: r?.summary ? String(r.summary) : "",
related: Array.isArray(r?.related)
  ? Array.from(new Set(r.related.map((x) => String(x || "").trim()).filter(Boolean)))
  : [],
```

- 기본값: 빈 문자열 / 빈 배열. v0.3 룰 로드해도 오류 없음.
- `related`는 중복 제거된 id 배열. 유효성(트리에 존재하는지)은 렌더 시 확인.

---

## 5. RulePage 구조 (재작성)

```
<main class="rule-page">
  { rule === null: 빈 상태 (기존 유지) }

  <!-- 상단 -->
  <div class="rp-top">
    <div class="rp-breadcrumb">판단 찾기 › {major} › {minor} › {itemShort}</div>
    <div class="rp-top-actions">
      <button class="rp-path-btn" onClick={togglePathEdit}>카테고리 이동</button>
      <button class="rp-hist-toggle" onClick={toggleHistory}>[역사]</button>
    </div>
  </div>

  { pathEditOpen && <PathEditForm categoriesDoc={...} value={rule.path} onSave={...} onCancel={...} /> }

  <!-- 판단 (제목) -->
  <EditableField multiline value={rule.judgment} displayClassName="rp-judgment" ... />

  <!-- Credit 한 줄 -->
  <div class="rp-credit">{creditLine}</div>

  <!-- 강도 pill + badge -->
  <div class="rp-strength-row">
    <button class="rp-strength" onClick={toggleKindPicker}>{rule.strength_label} ▾</button>
    { kindPickerOpen && ... }
    { rule.badge && <span class="rp-badge">{rule.badge}</span> }
  </div>

  <!-- 요약 카드 -->
  <section class="rp-summary-card">
    <span class="rp-summary-label">요약</span>
    <EditableField multiline value={rule.summary} displayClassName="rp-summary-text" placeholder="여기 요약을 적어보세요" ... />
  </section>

  <!-- note (한 줄, 짧게) -->
  <EditableField value={rule.note} displayClassName="rp-note" placeholder="+ 근거·양보 이력 한 줄 (선택)" ... />

  <!-- hold 안내 카드 (조건부) -->
  { isHold && <HoldNoticeCard rule={rule} /> }

  <!-- 재검토 카드 -->
  { hasReviewTrigger && <RecheckCard rule={rule} /> }

  <!-- 관련 판단 pills -->
  { hasRelated && <RelatedPills related={rule.related} tree={tree} onSelectRule={onSelectRule} /> }

  <!-- [역사] 뒤 -->
  { showHistory && <HistoryDrawer rule={rule} /> }
</main>
```

### 5.1 Credit line 합성 함수
```js
function creditLine(rule) {
  const author = rule.authors?.[0]?.name || "누군가";
  const at = rule.provenance?.extracted_at || rule.authors?.[0]?.at || rule.created_at || "";
  const source = rule.provenance?.source_contract
    ? `${rule.provenance.source_contract}에서`
    : originToHuman(rule.origin);
  const when = at ? fmtDateShort(at) : "";
  const verb = rule.provenance?.source_contract ? "추출" : "등재";
  return `${author}가 · ${source} · ${when} ${verb}`.replace(/\s+·\s+$/, "");
}

function originToHuman(o) {
  const m = { "인라인 편집": "인라인 편집으로", "룰 페이지 편집": "룰 페이지 편집으로",
              "수동 폼": "수동 폼으로", "새 룰": "새 룰 만들기로",
              "검토 추출": "검토 추출로", "계약 마이닝(diff)": "계약 마이닝으로",
              "인터뷰 채굴": "인터뷰로" };
  return m[o] || `${o}으로`;
}
```

### 5.2 Hold 안내 카드
```
<div class="rp-hold-card">
  <div class="rp-hold-head">⚠ 팀 판단이 갈린 자리 · 사람 확인 필요</div>
  <div class="rp-hold-body">{rule.note or "기준이 확정될 때까지 억지로 규칙화하지 않습니다."}</div>
  <div class="rp-hold-actions">
    <button class="rp-placeholder-btn" disabled>토론 열기</button>
    <button class="rp-placeholder-btn" disabled>담당자 지정</button>
  </div>
</div>
```

### 5.3 재검토 카드
```
<div class="rp-recheck-card">
  <div class="rp-recheck-info">
    <span class="rp-recheck-label">재검토</span>
    <div class="rp-recheck-triggers">
      { keywords.length && <div class="rp-triggers">chips</div> }
      { test && <div class="rp-trigger-test">테스트: {test}</div> }
      { renew_trigger && <div class="rp-trigger-test">RADAR: {renew_trigger}</div> }
    </div>
  </div>
  <button class="rp-placeholder-btn" disabled>재검토 요청</button>
</div>
```

### 5.4 관련 판단 pills
```
<div class="rp-related">
  <span class="rp-related-label">관련 판단</span>
  {rule.related.map(id => {
    const r = rulesById.get(id);
    if (!r) return null; // 존재 안 하면 skip
    return <button class="rp-related-pill" onClick={() => onSelectRule(id)}>{r.judgment}</button>;
  })}
</div>
```

### 5.5 Path 이동 폼
```
<div class="rp-path-edit">
  <select value={major} onChange={...}>
    {categoriesDoc.categories.map(c => <option>{c.name}</option>)}
    <option>미분류</option>
  </select>
  <select value={minor} onChange={...}>
    <option value="">(중분류 없음)</option>
    {majorNode.subs.map(s => <option>{s}</option>)}
  </select>
  <input type="text" value={item} onChange={...} placeholder="항목" />
  <button onClick={savePath}>이동</button>
  <button onClick={cancel}>취소</button>
</div>
```
- 중분류 드롭다운은 major 변경 시 그 대분류의 subs만 렌더.
- 저장 → `stampAuthoring({...rule, path: [major, minor, item].filter(...)}, "path 이동")` → `updateRule(rule.id, patched, categoriesDoc)` → `onTreeChange(newTree)`.
- 카테고리 자체 유효성은 코어(enforcePath)가 최종 방어.

---

## 6. WikiShell 배선

RulePage에 새 프롭 전달:
```
<RulePage
  rule={selectedRule}
  onTreeChange={onTreeChange}
  onRequireAuthor={onRequireAuthor}
  categoriesDoc={categoriesDoc}     ← 새
  rules={rules}                     ← 새 (관련 판단 lookup용)
  onSelectRule={onSelectRule}       ← 새 (관련 판단 클릭)
/>
```

WikiShell의 시그니처에는 이미 `categoriesDoc`·`rules`·`onSelectRule`이 있으니 통과만.

---

## 7. RulePage 편집 경로 전체 (updateRule에 categoriesDoc)

기존 `commitPatch(patch, action)` 함수에 categoriesDoc 인자를 넘김:
```js
function commitPatch(patch, action) {
  const stamped = stampAuthoring({ ...rule, ...patch }, action);
  const newTree = updateRule(rule.id, stamped, categoriesDoc);
  onTreeChange && onTreeChange(newTree);
}
```
- `saveJudgment`·`saveNote`·`saveKind`·`saveSummary`·`savePath` 모두 이 경로.
- `saveSummary`: action="summary 수정".
- `savePath`: action="path 이동", patch = `{ path: [...] }`. **path 이동은 판단·note 등을 안 바꿈** — 경로만.

---

## 8. CSS 추가 (styles-core.css)

**필요한 새 클래스:**
- `.rp-top-actions` — breadcrumb 우측 [카테고리 이동]·[역사] 정렬.
- `.rp-path-btn` — [카테고리 이동] 트리거 버튼.
- `.rp-path-edit` — 대·중분류 드롭다운 + 항목 인풋 + 저장/취소.
- `.rp-credit` — 은은한 mono/sub 톤 한 줄.
- `.rp-summary-card` + `.rp-summary-label` + `.rp-summary-text` (multiline EditableField 큰 본문 스타일).
- `.rp-hold-card` + `.rp-hold-head` + `.rp-hold-body` + `.rp-hold-actions` + `.rp-placeholder-btn`.
- `.rp-recheck-card` + `.rp-recheck-info` + `.rp-recheck-label` + `.rp-recheck-triggers`.
- `.rp-related` + `.rp-related-label` + `.rp-related-pill`.

디자인 HTML의 색·간격 참고: 카드에 `border:2px solid var(--ink); border-radius:5px; background:#fff; padding:15~17px`.

---

## 9. 완료 기준

1. **스키마** — `summary`·`related` 필드 있음. v0.3 룰 로드해도 폴백으로 빈 값. 새 편집 시 저장됨.
2. **RulePage** — 위→아래 순서: breadcrumb → 판단 → credit → 강도+badge → 요약 카드 → note → (hold이면) hold 안내 → 재검토 → 관련 판단.
3. **판단문·note·kind·요약** 마우스오버 편집 (기존 EditableField·KindPicker 재사용). 저장 시 history에 `"판단문 수정"`·`"note 수정"`·`"kind 변경"`·`"summary 수정"`.
4. **[카테고리 이동]** 버튼 → 대·중분류 드롭다운(카테고리에서만) + 항목 인풋 → 저장 시 path 이동, history에 `"path 이동"`. 카테고리 밖 이동 불가.
5. **Credit 한 줄** — provenance 있으면 계약명, 없으면 origin. 저작자 이름 + 날짜.
6. **hold 카드** — kind hold일 때만 등장. [토론 열기]/[담당자 지정] 자리(disabled).
7. **재검토 카드** — trigger 있을 때 등장. [재검토 요청] 자리(disabled).
8. **관련 판단** — rule.related의 id들이 pill로 렌더. 클릭 시 그 룰로 전환. 존재 안 하는 id는 skip.
9. **[역사] 토글**은 그대로 살아있음 (저작·이력·출처).
10. `npm run build` 성공.

---

## 10. 끝나고 확인할 것 (나에게)

- **2번(구조)**을 집요하게 본다. 룰 페이지가 나무위키 문서처럼 위→아래 흐르는지. 정보 밀도 vs 여백.
- **4번(path 이동)** — 카테고리 드롭다운이 카테고리 안 것만 보여주는지. 삭제된 카테고리로는 못 가는지.
- **3번(편집 저장)** — 저장 후 본문에 flash·강조 없음. 새 값 렌더되는 게 유일한 성공 신호(10.8).
- 조각 6b가 서면 룰 페이지가 v0.4의 형태를 갖춤. 다음(6c)에서 [지식 넣기]로 새 룰이 이 페이지에 진입.

---

## 11. 다음 조각 예고 (여기 만들지 않음)

**조각 6c — [지식 넣기] 상단 nav → 룰 페이지 새 룰 모드.**
- 상단 nav [지식 넣기] 버튼.
- 클릭 → 룰 페이지가 "새 룰 편집 모드" (빈 값들 + 카테고리 드롭다운 + 판단문 + kind + 저장 버튼).
- 저장 → 트리 push + 그 룰이 selectedRule로.
- MoreDrawer의 "수동 폼" 항목 제거.
