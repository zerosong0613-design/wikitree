# 작업지시서 v0.4 — 조각 6c : [지식 넣기] = 룰 페이지 새 룰 모드 (2026-08-02)

> `CLAUDE.md v0.4`의 6.2·10.1·10.7 참고. 조각 6a·6b 완료 후.

---

## 0. 시작 전 전제

- [ ] 조각 6a·6b 완료. 카테고리 저장소·룰 페이지 새 스펙 살아있음.
- [ ] 카테고리 몇 개 심어둔 상태 (드롭다운 데모용).

---

## 1. 목표 (한 문장)

상단 nav의 **[지식 넣기]** 버튼을 누르면 룰 페이지 자리가 **새 판단 폼**으로 바뀌고, 카테고리 드롭다운(codex_categories에서만) + 판단문 + kind로 5초 안에 심어진다. 저장 후 그 룰이 자동 선택되고 원래 화면으로.

---

## 2. 오늘 범위

- **신설 `views/NewRuleForm.jsx`** — 새 룰 편집 폼. 대·중 드롭다운·항목 인풋·판단 textarea·kind 칩·note·summary·[심기]·[취소].
- **`App.jsx`** — `newRuleMode` state + 상단 nav에 [판단 찾기]·[지식 넣기] 두 버튼(토글식). onSaveNewRule/onCancelNewRule 콜백. onSelectRule 시 자동 새 룰 모드 종료.
- **`views/WikiShell.jsx`** — `newRuleMode` 프롭. true면 RulePage 대신 NewRuleForm 렌더.
- **`views/MoreDrawer.jsx`** — "수동 폼" 탭 제거. 기본 탭을 "mining"으로.
- **`entries/ManualEntry.jsx`** 삭제.
- **카테고리 없음 처리** — NewRuleForm이 empty 상태를 자체 렌더 ("먼저 카테고리를 정하세요" + [카테고리 설정] 버튼).
- **저작자 자동 stamp** — `stampAuthoring(rule, "새 룰 등재")`, `origin="새 룰"`.
- CSS: 새 룰 폼·상단 nav 좌측 액션 그룹.

**오늘 안 하는 것 (6d·6e):** AI 찾기 채팅 UI·입구 프롬프트 카테고리 주입·AI path 제안 재활용(NewRuleForm 안).

---

## 3. NewRuleForm 시그니처

Props: `{ categoriesDoc, onSaved(newTree, newRule), onCancel, onOpenCategorySetup, onRequireAuthor }`

State: major/minor/item/judgment/kind/note/summary/error.

동작:
- 카테고리 없음 → empty 카드 (제목 + 안내 + [카테고리 설정] 버튼) 렌더.
- 대분류 드롭다운 = `codex_categories` + "미분류".
- 중분류 드롭다운 = 선택된 대분류의 subs. 대분류 바뀌면 중분류 초기화.
- 판단문 필수, 대분류 필수.
- 심기 → `stampAuthoring` → `addRules([stamped], categoriesDoc)` → `onSaved(newTree, savedRule)`.
- 이름 없음 → `NoAuthorError` catch → `onRequireAuthor()`, 폼 유지.

---

## 4. App.jsx 배선

State: `newRuleMode` (boolean).

핸들러:
```js
const openNewRule = () => setNewRuleMode(true);
const exitNewRule = () => setNewRuleMode(false);
const handleSelectRule = (id) => {
  setSelectedRuleId(id);
  setNewRuleMode(false); // 룰 클릭 시 자동 종료
};
const handleSaveNewRule = (newTree, newRule) => {
  setTree(newTree);
  setSelectedRuleId(newRule.id);
  setNewRuleMode(false);
};
```

상단 nav 좌측:
- [판단 찾기] (active if !newRuleMode) → `exitNewRule`
- [지식 넣기] (active if newRuleMode) → `openNewRule`
- [카테고리 설정] (기존)

우측 유지: [더보기]·[md 저장]·[비우기]·[⚙].

WikiShell에 `newRuleMode`·`onSaveNewRule`·`onCancelNewRule` 프롭 전달.

---

## 5. WikiShell 배선

`newRuleMode`이면 중앙에 `<NewRuleForm ... />` 렌더 (RulePage 대신). 좌 목차·우 AI 찾기는 그대로.

---

## 6. MoreDrawer 정리

- TABS 배열에서 `{ id: "manual", ... }` 제거.
- import ManualEntry 제거.
- 기본 탭을 `"mining"`으로.
- ManualEntry 렌더 코드 제거.

---

## 7. 완료 기준

1. 상단 nav에 **[판단 찾기]**·**[지식 넣기]** 두 버튼. active 상태 시각 표시.
2. [지식 넣기] 클릭 → 룰 페이지 자리에 새 룰 폼. 대·중 드롭다운·판단·kind·note·summary·[심기].
3. 대분류 = 카테고리에서만. 밖 선택 불가.
4. 판단 + 대분류 있으면 [심기] 활성. 저장 시 트리 추가 + selectedRule 세팅 + 폼 종료.
5. 이름 없이 저장 → Settings 자동 open.
6. 카테고리 없을 때 [지식 넣기] → "먼저 카테고리를 정하세요" 카드 + [카테고리 설정] 버튼.
7. 목차 룰 클릭 → 새 룰 모드 자동 종료.
8. MoreDrawer에서 "수동 폼" 탭 없음. 첫 탭 "계약 마이닝".
9. `entries/ManualEntry.jsx` 파일 삭제됨.
10. `npm run build` 성공.

---

## 8. 다음 조각 예고

**조각 6e — 입구 프롬프트 카테고리 제약.** 계약 마이닝·AI path 제안이 `codex_categories`를 컨텍스트로 받아 그 안에서만 분류.
**조각 6d — AI 찾기 채팅 UI.** 대화 버블 흐름 재작성.
