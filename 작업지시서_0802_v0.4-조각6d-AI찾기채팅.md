# 작업지시서 v0.4 — 조각 6d : AI 찾기 채팅 UI (2026-08-02)

> `CLAUDE.md v0.4`의 **7.2·10.5** 참고. 조각 6a·6b·6c·6e 완료 후 · v0.4의 마지막 조각.
> 디자인 참고: `uploads/design_판단위키.dc.html` 우측 AI 찾기 영역.

---

## 0. 시작 전 전제

- [ ] 조각 6a·6b·6c·6e 완료.
- [ ] 트리에 룰 심어짐 + API 키 세팅.

---

## 1. 목표 (한 문장)

우측 AI 찾기 패널을 **채팅 버블 흐름**으로 재작성한다 — 사용자/AI 대화 이력, 답 안에 **근거 pill**(강도색 dot + 룰 이름 + "김변호사, 8월" credit), 근거 없으면 회색 hold 배너, 하단 Cody가 상태별 안내.

---

## 2. 오늘 범위

- **`views/AiFindPanel.jsx`** 대개편:
  - `messages` state (배열, 세션 유지): `[{id, role: "user"|"ai", text, basis?, verdict?, error?, at}]`
  - 인풋: single-line input (chat 느낌), Enter 전송, 우측 ↵ 힌트
  - 버블: user(오른쪽·흰 배경) / ai(왼쪽·회색 배경, 검정 테두리)
  - AI 버블 안: 답 텍스트 + (조건부) hold 배너 + (있으면) 근거 pill 그룹
  - 근거 pill: `[색 dot | 짧은 이름 | credit]` — 클릭 시 룰 페이지 전환
  - 하단 Cody: 미니 두더지 + 말풍선(상태별 안내 한 줄)
  - 스크롤 자동 하단 이동
  - API 키·트리 없음 배너 유지
- **`styles-core.css`** 대화 UI 스타일:
  - 기존 `.af-result`·`.af-basis-card`·`.af-basis-*` 재활용 or 새 `.af-bubble-*` 세트
  - Cody row 스타일

**오늘 안 하는 것 (미래):**
- 대화 이력 지속 저장 (localStorage) — 새로고침 시 리셋 유지
- 팔로업 질문 (같은 세션 안 다중 턴 컨텍스트) — 지금은 각 질문마다 독립적으로 답
- 스트리밍

---

## 3. 메시지 데이터 구조

```js
{
  id: "msg-" + timestamp,
  role: "user" | "ai",
  text: string,
  basis: [{rule_id, why}]  // ai only
  verdict: "answered" | "hold"  // ai only
  error: boolean  // ai only, 오류 시
  at: ISO string
}
```

---

## 4. Credit 포맷 ("김변호사, 8월")

```js
function formatCredit(rule) {
  const author = rule.authors?.[0]?.name || "";
  const at = rule.provenance?.extracted_at || rule.authors?.[0]?.at || rule.created_at || "";
  if (!at) return author;
  const month = new Date(at).getMonth() + 1;
  return author ? `${author}, ${month}월` : `${month}월`;
}
```

## 5. 짧은 이름 (근거 pill 라벨)

```js
function ruleShort(rule) {
  return rule.path?.[2] || rule.path?.[1] || rule.path?.[0] || (rule.judgment || "").slice(0, 18);
}
```

---

## 6. 렌더 구조

```jsx
<aside class="ai-find-panel">
  <div class="af-head">
    <span class="af-title">AI 찾기</span>
    <span class="af-sub">우리 판단에서만 답을 찾습니다</span>
  </div>

  <div class="af-search-input">
    <input placeholder="무엇을 확인할까요?" onKeyDown={Enter → submit} />
    <span class="af-enter-hint">↵</span>
  </div>

  {/* 배너 */}
  { errorBanner }

  {/* 대화 리스트 */}
  <div class="af-messages" ref={messagesRef}>
    { messages.length === 0 && <IdleHint /> }
    { messages.map(m => 
      m.role === "user" 
        ? <UserBubble msg={m} />
        : <AiBubble msg={m} rulesById={...} onSelectRule={...} />
    ) }
    { status === "asking" && <LoadingBubble /> }
  </div>

  {/* 하단 Cody */}
  <div class="af-cody-row">
    <MiniMole />
    <div class="af-cody-bubble">{codyLine}</div>
  </div>
</aside>
```

### UserBubble
```jsx
<div class="af-bubble af-bubble-user">{msg.text}</div>
```

### AiBubble
```jsx
<div class="af-bubble af-bubble-ai">
  { msg.error && <span class="af-bubble-error-tag">오류</span> }
  <div class="af-bubble-text">{msg.text}</div>
  { msg.verdict === "hold" && (!msg.basis || msg.basis.length === 0) && (
    <div class="af-hold-inline">⚠ 확정 기준 없음 · 사람 확인 필요</div>
  )}
  { msg.basis?.length > 0 && (
    <div class="af-basis-inline">
      <span class="af-basis-label">근거 :</span>
      { msg.basis.map(b => {
        const rule = rulesById.get(b.rule_id);
        if (!rule) return null;
        return (
          <button class="af-basis-pill" onClick={() => onSelectRule(rule.id)}>
            <div class="af-basis-dot" style={{background: KIND_COLOR[rule.kind]}} />
            <span>{ruleShort(rule)}</span>
            <span class="af-basis-credit">{formatCredit(rule)}</span>
          </button>
        );
      }) }
    </div>
  )}
</div>
```

### Cody line (상태별)
```js
if (status === "error") return "잠깐 문제가 있어요";
if (status === "asking") return "트리를 근거로 찾고 있어요…";
const last = messages[messages.length - 1];
if (last?.role === "ai" && last.verdict === "hold") return "확정 기준이 없어요 — 사람 확인이 필요해요";
if (last?.role === "ai") return "근거가 있는 것만 답해요. 없으면 없다고 말할게요 🔦";
return "무엇이 궁금하세요? 우리 판단에서만 찾을게요";
```

---

## 7. 스크롤 자동 이동

`useEffect`로 messages·status 변화 시 `messagesEndRef.current?.scrollIntoView({behavior: "smooth"})`. 새 메시지가 하단에 붙어야 대화 느낌.

---

## 8. aiQuery / aiTrigger 통합

기존 useEffect 유지 — `aiTrigger`가 바뀌면 `runAsk(aiQuery)` 실행. 이제 대화 리스트에 자연스레 추가됨.

---

## 9. 인풋 처리

- Enter → submit (Ctrl+Enter 아님, 채팅 UX에 맞게 single-line은 Enter로)
- 전송 후 인풋 클리어
- textarea가 아니라 input (single line) — 긴 질문은 스크롤

---

## 10. CSS 스타일

기존 `.ai-find-panel`·`.af-head` 등은 유지·조정.

**신규/변경:**
- `.af-sub` — 서브 문구
- `.af-search-input` — 인풋 컨테이너 (테두리 있는 검색바 스타일, height 36px)
- `.af-enter-hint` — 우측 ↵ mono
- `.af-messages` — flex column, gap, flex:1, overflow-y auto
- `.af-bubble` (base) — border-radius 9px, padding 10~12px
- `.af-bubble-user` — align-self flex-end, border 2px solid var(--hold), background #fff, max-width 90%
- `.af-bubble-ai` — align-self flex-start, border 2px solid var(--ink), background var(--bg), max-width 100%
- `.af-bubble-text` — line-height 1.55
- `.af-hold-inline` — dashed hold border 배너 (기존 hold-banner 재활용/축소)
- `.af-basis-inline` — flex wrap, 답 하단, gap 6px
- `.af-basis-label` — "근거 :" 소제목 (mono·sub 색)
- `.af-basis-pill` — height 26px, padding 0 9px, border 2px solid (color), border-radius 13px, background #fff, cursor pointer, flex align-center gap
- `.af-basis-dot` — 8x8 원, kind 색
- `.af-basis-credit` — mono·sub 색
- `.af-cody-row` — 하단 flex row, border-top, padding-top
- `.af-cody-bubble` — 말풍선 (기존 cody-bubble 스타일 재활용 or 정의)

**제거·수정 대상 (기존 답 UI):** `.af-result`, `.af-answer`, `.af-basis` (기존), `.af-basis-card`, `.af-basis-top`, `.af-basis-kind`, `.af-basis-path`, `.af-basis-judgment`, `.af-basis-why`, `.af-basis-authors`, `.af-idle-hint`, `.af-hold-banner`. 새 클래스로 대체.

---

## 11. 완료 기준

1. **채팅 흐름** — 질문 → 사용자 버블 오른쪽, 답 → AI 버블 왼쪽. 대화 이력 유지 (세션 내).
2. **근거 pill** — 답 안에 (색 dot · 짧은 이름 · "김변호사, 8월") 형태. 클릭 시 룰 페이지 전환.
3. **hold 배너** — verdict hold 또는 근거 없을 때 회색 "⚠ 확정 기준 없음 · 사람 확인 필요".
4. **Cody 하단 상주** — 상태별 안내 한 줄.
5. **인풋 Enter 전송**, 전송 후 클리어.
6. **자동 스크롤 하단**.
7. **상단 검색바 엔터 승격** — 여전히 작동, 대화에 사용자 메시지로 추가됨.
8. **API 키·트리 없음** — 기존 배너 유지, 대화 안 됨.
9. 코어·다른 화면 안 바뀜.
10. `npm run build` 성공.

---

## 12. 완주 후

v0.4 "지금 범위" 5종 모두 완성 (6a·6b·6c·6e·6d). 이후 필요 시:
- CLAUDE.md 12장 **선택** 항목: 검토 추출 4단계·검토 렌즈.
- 팔로업 질문, 대화 지속 저장, 스트리밍 등 폴란드.
- path 이동 자유도, 인터뷰 채굴, 팀 유형별 발판.
