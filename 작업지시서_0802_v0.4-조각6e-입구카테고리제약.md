# 작업지시서 v0.4 — 조각 6e : 입구 프롬프트 카테고리 제약 + AI 항목 제안 (2026-08-02)

> `CLAUDE.md v0.4`의 **4.5(AI 제안)·4.6(카테고리 저장소)·6.3·6.4** 참고. 조각 6a·6b·6c 완료 후.

---

## 0. 시작 전 전제

- [ ] 조각 6a·6b·6c 완료: 카테고리 저장소, 룰 페이지 개편, 새 룰 폼 살아있음.
- [ ] 카테고리 몇 개 심어둔 상태 (프롬프트 컨텍스트로 넘기려면).

---

## 1. 목표 (한 문장)

계약 마이닝·AI path 제안이 **`codex_categories`를 컨텍스트로 받고 그 안에서만 분류·제안**한다. 새 대분류·중분류 발명 금지 (원칙 10·11). 카테고리 밖은 코어가 "미분류"로 강제 (원칙 11).

---

## 2. 오늘 범위

- **`prompt.js`** — 
  - `buildMiningPrompt(signedText, standardText, categoriesDoc)` 시그니처 확장. 카테고리 목록 주입 + "이 안에서만" 지시.
  - `buildPathSuggestPrompt(judgment, categoriesDoc, initialPath)` 재작성. rules 인자 제거, categoriesDoc으로. 새 대분류 발명 금지, `is_new_major` 필드 제거.
  - `summarizeCategoriesForPrompt(doc)` 헬퍼 신설 (`summarizeTaxonomy` 대체).
- **`entries/MiningEntry.jsx`** — `categoriesDoc` prop. buildMiningPrompt·addRules에 넘김.
- **`views/MoreDrawer.jsx`** — `categoriesDoc` prop 통과 → MiningEntry.
- **`App.jsx`** — MoreDrawer에 `categoriesDoc` 전달.
- **`views/NewRuleForm.jsx`** — **[✨ AI에 path 제안받기]** 버튼 추가. judgment 입력 후 클릭 → 3-카드 → 클릭 시 major/minor/item 채움. 카테고리 없거나 판단문 없으면 비활성.
- CSS: NewRuleForm 안 제안 카드 (기존 `.inr-suggest-*` 재사용 or `.nrf-suggest-*`).

**오늘 안 하는 것 (6d):** AI 찾기 채팅 UI.

---

## 3. 프롬프트 재작성

### 3.1 `buildMiningPrompt` — 카테고리 제약 주입

기존 프롬프트 하단에 새 섹션 추가:
```
[카테고리 제약 — v0.4 필수]
아래는 이 팀이 미리 정한 카테고리다. path의 [대분류, 중분류]는 반드시 이 목록에서 고른다.
새 카테고리를 만들지 마라. 어디에도 맞지 않으면 대분류를 "미분류"로 두고 이유를 note에 남긴다.

[카테고리]
- 정보통제 (비밀유지, 목적 외 이용, 데이터 활용)
- 책임·분쟁 (손해배상, 면책, 관할)
- ...

각 필드 설명 갱신:
- path: [대분류, 중분류, 항목]. 대·중분류는 반드시 [카테고리]에서 선택. 항목은 판단의 구체 소재.
```

카테고리 문서가 비어 있으면 이 섹션 대신 "카테고리 미설정 — 자유롭게 생성하되 코어가 미분류로 강제할 수 있음" 안내.

### 3.2 `buildPathSuggestPrompt` — 재작성

```js
export function buildPathSuggestPrompt(judgment, categoriesDoc, initialPath = []) {
  const taxonomy = summarizeCategoriesForPrompt(categoriesDoc);
  return `당신은 이 팀의 판단 위키 taxonomy 도우미다.
사용자가 새로 심으려는 판단문을 보고, 아래 [카테고리] 안에서 path 3개를 제안한다.

[출력 규칙]
- JSON만.
- 3개 제안. path(대·중·항목 3단 배열) + reason(왜 이 자리인가 한 줄).
- 대·중분류는 반드시 [카테고리]에서 고른다. 새 대분류·중분류 발명 금지.
- 항목(path[2])만 판단문의 성격으로 새로 만든다(짧게, 예: "배상 상한").
- 어디에도 안 맞으면 대분류 "미분류"로.
- initialPath가 주어졌으면 그 시작 경로를 존중.

[출력 형식]
{ "suggestions": [{ "path":["대","중","항목"], "reason":"..." }, ...] }

[카테고리]
${taxonomy || "(설정되지 않음 — 미분류만 가능)"}

[새 판단]
${String(judgment || "").trim()}

[초기 경로]
${(initialPath || []).filter(Boolean).join(" > ") || "없음"}`;
}
```

- v0.3의 `is_new_major` 필드 제거.
- v0.3의 `rules` 인자 (트리 상태 요약) 제거 — v0.4는 카테고리가 더 강한 축.

### 3.3 헬퍼
```js
function summarizeCategoriesForPrompt(doc) {
  if (!doc?.categories?.length) return "";
  return doc.categories
    .map((c) => `- ${c.name}${c.subs.length ? ` (${c.subs.join(", ")})` : ""}`)
    .join("\n");
}
```

---

## 4. MiningEntry 배선

- Props: `{ apiKey, onInjected, categoriesDoc }` 추가.
- `buildMiningPrompt(signedText, standardText, categoriesDoc)`.
- `addRules(stamped, categoriesDoc)` — 응답 검증.
- (선택) 카테고리 미설정 배너: MoreDrawer 안 계약 마이닝 탭에서 카테고리 없으면 "카테고리를 먼저 정하는 게 좋아요" 부드러운 안내.

---

## 5. NewRuleForm — AI 항목 제안

기존 판단 textarea 아래에 버튼 추가:
```
[✨ AI에 path 제안받기]  ← 판단문 있고 API 키 있고 카테고리 있을 때 활성
  (로딩)
[제안 카드 3개]  ← 클릭 시 major/minor/item 채움
```

동작:
- suggestPath 재사용(`api.js`).
- prompt: `buildPathSuggestPrompt(judgment, categoriesDoc, [major, minor, item])`.
- 결과 카드 렌더: path 3단 + reason. `is_new_major` 배지 제거.
- 카드 클릭 → setMajor/setMinor/setItem으로 채움. major/minor가 카테고리에 없는 경우(AI가 실수한 경우)는 코어 enforcePath가 미분류로 잡음.
- 응답의 path[0]이 카테고리에 없으면 필터로 제외 (안전장치).

Props: NewRuleForm에 `apiKey` 추가. WikiShell·App에서 전달.

---

## 6. 완료 기준

1. **계약 마이닝** — MoreDrawer의 계약 마이닝 탭에서 마이닝 시 프롬프트에 카테고리 주입됨. 카테고리 밖 결과는 코어가 미분류로 잡음.
2. **NewRuleForm AI 제안** — 판단문 쓰고 [✨ AI에 path 제안받기] 클릭 → 3-카드 → 각 path가 카테고리 안에 있음. 새 대분류 없음.
3. **카테고리 없을 때** — MiningEntry는 배너로 안내. NewRuleForm 버튼은 비활성.
4. `npm run build` 성공.
5. 응답 검증 안전장치: AI가 없는 대분류 반환해도 코어가 미분류로 처리.

---

## 7. 다음 조각 예고

**조각 6d — AI 찾기 채팅 UI.** 대화 버블 흐름 재작성.
