# 작업지시서 v0.5 — 조각 7a : 스키마 다축 확장 + 카테고리 v2 + 하위호환 (2026-08-02)

> 먼저 `CLAUDE.md v0.5`(**특히 3장 원칙 12·4장·4.5·4.6·5장**)를 읽는다. v0.4까지 완주된 코드 위에 얹는다.
> 이 조각이 **v0.5의 뼈대** — 다축 스키마·카테고리 3축·하위호환이 서야 뒤 조각들(UI·프롬프트)이 얹힌다.

---

## 0. 시작 전 전제

- [ ] `CLAUDE.md v0.5` 읽음 (원칙 12 신설, 4장 tags/contract_view, 4.6 카테고리 v2).
- [ ] v0.4 완주 코드 상태. `codex_tree`·`codex_categories`(v1)에 데이터 있음.

---

## 1. 목표 (한 문장)

룰 스키마에 **`tags`(원천·의뢰부서·주제) + `contract_view`(계약 자세)** 를 얹고, `codex_categories`를 **v2(3축)로 마이그레이션**한다. 기존 v0.4 룰·카테고리는 자동으로 살아남는다 (하위호환).

---

## 2. 오늘 범위 (이것만)

- **`core/schema.js`** — normalizeRule에 `tags`·`contract_view` 신설. v0.4 룰(tags 없음)은 빈 값 폴백. `path`는 정본으로 유지.
- **`core/categories.js`** — v2 스키마(3축) + v1→v2 마이그레이션. 3개 프리셋 상수(`PRESET_LEGAL`·`PRESET_ORIGIN`·`PRESET_DEPARTMENT`). API 확장 (`enforceOrigin`·`enforceDepartment`·`groupByAxis`).
- **`core/store.js`** — merge에서 tags 병합 규칙 추가 (단일 값 최신, 배열 합집합, contract_view 새 값 우선). enforce는 categories.js 위임. path·닫힌 축 검증.
- **하위호환:** loadTree 시 v0.4 룰이 오류 없이 로드되고 `tags = {원천:null, 의뢰부서:null, 주제:[]}`·`contract_view = null`이 자동 채워짐. loadCategories 시 v1 문서가 v2로 마이그레이션 (`판단`엔 원 categories, `원천`·`의뢰부서`는 빈 배열).
- **UI는 안 건드림** — 7b·7c·7d·7e·7f의 몫. 오늘은 코어만.

**오늘 안 하는 것:**
- [카테고리 설정] UI 3축 확장 (7b)
- 위키 축 토글 (7c)
- 룰 페이지 태그 카드 (7d)
- 프롬프트·NewRuleForm 3축 (7e)
- AI 찾기 근거 pill 확장 (7f)

---

## 3. 스키마 확장 (`core/schema.js`)

### 3.1 normalizeRule 반환에 추가 필드

```js
// v0.5 조각 7a — 다축 태그
tags: normalizeTags(r?.tags),
contract_view: normalizeContractView(r?.contract_view, tags.원천),
```

### 3.2 헬퍼

```js
function normalizeTags(t) {
  const o = t && typeof t === "object" ? t : {};
  return {
    // 원천·의뢰부서: 닫힌 축, 단일 문자열 or null
    원천: o["원천"] ? String(o["원천"]).trim() || null : null,
    의뢰부서: o["의뢰부서"] ? String(o["의뢰부서"]).trim() || null : null,
    // 주제: 열린 축, 문자열 배열 (dedupe)
    주제: Array.isArray(o["주제"])
      ? Array.from(new Set(o["주제"].map((s) => String(s || "").trim()).filter(Boolean)))
      : [],
  };
}

function normalizeContractView(cv, originValue) {
  // 원천이 "계약"이 아니면 contract_view는 항상 null
  if (originValue !== "계약") return null;
  if (!cv || typeof cv !== "object") return null;
  const contract_type = String(cv.contract_type || "").trim();
  const contract_group = String(cv.contract_group || "").trim();
  const article = String(cv.article || "").trim();
  const article_title = String(cv.article_title || "").trim();
  if (!contract_type && !contract_group && !article) return null;
  return {
    contract_type: contract_type || null,
    contract_group: contract_group || null,
    article: article || null,
    article_title: article_title || null,
  };
}
```

**핵심 규칙:**
- `tags.원천`·`tags.의뢰부서`가 없으면 `null` (사용자가 사후에 채움).
- `tags.주제`는 빈 배열 폴백.
- `contract_view`는 **원천=계약일 때만** 값 유지. 원천 바뀌면 자동 null (schema 단에서 정리).

### 3.3 v0.4 룰 하위호환 자동 처리
- v0.4 룰: `tags` 필드 없음 → normalizeTags(undefined) → 빈 값.
- `contract_view` 없음 → null.
- **`path`는 그대로 유지** — 판단 축의 정본. 아무 변경 없음.

---

## 4. 카테고리 저장소 v2 (`core/categories.js`)

### 4.1 v2 문서 형식

```json
{
  "version": 2,
  "판단": [ { "name": "정보·보안", "subs": ["비밀유지", "..."] }, ... ],
  "원천": [ { "name": "계약" }, { "name": "자문" }, ... ],
  "의뢰부서": [ { "name": "법무" }, { "name": "인사" }, ... ]
}
```

- `판단`만 `subs`(중분류) 배열. `원천`·`의뢰부서`는 단일 값(subs 없음, 있어도 무시).

### 4.2 프리셋 상수 (3개)

```js
export const PRESET_LEGAL = [ ...기존 v0.4 6분류 유지... ];

export const PRESET_ORIGIN = [
  { name: "계약" },
  { name: "자문" },
  { name: "회의·의사결정" },
  { name: "사고·분쟁대응" },
  { name: "규제·법령대응" },
  { name: "내부규정" },
];

export const PRESET_DEPARTMENT = [
  { name: "법무" },
  { name: "인사" },
  { name: "재무·세무" },
  { name: "구매·조달" },
  { name: "영업" },
  { name: "연구개발" },
  { name: "IT" },
  { name: "경영지원" },
];
```

### 4.3 v1 → v2 마이그레이션 (loadCategories 안)

```js
function migrateToV2(doc) {
  if (!doc || typeof doc !== "object") {
    return { version: 2, 판단: [], 원천: [], 의뢰부서: [] };
  }
  // 이미 v2
  if (doc.version === 2 && (doc["판단"] || doc["원천"] || doc["의뢰부서"])) {
    return normalizeV2(doc);
  }
  // v1 → v2: categories → 판단
  const 판단 = Array.isArray(doc.categories) ? doc.categories : [];
  return normalizeV2({
    version: 2,
    "판단": 판단,
    "원천": [],
    "의뢰부서": [],
  });
}
```

정규화:
- 각 축의 원소는 `{ name, subs? }` 형태. name 필수. 이름 중복 제거.
- 원천·의뢰부서는 subs 제거(단일 값).

### 4.4 API 확장

기존:
- `loadCategories()` — v2 반환 (v1 자동 마이그레이션)
- `saveCategories(doc)` — v2 저장
- `clearCategories()` — 유지

**신설·개편:**
- `loadPresetLegal()` — v2의 `판단` 축에 프리셋 심음 (기존 유지, 이름만 그대로).
- `loadPresetOrigin()` — v2의 `원천` 축에 프리셋 심음.
- `loadPresetDepartment()` — v2의 `의뢰부서` 축에 프리셋 심음.
- `enforcePath(path, doc)` — 기존과 동일 (doc.판단 사용).
- `enforceOrigin(value, doc)` — value가 doc.원천에 있으면 유지, 없으면 `null` 반환. `UNCATEGORIZED` 값도 null 취급(원천에는 미분류 개념 없음, 그냥 미지정).
- `enforceDepartment(value, doc)` — 위와 같은 방식.
- `groupByCategories(rules, doc)` — 기존 유지 (판단별 그룹핑).
- **`groupByAxis(rules, axis, doc)`** — 신설. `axis`는 `"판단"|"계약서"|"부서"|"원천"`. 7c에서 실체 채우지만 6a에서 시그니처만 준비.
- `countRulesByMajor(rules)` — 기존 유지.

**미리 정의만 (7c에서 실제 그룹핑 로직 채움):**
```js
export function groupByAxis(rules, axis, doc) {
  // 7c 조각에서 실체. 지금은 axis === "판단"이면 기존 groupByCategories, 나머진 [] 반환.
  if (axis === "판단") return groupByCategories(rules, doc);
  return { categorized: [], uncategorized: null }; // 7c에서 대체
}
```

---

## 5. 병합 규칙 확장 (`core/store.js`)

merge 함수 안 같은-path 병합 블록에 추가:

```js
tags: mergeTags(cur.tags, r.tags),
contract_view: r.contract_view || cur.contract_view || null,
```

`mergeTags` 헬퍼:
```js
function mergeTags(a = {}, b = {}) {
  return {
    원천: b?.["원천"] || a?.["원천"] || null,        // 단일: 새 값 우선, 없으면 기존
    의뢰부서: b?.["의뢰부서"] || a?.["의뢰부서"] || null,
    주제: Array.from(
      new Set([...(a?.["주제"] || []), ...(b?.["주제"] || [])])
    ),
  };
}
```

- 단일 값(원천·의뢰부서): 새 룰의 값이 있으면 새 것 · 없으면 기존 유지.
- 배열(주제): 합집합.
- `contract_view`: 새 값 우선.

**닫힌 축 검증 확장:**
`addRules(incoming, categoriesDoc)`에 이미 categoriesDoc 있음. 각 룰의 tags도 검증:
```js
const guarded = (incoming || []).map((r) => {
  if (!categoriesDoc) return r;
  const path = enforcePath(r?.path, categoriesDoc);
  const tags = r?.tags || {};
  return {
    ...r,
    path,
    tags: {
      ...tags,
      원천: enforceOrigin(tags["원천"], categoriesDoc),
      의뢰부서: enforceDepartment(tags["의뢰부서"], categoriesDoc),
      주제: Array.isArray(tags["주제"]) ? tags["주제"] : [],
    },
  };
});
```

`updateRule`도 동일 처리.

---

## 6. 완료 기준

1. **스키마 확장** — `normalizeRule` 반환에 `tags`·`contract_view` 있음. v0.4 룰 로드해도 폴백으로 `tags = {원천:null, 의뢰부서:null, 주제:[]}`, `contract_view = null`. `path`는 그대로.
2. **카테고리 v2** — `loadCategories()`가 v2 반환. v1 저장돼 있으면 자동 마이그레이션(`판단`엔 v1 categories, `원천`·`의뢰부서`는 빈 배열). 저장 시 v2 형식.
3. **프리셋 3개** — `loadPresetLegal`·`loadPresetOrigin`·`loadPresetDepartment` 각각 해당 축만 심는다.
4. **enforce 3함수** — path·origin·department 각각 카테고리 밖이면 미분류/null.
5. **merge 확장** — 같은 path 병합 시 tags 병합(단일 새 값 우선·주제 합집합), contract_view 새 값 우선.
6. **addRules·updateRule** — categoriesDoc이 있으면 tags도 검증.
7. **groupByAxis 시그니처** — 존재 (판단 축만 실체, 나머지 7c에서).
8. **UI 안 건드림** — TocPanel·RulePage·NewRuleForm·CategorySetup·App 코드 변경 없음.
9. `npm run build` 성공.

---

## 7. 끝나고 확인할 것

- **v0.4 데이터로 새로고침** — 오류 없이 로드. 룰 페이지·목차 그대로 뜸(다축은 UI가 없으니 안 보임). localStorage `codex_categories`가 v2로 자동 변환됨.
- **7b·7c·7d·7e에서 이 파일들을 import해 UI를 얹을 수 있는가.** categories.js API가 명확한가.
- `loadCategories()` 여러 번 호출해도 안전한가 (idempotent).

---

## 8. 다음 조각 예고

**7b — [카테고리 설정] 3축 탭.** CategorySetup 모달에 상단 탭 3개. 각 축 CRUD + 프리셋.
