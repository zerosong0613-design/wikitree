# 작업지시서 v0.5 — 조각 7b : [카테고리 설정] 3축 탭 (2026-08-02)

> `CLAUDE.md v0.5` 10.6 참고. 7a 완료 후.

---

## 목표

CategorySetup 모달에 **상단 탭 [판단]·[원천]·[의뢰부서]** 추가. 각 축에서 CRUD + 프리셋 시드.

---

## 오늘 범위

- `views/CategorySetup.jsx` — 탭 UI 추가. 활성 탭에 따라 해당 축의 리스트 렌더 + 프리셋 버튼.
  - 판단 탭: 기존 UI (subs 있음).
  - 원천·의뢰부서 탭: 단일 값 리스트 (subs 없음, 중분류 pills 영역 숨김).
- `App.jsx` — `ruleCounts` 계산 시 판단 축(대분류) 카운트만이 아니라 원천·부서도 필요할 수 있으나, 지금은 판단 축만 유지 (삭제 경고용). 나머지 축의 삭제 경고는 나중.
- CSS — 탭 스타일.

---

## 완료 기준

1. 모달 상단에 3개 탭. 클릭 시 활성 축 전환.
2. 각 탭에서 대분류 추가·이름 편집·순서 변경·삭제.
3. 판단 탭에서만 중분류 pills. 원천·의뢰부서는 subs 없음.
4. 각 탭에 프리셋 시드 버튼 (해당 축 비어있을 때만).
5. 저장 시 모든 축 유지 (다른 축 지우지 않음).
6. `npm run build` 성공.
