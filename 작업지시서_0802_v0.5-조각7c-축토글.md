# 작업지시서 v0.5 — 조각 7c : 위키 축 토글 + groupByAxis 실체화 (2026-08-02)

> `CLAUDE.md v0.5` 10.3 참고. 7a·7b 완료 후.

---

## 목표

목차 상단에 **축 토글 [판단별] [계약서별] [부서별] [원천별]**. 클릭 시 같은 데이터를 다른 그룹핑으로. **판단별 = 집계**(기존), **계약서별 = 나열**(원천=계약만, contract_group>type>article), **부서별/원천별 = 그룹핑**.

---

## 오늘 범위

- `core/categories.js` — `groupByAxis(rules, axis, doc)` 실체 채움:
  - `판단`: `groupByCategories` (기존)
  - `계약서`: 원천=계약 룰만 필터, contract_group>contract_type>article 순
  - `부서`: `tags.의뢰부서` 기준 그룹핑 (부서 목록 순서, 미지정 하단)
  - `원천`: `tags.원천` 기준 그룹핑 (원천 목록 순서, 미지정 하단)
- `views/TocPanel.jsx` — 축 토글 UI 추가, `axis` prop 받아 `groupByAxis` 호출. 그룹 렌더는 축별로 살짝 다르게(계약서별은 3단 nested).
- `views/WikiShell.jsx` — `axis`·`onAxisChange` prop 통과.
- `App.jsx` — `axis` state.
- CSS.

---

## 완료 기준

1. 목차 상단에 4개 축 토글. 기본 판단별.
2. 축 클릭 시 그룹핑 즉시 재계산 (데이터 불변).
3. 계약서별에서 원천=계약 룰만 · contract_group > contract_type > article 순.
4. 부서·원천 없는 룰은 "미지정" 섹션 하단.
5. 계약 원천 룰 없으면 계약서별에 안내.
6. `npm run build` 성공.
