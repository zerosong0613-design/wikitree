# 작업지시서 v0.5 — 조각 7d : 룰 페이지 태그 카드 + contract_view 편집 (2026-08-02)

> `CLAUDE.md v0.5` 10.4 참고. 7a·7b·7c 완료 후.

---

## 목표

룰 페이지에 **태그 카드** (원천·의뢰부서 드롭다운 · 주제 pills) + **contract_view 카드** (원천=계약일 때만) 추가. 편집 시 `updateRule`에 categoriesDoc 전달, action별 history stamp.

---

## 오늘 범위

- `views/RulePage.jsx` — 태그 카드 + contract_view 카드 신규. 편집 헬퍼 추가:
  - `saveOrigin(newValue)` action="원천 변경". 원천이 "계약"이 아니게 되면 contract_view 자동 null.
  - `saveDepartment(newValue)` action="의뢰부서 변경".
  - `saveTopics(newArray)` action="주제 편집" (추가·제거 모두 이 액션 하나로).
  - `saveContractView(newObj)` action="contract_view 수정".
- 태그 카드 UI: 원천/의뢰부서 드롭다운 + 주제 pills 추가/제거.
- contract_view 카드 UI: 4필드(contract_type·contract_group·article·article_title) 인라인 편집.
- CSS.

---

## 완료 기준

1. 룰 페이지에 태그 카드 등장. 원천/의뢰부서/주제 표시 + 편집.
2. 원천=계약일 때만 contract_view 카드 등장. 4필드 편집.
3. 원천을 "계약"에서 다른 값으로 바꾸면 contract_view 자동 null.
4. 편집마다 history 자동 stamp.
5. `npm run build` 성공.
