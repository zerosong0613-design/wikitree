# 작업지시서 v0.5 — 조각 7e : 입구 프롬프트 3축 + NewRuleForm 다축 (2026-08-02)

> `CLAUDE.md v0.5` 4.5·4.6·6장 참고. 7a~7d 완료 후.

---

## 목표

- 계약 마이닝·AI 축 제안 프롬프트에 **3축 카테고리 목록 주입** + `contract_view` 자동 채움 지시.
- NewRuleForm에 **원천·의뢰부서 드롭다운 + 주제 pills + contract_view 폼(조건부)** 추가.
- AI 축 제안이 이제 판단 path뿐 아니라 원천·의뢰부서·주제·contract_view까지 자동 제안.

---

## 오늘 범위

- `prompt.js` — 
  - `buildMiningPrompt(text, standardText, categoriesDoc)` 프롬프트에 3축 + contract_view 지시 추가.
  - `buildPathSuggestPrompt` → `buildTagsSuggestPrompt(judgment, categoriesDoc, initialPath, initialTags)`로 확장. path뿐 아니라 원천·의뢰부서·주제·contract_view까지 제안.
  - `axesBlockForPrompt(doc)` 헬퍼 (3축 요약).
- `views/NewRuleForm.jsx` — 원천/의뢰부서 드롭다운·주제 pills·contract_view 폼 (원천=계약이면). AI 제안 버튼 결과가 이제 태그도 채움.
- `entries/MiningEntry.jsx` — 프롬프트에 categoriesDoc 그대로 전달(기존 그대로), 반환 룰이 tags·contract_view도 갖도록 normalizeRule 통과 → store가 검증.
- CSS.

---

## 완료 기준

1. NewRuleForm에 원천·의뢰부서 드롭다운·주제 pills·contract_view 폼(원천=계약이면).
2. [AI에 축 제안받기] 클릭 → AI가 판단 path + 원천 + 의뢰부서 + 주제 + contract_view까지 제안.
3. 클릭 시 필드 자동 채움. 카테고리 밖 값은 필터 (안전장치).
4. 계약 마이닝 결과 룰이 tags·contract_view도 채움.
5. `npm run build` 성공.
