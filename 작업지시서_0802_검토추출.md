# 작업지시서 — 검토 추출 입구 + 저작·출처 (2026-08-02)

> 먼저 `CLAUDE.md v0.2`를 읽는다. 이 지시서는 그 위에 얹는 **한 조각**이다.
> 오늘 만드는 것: **검토 추출 입구(6.2) + 저작·출처 스키마(4장).**
> 이게 "실제 활용"을 여는 핵심 조각이다. 통계 채굴과 별개의 새 워크플로우다.

---

## 0. 시작 전 전제 확인

- [ ] 트리가 localStorage(`codex_tree`)에 쌓이고 새로고침해도 남는다.
- [ ] 룰이 CLAUDE.md 4장 스키마를 따른다.
- [ ] [위키] 화면이 `path`로 트리를 그린다.

전제가 되어 있으면, **코어·기존 입구 코드는 건드리지 말고** 새 입구만 붙인다(구조 원칙 1). 스키마에 필드를 더하는 것은 코어 저장 로직에 필드가 통과되게만 하고 기존 룰과 호환(없으면 null/빈배열)되게 한다.

---

## 1. 목표 (한 문장)

내가 **검토한 계약 한 건**을 넣으면, AI가 조항별로 "여기서 이렇게 판단했다"를 추출하고, 나는 조항별로 **넣기/수정/빼기**를 골라, 넣은 것만 **누가·어느 계약·언제**의 출처와 함께 트리에 저장한다.

---

## 2. 오늘 범위 (이것만)

- 스키마에 `authors`·`history`·`provenance` 추가(4장).
- '지식 넣기'에 **검토 추출** 입구 추가(계약 마이닝과 별개 모드).
- 4단계 워크플로우: 넣기 → AI 분석 → 사람이 고르기 → 출처와 저장.
- 설정에 **내 이름** 입력.

**오늘 안 하는 것:** AI 찾기, 인터뷰 채굴, 파일 OCR/hwp, 위키 룰 페이지 개편. (검토 추출로 들어온 룰이 기존 위키에 그대로 보이기만 하면 됨.)

---

## 3. 스키마 — 저작·출처 추가

CLAUDE.md 4장의 신규 3필드를 실제로 채운다.
```json
"authors": [{"name": "", "role": "초안|승인|검토·추출|수정", "at": ""}],
"history": [{"who": "", "action": "", "at": ""}],
"provenance": {
  "source_contract": "계약명 (시점)",
  "reviewed_by": "검토자 이름",
  "extracted_at": "YYYY-MM-DD",
  "source_article": "제3조",
  "extraction_method": "검토 추출"
}
```
- 기존에 이미 쌓인 룰에 이 필드가 없어도 오류 없이 동작해야 한다(폴백: 빈 배열/객체).
- **검토 추출로 들어온 룰은** `origin`=`검토 추출`, `authors`=[{name: 내이름, role: "검토·추출"}], `provenance` 채워짐, `status`=`candidate`.

---

## 4. 화면 — 검토 추출 입구 (4단계)

'지식 넣기' 탭에서 **검토 추출** 모드 선택 시:

**1단계 넣기.**
- "검토한 계약 한 건" 입력(텍스트, 향후 파일 드롭 자리). + "표준안"(선택).
- 상단에 이 건의 메타: **계약명**(사용자 입력) + **검토자**(설정의 내 이름 자동) + 오늘 날짜. → 이게 provenance가 된다.
- "분석" 버튼.

**2단계 AI 분석(추출, 판정 아님).**
- AI가 조항별 카드 목록 반환: 각 카드 = 조항 번호 + "여기서 내린 판단" 한 줄 + (표준안 있으면) 표준 대비 상태(표준대로/수정/신규) + 제안 `path`·`kind`.
- **AI는 옳고 그름을 말하지 않는다.** "당신이 이렇게 했다"만. 카드 문구도 "~로 판단함", "~를 추가함" 식.
- 코디: "이 계약에서 조항별로 이렇게 판단하셨네요. 어떤 걸 트리에 넣을지 골라주세요."

**3단계 사람이 고르기(자동화 금지).**
- 조항 카드마다 버튼 3개: **[넣기] / [수정해서 넣기] / [빼기]**.
- [수정해서 넣기]: 판단문·path·kind를 인라인 편집 후 넣기.
- [빼기]: **빼는 이유 필수 입력**("이 계약 특수사항 — 일반 기준 아님" 등). 뺀 것과 이유는 이 검토 세션 기록에는 남기되 트리에는 안 넣는다.
- 기본값은 아무것도 선택 안 됨(전부 자동 등재 금지). 사람이 명시적으로 골라야 한다.

**4단계 저장.**
- 넣기로 한 조항만 스키마로 변환 → provenance(계약명·검토자·날짜·조항)·authors(검토자, role "검토·추출")·history(1건: "검토 추출로 등재") 박아서 코어에 주입.
- 같은 path가 이미 있으면 병합(CLAUDE.md 5장) — history·authors 누적.
- 완료 후: "N개 조항을 트리에 넣었어요(출처: {계약명}). K개는 빼기로 두셨어요." + 위키에서 확인 링크.

---

## 5. AI 분석 프롬프트 (추출 전용, 판정 아님)

```
아래는 한 사람이 이미 검토·수정한 계약 한 건이다. (있으면) 표준안과 함께 준다.
당신의 일은 옳고 그름을 판정하는 것이 아니라, 이 사람이 조항별로 어떤 판단을 내렸는지 그대로 추출하는 것이다.

각 조항에 대해:
- 이 조항에서 내린 판단을 한 문장으로.
- (표준안이 있으면) 표준 대비 상태: 표준대로 | 수정함 | 신규 추가.
- 제안 path([대분류, 중분류, 항목])와 kind(hard|soft|neu|hold). 단, 이건 제안일 뿐 최종은 사람이 정한다.
- 이 조항이 '일반 기준'으로 보이는지 '이 계약만의 특수사항'으로 보이는지 힌트를 준다(단정하지 말 것).

JSON 배열만 출력:
[{"article":"제3조","judgment":"...","vs_standard":"표준대로|수정|신규","suggested_path":["",""],"suggested_kind":"","generality_hint":"일반 기준으로 보임|이 계약 특수사항으로 보임"}]

[검토한 계약] {{계약 텍스트}}
[표준안] {{있으면, 없으면 "없음"}}
```
- `generality_hint`는 사람의 3단계 판단을 돕는 힌트일 뿐, 자동 등재/제외에 쓰지 않는다.
- API 호출은 기존 계약 마이닝과 같은 함수 재사용(설정 키, 모델 상수 MODEL).

---

## 6. 설정 — 내 이름

- 설정(⚙)에 "내 이름"(검토자) 입력 필드 추가, localStorage 저장.
- 이름이 없으면 검토 추출·수동 입력 시 "이름을 먼저 입력하세요" 안내(저작자 없는 룰 방지, CLAUDE.md 2장).

---

## 7. 완료 기준

1. 검토 추출 모드에서 계약 한 건 넣고 분석 → 조항별 카드가 뜬다(판정이 아니라 "이렇게 판단함" 추출).
2. 카드별 넣기/수정/빼기가 되고, 빼기는 이유를 받는다.
3. 넣은 룰에 **provenance(계약명·검토자·날짜·조항)와 authors(검토자)** 가 실제로 박힌다.
4. 위키 룰(또는 카드)에서 그 룰의 **출처와 저작자가 보인다**. 로그(최근 변경)에 "누가"가 나온다.
5. 같은 path 재등재 시 병합되며 history·authors가 누적(덮어쓰지 않음).
6. 이름 없이 저장 시도 → 막힌다.

---

## 8. 끝나고 확인할 것 (나에게)

- **3·4번**을 집요하게 본다. 이 워크플로우의 값은 판단이 아니라 **"누가·어디서·언제"가 룰에 박히는 것**이다. 그게 없으면 그냥 또 하나의 채굴이고, 있으면 책임 있는 제도적 지식이 된다.
- 3단계 "빼기 + 이유"가 실제로 기록되는지 본다. 안 넣기로 한 결정과 이유도 지식이다(나중에 "왜 이건 트리에 없지?"의 답).
- 이게 되면 발표 클라이맥스가 선다: "어제 검토한 계약을 넣으면, 제 판단이 조항별로 정리되고, 제가 고른 것만 제 이름·이 계약·날짜와 함께 트리에 남는다. 3년 뒤 누가 봐도 제가 정한 걸 안다."
