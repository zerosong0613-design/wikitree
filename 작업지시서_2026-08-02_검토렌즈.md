# 오늘 작업지시서 — 검토 렌즈 (2026-08-02)

> 먼저 `CLAUDE.md`(마스터 계획서)를 읽는다. 이 지시서는 그 위에 얹는 **오늘 조각 하나**다.
> 오늘 만드는 것: **[검토하기] 탭 = 쌓인 지식으로 새 입력을 비추는 렌즈.**
> 이게 "저장된 지식이 실제로 활용된다"의 증명이고, 지금 빌드에 유일하게 빠진 조각이다.

---

## 0. 시작 전 전제 확인

아래가 이미 되어 있어야 오늘 작업이 의미 있다. 안 되어 있으면 **먼저 그것부터** 하고 렌즈는 그다음.
- [ ] 트리가 localStorage(`codex_tree`)에 쌓이고 새로고침해도 남는다.
- [ ] 룰이 CLAUDE.md 3장 공통 스키마를 따른다.
- [ ] [위키트리] 화면이 `path`로 트리를 그린다.

전제가 되어 있으면, **기존 코어·입구 코드는 건드리지 말고** 렌즈만 새로 붙인다(구조 원칙 1).

---

## 1. 목표 (한 문장)

새 입력(계약 조항·법령 변경·사업 제안 등 아무 텍스트)을 붙여넣으면, 트리의 노드들과 대보고 **위반 / 해당 / 보류 / 무관**으로 판정해 걸린 노드를 트리에서 드러낸다.

---

## 2. 오늘 범위 (이것만)

- 스키마에 `review_trigger` 필드 채우기(기존 룰에도 소급 부여).
- [검토하기] 탭 추가 + 판정 로직(1차 키워드 → 2차 LLM → 3차 보류 안전장치).
- 결과를 트리 하이라이트 + 판정 배지로 표시.

**오늘 안 하는 것:** 다른 입구, 파일 드롭, 챗봇, 초안 생성, Notion. (자리만 있으면 됨.)

---

## 3. 스키마 — `review_trigger` 채우기

CLAUDE.md 3장 스키마의 `review_trigger`를 실제로 쓴다.
```json
"review_trigger": {
  "keywords": ["배상", "손해", "cap", "한도"],   // 입력에 이 신호가 있으면 이 노드가 1차 후보
  "law_group": "상법 배상책임",                 // 규제형 노드만(선택). 계약형은 null 허용
  "test": "입력에 배상 상한 조항이 있는가"        // 애매할 때 LLM이 판정하는 문장
}
```
- 계약 마이닝·수동 입력이 앞으로 룰을 만들 때 이 필드도 채우게 한다(마이닝 프롬프트에 "각 룰에 review_trigger.keywords 3~6개와 test 한 문장을 부여" 추가).
- **기존에 이미 쌓인 룰**에는 `review_trigger`가 없을 수 있다. 없으면 판정 시 `judgment`·`path`·`note`에서 LLM이 즉석 추론하도록 폴백을 둔다(없다고 그 노드를 건너뛰지 말 것 — 미탐 방지).

---

## 4. [검토하기] 화면

- 입력창(멀티라인) + "검토" 버튼. placeholder: "검토받을 계약 조항·법령 변경·제안을 붙여넣으세요."
- 검토 실행 시 코디가 헤드램프로 트리를 비추는 로딩 상태.
- 결과: 걸린 노드를 [위키트리]와 같은 트리 위에서 하이라이트 + 판정 배지.
  - **위반** → hard색 `#9E3524` 테두리 점멸.
  - **해당** → soft색 `#C8871F`.
  - **보류** → hold색 `#8A9086` 점선 + "?" 표기. (관련 있는데 트리에 확정 기준이 없음 — 사각지대가 드러나는 순간.)
  - **무관** → 표시 안 함.
- 각 판정에 근거 한 줄 + 그 기준이 어디서 왔는지 `path` 표시.
- 코디 멘트 예: "이 입력, 3개 기준에 걸렸어요. 하나는 마지노선 위반, 하나는 저도 몰라서 사람 확인이에요."

---

## 5. 판정 로직 (3단)

```
[새 입력]
  │
1차 로컬 필터 ── 각 노드 review_trigger.keywords와 입력 매칭 → 후보 노드 추림 (빠름)
  │
2차 LLM 판정 ── 후보 노드 + 입력을 Anthropic에 보내 노드별 verdict 반환
  │
3차 보류 안전장치 ── keywords가 하나도 안 걸려도, 입력이 어떤 대분류와 문언상 관련되면
                     '보류'로 사람 확인에 올린다 (RADAR C유형 대응·미탐 방지)
```

**미탐 우선(양보 불가):** 확실치 않으면 무관이 아니라 보류. 트리에 기준 없는 관련 입력은 반드시 보류로 올린다.

---

## 6. LLM 판정 프롬프트 (고정)

```
아래는 우리 팀이 쌓아온 판단 기준(트리 노드)들이다. 그리고 새로 들어온 입력이다.
각 기준에 대해 이 입력이 (위반 | 해당 | 무관 | 보류) 중 무엇인지 판정하라.
- 위반: 입력이 이 기준(특히 hard/마지노선)을 어긴다.
- 해당: 입력이 이 기준의 검토 대상에 든다.
- 보류: 관련은 있으나 트리에 확정된 기준이 없다. 억지로 판정하지 말고 보류로 사람에게 올려라.
- 무관: 관계없음.
확실하지 않으면 무관이 아니라 보류를 택하라. 놓치는 것이 틀리는 것보다 위험하다.

JSON 배열만 출력한다:
[{"node_id": "", "verdict": "위반|해당|무관|보류", "why": "한 문장"}]

[트리 기준들] {{후보 노드 배열: id, path, judgment, kind, review_trigger}}
[새 입력] {{입력 텍스트}}
```

- API 호출은 기존 계약 마이닝과 같은 방식(설정 화면의 키, 모델 상수 `MODEL`). 호출부를 공용 함수로 재사용한다.

---

## 7. 완료 기준

1. 트리에 룰이 있는 상태에서 관련 조항을 넣으면 걸린 노드가 하이라이트 + 판정 배지가 뜬다.
2. hard 노드를 어기는 입력 → '위반' 빨강.
3. **트리에 기준이 없는 새 쟁점 입력 → '무관'이 아니라 '보류' 회색 점선.** (이 하나가 오늘의 핵심. 안 되면 실패.)
4. keywords 불일치인데 문언상 관련된 입력도 보류로 잡힌다.
5. 판정 근거에 `path`가 붙어 "어디서 온 기준인지" 추적된다.
6. 코어·입구 코드는 안 바뀌었다(렌즈만 추가됨).

---

## 8. 끝나고 확인할 것 (나에게)

- **3번**만 집요하게 본다. 검토가 아는 것만 잡고 모르는 걸 흘려보내면 법무에서 못 쓴다. 모르는 걸 '보류'로 올리는 게 이 시스템 전체의 신뢰도다.
- 이게 되면 루프가 닫힌다: **넣기(입구) → 저장(트리) → 비추기(렌즈)**, 그리고 md로 확산. CODEX와 RADAR가 한 트리 위 두 방향으로 합쳐진다.
