# 오늘 작업지시서 — 코어(스키마·트리·위키트리) (2026-08-02)

> 먼저 `CLAUDE.md`(마스터 계획서)를 읽는다. 이 지시서는 그 위에 얹는 **오늘 조각 하나**다.
> **방향 전환.** 원래 오늘 조각은 [검토하기] 렌즈였다. 그런데 렌즈의 전제(코어·트리·위키트리)가 **하나도 구현돼 있지 않다.** 작업지시서 규칙("전제가 안 되어 있으면 먼저 그것부터")과 CLAUDE.md 11장(완료기준 1~5=코어, 6=렌즈)에 따라, 오늘은 **코어를 먼저 세운다.** 렌즈는 코어가 선 다음 조각(§9)으로 미룬다.

---

## 0. 현재 빌드 상태 (사실 확인)

지금 존재하는 것은 `codex-mining-tool` 하나 — CLAUDE.md **5.2 계약 마이닝 입구**의 단발성 버전이다.

- **있음:** 설정(키 localStorage `codex_api_key`), 표준안·서명본 입력, Anthropic 직접 호출, 룰 카드·10칸 미터·코디·hold 점선 큐·숫자판, 9장 비주얼 규약.
- **없음(오늘 채운다):**
  - 3장 **공통 스키마 전체.** 현재는 `id / judgment / kind / strength_label / held / total / badge / note / origin`만 있고, `path` · `source` · `status` · `review_trigger` · `renew_trigger` · `created_at` 가 없다.
  - **트리 저장·병합**(`codex_tree`) — 지금은 한 번 캐서 화면에 뿌리고 끝. **지식이 쌓이지 않는다.**
  - **[위키트리] 화면**, **md 내보내기**, **수동 입력 입구**, **탭 구조.**

오늘 목표는 이 단발 도구를 **지식이 남는 아래층(코어)** 으로 바꾸는 것이다.

---

## 1. 목표 (한 문장)

계약 마이닝·수동 입력 어느 쪽으로든 만든 룰이 **3장 스키마 그대로 `codex_tree`에 쌓이고(병합·영속)**, [위키트리]로 보이고, md 한 장으로 떨어진다 — "앱이 바뀌어도 남는 지식 계층"을 실제로 세운다.

---

## 2. 오늘 범위 (이것만)

- 스키마를 3장 전체로 확장(`path` · `status` · `review_trigger` · `source` · `renew_trigger` · `created_at`).
- 코어 저장: `codex_tree` 평면 배열 + `path` 기준 **병합**(4장).
- 기존 계약 마이닝 도구를 **코어에 연결**(결과를 `candidate`로 주입, 프롬프트에 `path`·`review_trigger` 부여).
- **수동 입력 입구**(5.1) 추가.
- **[위키트리]** 화면(path 그룹핑 접이식, 다수 hold 가지 ⚠).
- **md 내보내기**(7장: 헤딩 트리 + 말미 JSON 블록).
- **탭 구조**([지식 넣기] / [위키트리] + 설정). [검토하기] 탭은 자리만(비활성 — 다음 조각).

**오늘 안 하는 것:** [검토하기] 렌즈(§9로 미룸), 다른 입구(규정·자문·인터뷰), 파일 드롭·OCR·hwp, Notion/백엔드.

> 구조 원칙 1(코어↔입구/렌즈 분리)을 오늘부터 지킨다. 마이닝 결과 렌더링 코드와 **코어(스키마·저장·병합·트리·md)를 파일부터 분리**해서, 뒤에 렌즈·입구를 더해도 코어가 안 바뀌게 짠다.

---

## 3. 스키마 확장 — 마이닝/입력 결과를 3장 형태로

CLAUDE.md 3장 스키마를 **그대로** 쓴다. 현재 축소판에서 아래 필드를 채운다.

- `path`: `[대분류, 중분류, 항목]`. **성격으로 묶는다**(정보통제 / 절차·기한 / 책임·분쟁 등). 하드코딩 금지 — 룰마다 부여하고 트리는 여기서 생성.
- `status`: 채굴/입력 직후 `candidate`. 사람이 승인하면 `approved`.
- `source`: 근거 위치(제7조 등). 없으면 `null`.
- `review_trigger`: `{ keywords[], law_group(선택), test }`. **다음 조각(렌즈)의 연결점** — 지금 값을 채워두면 렌즈가 바로 쓴다.
- `renew_trigger`: RADAR 연결점(선택, 규제형 노드만).
- `created_at`: 생성 시각.
- `kind`는 등장 빈도가 아니라 **판단의 성격**이다(3장 규칙 그대로 — 절대 혼동 금지).

**마이닝 프롬프트 보강.** 기존 계약 마이닝 프롬프트(5.2)에 다음을 추가:
`각 룰에 path([대분류,중분류,항목])와 review_trigger.keywords 3~6개, test 한 문장을 부여한다.`

---

## 4. 코어 저장·병합 (4장 그대로)

- localStorage 키 `codex_tree` 에 룰 **평면 배열**로 저장. 새로고침해도 유지.
- **병합:** 같은 `path`의 룰이 이미 있으면 새로 만들지 말고 병합 — `held/total` 합산, `note`·`badge`·`strength_label` 최신값 갱신. 없으면 추가. → 같은 대상을 다시 넣어도 중복 가지가 안 생기고 숫자만 커진다.
- 가지(대분류·중분류)는 **저장하지 않는다.** `path`에서 그때그때 생성.
- 계약서 원본은 저장하지 않는다(추출 텍스트/룰만).

---

## 5. 입구 두 개 ([지식 넣기] 탭)

각 입구는 코어가 정한 3장 스키마를 반환하기만 한다. 입구를 더해도 코어는 안 바뀐다.

### 5.1 수동 입력
폼: `path`(대분류/중분류/항목) + 판단문 + `kind` + note → 스키마로 만들어 코어에 주입(`origin: 수동 입력`, `status: candidate`). 마이닝 없이도 트리가 살아있게 하는 기본 입구.

### 5.2 계약 마이닝 (기존 도구 흡수)
현재 `codex-mining-tool`의 입력·호출·표시를 이 입구로 옮긴다. 결과를 화면에만 뿌리지 말고 **코어에 `candidate`로 주입**(`origin: 계약서 마이닝(diff)`). 채굴 현장(코디 로딩)·관철/양보 배지·hold 점선 큐는 그대로.

향후 입구(규정·자문·인터뷰)는 **'준비 중' 비활성**으로 자리만 노출(확장성 시연).

---

## 6. [위키트리] 화면 + md 내보내기

### 6.1 위키트리
`codex_tree`를 `path`로 그룹핑한 **접이식 트리**. 대분류 > 중분류 > 룰 카드. **다수 hold 가지엔 ⚠ + "표준 미확정".** 룰 카드는 9장 강도색·미터 그대로. 상단에 md 저장 버튼.

### 6.2 md 내보내기 (7장 — 핵심 산출물)
'위키트리 md 저장' → `codex_wikitree_YYYY-MM-DD.md` 다운로드.
- `#` 제목 + 요약(총 규칙 수 / hard·soft·hold 분포) → `##` 대분류 → `###` 중분류(다수 hold면 옆에 ⚠) → 룰은 `- **[HARD] 판단문** \`관철 4/4\`` 아래 근거·origin·status·재검토 트리거를 하위 항목으로.
- **문서 말미 `## 기계 판독용 데이터`에 ```json 으로 전체 룰 배열(스키마 그대로).** 사람은 위를, AI는 이 블록을 읽는다.
- 이 파일이 "앱이 없어져도 지식은 남는다"의 물증이다.

---

## 7. 완료 기준 (= CLAUDE.md 11장의 1~5, 오늘분)

1. 수동 입력·계약 마이닝 어느 쪽으로든 룰이 트리에 실제로 쌓이고, 새로고침해도 남는다.
2. 새 `path`를 쓰면 가지가 자동 생성된다(가지 하드코딩 아님).
3. 같은 대상 재입력/재채굴 시 중복이 아니라 병합(카운트만 증가).
4. 다수 hold 가지에 ⚠가 뜬다.
5. md 저장 시 헤딩 트리 + 말미 JSON 블록이 든 파일이 실제로 떨어진다.

> 11장 6번(검토하기 렌즈)은 오늘 범위가 아니다 — §9 다음 조각.

---

## 8. 끝나고 확인할 것 (나에게)

- **3번(병합)** 을 집요하게 본다. 같은 대상을 다시 넣었을 때 가지가 중복 생성되면 코어가 뷰가 아니라 저장이 돼버린 것 — 구조 원칙 3 위반.
- 코어 코드와 입구 코드가 **파일 단위로 분리**됐는지 본다. 다음 조각(렌즈)을 붙일 때 코어를 안 건드릴 수 있어야 오늘이 성공이다.
- 이게 되면 아래층이 선다. 그다음 렌즈가 얹히면 루프가 닫힌다: **넣기(입구) → 저장(트리) → 비추기(렌즈)**.

---

## 9. 다음 조각 — [검토하기] 렌즈 (코어가 선 뒤에)

> 코어(§1~7)가 완료된 다음 조각. 오늘은 만들지 않는다. 여기 보존해 두는 건, 오늘 채운 `review_trigger`가 바로 이 렌즈의 연결점이기 때문.

**목표.** 새 입력(계약 조항·법령 변경·제안 등 아무 텍스트)을 붙여넣으면, 트리 노드들과 대보고 **위반 / 해당 / 보류 / 무관**으로 판정해 걸린 노드를 트리에서 드러낸다. "저장된 지식이 실제로 활용된다"의 증명.

**판정 로직 (3단).**
```
[새 입력]
 1차 로컬 필터 ── 각 노드 review_trigger.keywords와 입력 매칭 → 후보 노드 추림 (빠름)
 2차 LLM 판정 ── 후보 노드 + 입력을 Anthropic에 보내 노드별 verdict 반환
 3차 보류 안전장치 ── keywords가 하나도 안 걸려도, 입력이 어떤 대분류와 문언상 관련되면 '보류'
```
- 기존 룰에 `review_trigger`가 없으면 `judgment`·`path`·`note`에서 LLM이 즉석 추론하는 폴백(없다고 노드를 건너뛰지 말 것 — 미탐 방지).

**미탐 우선(양보 불가).** 확실치 않으면 무관이 아니라 보류. 트리에 기준 없는 관련 입력은 반드시 보류로 사람에게 올린다. **놓치는 것이 틀리는 것보다 위험하다.**

**화면.** 입력창 + "검토" 버튼 → 코디가 헤드램프로 트리를 비추는 로딩 → 걸린 노드 하이라이트 + 판정 배지(위반=hard색 점멸 / 해당=soft색 / 보류=hold색 점선+"?" / 무관=표시 안 함) + 근거 한 줄 + `path` 표시.

**판정 프롬프트 (고정).**
```
아래는 우리 팀이 쌓아온 판단 기준(트리 노드)들이다. 그리고 새로 들어온 입력이다.
각 기준에 대해 이 입력이 (위반 | 해당 | 무관 | 보류) 중 무엇인지 판정하라.
- 위반: 입력이 이 기준(특히 hard/마지노선)을 어긴다.
- 해당: 입력이 이 기준의 검토 대상에 든다.
- 보류: 관련은 있으나 트리에 확정된 기준이 없다. 억지로 판정하지 말고 보류로 사람에게 올려라.
- 무관: 관계없음.
확실하지 않으면 무관이 아니라 보류를 택하라. 놓치는 것이 틀리는 것보다 위험하다.

JSON 배열만 출력한다:
[{"node_id": "", "verdict": "위반|해당|무관|보류", "why": "한 문장"}]

[트리 기준들] {{후보 노드 배열: id, path, judgment, kind, review_trigger}}
[새 입력] {{입력 텍스트}}
```
- API 호출은 계약 마이닝과 같은 방식(설정 키, 모델 상수 `MODEL`). 호출부를 공용 함수로 재사용.

**렌즈 완료 기준 (= CLAUDE.md 11장 6번).**
1. 관련 조항을 넣으면 걸린 노드 하이라이트 + 판정 배지.
2. hard 노드를 어기는 입력 → '위반' 빨강.
3. **트리에 기준이 없는 새 쟁점 입력 → '무관'이 아니라 '보류' 회색 점선.** (이 하나가 렌즈의 핵심. 안 되면 실패.)
4. keywords 불일치인데 문언상 관련된 입력도 보류로 잡힌다.
5. 판정 근거에 `path`가 붙어 "어디서 온 기준인지" 추적된다.
6. 코어·입구 코드는 안 바뀌었다(렌즈만 추가됨).
