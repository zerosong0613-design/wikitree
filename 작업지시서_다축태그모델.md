# 작업지시서 — 다축 태그 모델 + 축별 보기 (2026-08-02)

> 먼저 `CLAUDE.md v0.2`를 읽는다. 이 지시서는 데이터 모델의 근본을 바꾸는 조각이라 **CLAUDE.md 4장 스키마도 함께 갱신**한다(9장).
> 이번 변경의 본질: **단일 `path`(판단 한 축)에서, 룰 하나에 여러 축 태그를 다는 다축 모델로.**
> 이유: 지식은 계약에서만 나오지 않는다(자문·회의·사고·규제도 원천이다). 축을 고정하면 새 축이 필요할 때마다 구조를 뜯어야 한다. 룰마다 태그를 달면 "보기"는 "어느 태그로 묶느냐"가 되어 축이 무한히 늘어도 구조는 안 바뀐다.

> **불변 원칙 유지:** 트리는 저장이 아니라 뷰(저장은 평면 배열). AI는 초안, 사람이 확정. 닫힌 축은 정해진 목록에서만, 안 맞으면 미분류. 코드에 축 목록을 하드코딩하지 않는다(도메인 중립).

---

## 0. 시작 전 전제

- [ ] 트리가 localStorage(`codex_tree`)에 룰 평면 배열로 저장된다.
- [ ] [카테고리 설정]에서 사람이 정한 분류가 localStorage에 저장된다(기존 `codex_categories`).

**주의:** 지금 룰은 `path`(판단 한 축) 하나만 갖는다. 이번 작업으로 `tags`(다축) + `contract_view`(원천이 계약일 때만)로 확장한다. **기존 룰 호환:** `tags`가 없으면 기존 `path`를 `tags.판단`으로 마이그레이션하고, 나머지 축은 비워둔다(오류 없이 동작).

---

## 1. 목표 (한 문장)

룰 하나에 **판단·원천·의뢰부서·주제** 태그를 달고(닫힌 축 3 + 열린 축 1), 위키 상단 **축 선택**으로 같은 데이터를 판단별/계약서별/부서별/원천별로 돌려본다. 계약이 아닌 자문·회의도 다 들어온다.

---

## 2. 오늘 범위 (이것만)

- 스키마를 `tags`(다축) + `contract_view`로 확장(4장).
- 축 목록을 [카테고리 설정]에서 관리(닫힌 축 3개).
- 입구(검토 추출·마이닝)가 태그를 **자동 제안**, 사람이 확정.
- 위키에 **축 선택 토글**([판단별]/[계약서별]/[부서별]/[원천별]).

**오늘 안 하는 것:** AI 찾기 개편, 관련 룰 링크, 룰 페이지 대개편.

---

## 3. 축 정의 (확정본)

**닫힌 축 3개 + 열린 축 1개.** 닫힌 축은 [카테고리 설정]에서 시드를 불러와 팀이 수정. 코드 하드코딩 금지.

| 축 | 통제 | 시드 목록 |
|---|---|---|
| **판단** | 닫힘 | 정보·보안 / 책임·분쟁 / 계약·거래 / 인사·노무 / 규제·컴플라이언스 / 지식재산 / 조직·지배구조 (각 아래 중분류) |
| **원천** | 닫힘 | 계약 / 자문 / 회의·의사결정 / 사고·분쟁대응 / 규제·법령대응 / 내부규정 |
| **의뢰부서** | 닫힘 | 법무 / 인사 / 재무·세무 / 구매·조달 / 영업 / 연구개발 / IT / 경영지원 |
| **주제** | 열림 | (AI 자유, 통제 없음, 검색·연결용) |

- 판단 축만 대분류·중분류 2단(기존 카테고리 구조 재사용). 원천·의뢰부서는 단일 값.
- **원천의 "규제·법령대응"은 RADAR와 연결되는 자리.** RADAR가 잡은 규제 변경이 이 원천으로 들어온다.

---

## 4. 스키마 확장 (4장 갱신)

기존 `path` 단일 축을 `tags` 다축으로 바꾼다.
```json
{
  "id": "RULE-0001",
  "judgment": "판단 한 문장",
  "kind": "hard | soft | neu | hold",
  "strength_label": "...",
  "held": 4, "total": 4, "badge": "관철 4/4",
  "note": "...",

  "tags": {                                  // ★ 신규 — 다축 태그
    "판단": ["책임·분쟁", "손해배상"],          // 닫힘. [대분류, 중분류]
    "원천": "계약",                            // 닫힘. 단일 값
    "의뢰부서": "구매·조달",                    // 닫힘. 단일 값
    "주제": ["배상", "NDA"]                    // 열림. AI 자유
  },
  "contract_view": {                          // ★ 원천이 "계약"일 때만 채움, 아니면 null
    "contract_type": "NDA(국문)",
    "contract_group": "정보보호 계약",
    "article": "제7조",
    "article_title": "위반시 배상"
  },

  "origin": "검토 추출 | 계약 마이닝(diff) | 수동 입력 | 인터뷰 채굴",
  "source": "...",
  "status": "candidate | approved",
  "authors": [ ... ],       // 기존 유지
  "history": [ ... ],       // 기존 유지
  "provenance": { ... },    // 기존 유지
  "review_trigger": { ... },
  "renew_trigger": "...",
  "created_at": ""
}
```
- **닫힌 축(판단·원천·의뢰부서) 값은 반드시 축 목록 안의 값.** 아니면 코드가 "미분류"로 강제.
- **주제**는 자유 배열.
- `contract_view`는 원천="계약"일 때만. 자문·회의 등은 null(계약서별 보기에 안 나타남).

---

## 5. 입구의 태그 자동 제안 (AI 초안 → 사람 확정)

- 검토 추출·마이닝 AI 프롬프트에 **닫힌 축 목록을 주입**하고 지시:
  ```
  각 판단에 아래 축의 태그를 단다.
  - 판단/원천/의뢰부서: 반드시 아래 목록에서 고른다. 없으면 그 축을 "미분류"로 두고 note에 이유.
  - 주제: 자유롭게 2~4개.
  - 원천이 "계약"이면 contract_view(계약종류·조항)를 채운다. 아니면 null.
  [판단 축] {{...}} [원천 축] {{...}} [의뢰부서 축] {{...}}
  ```
- 검토 추출 3단계(사람이 고르기)에서 **AI가 단 태그를 사람이 확인·수정.** 특히 닫힌 축(부서·원천)이 틀리면 여기서 고친다.
- 반환값의 닫힌 축이 목록에 없으면 코드가 "미분류"로 강제(AI가 어겨도 막는다).

---

## 6. 위키 — 축 선택 보기

- 위키 상단에 **축 토글**: [판단별] [계약서별] [부서별] [원천별]. 기본 = 판단별.
- 각 보기 = 해당 태그로 그룹핑해 목차를 다시 그림. **저장은 그대로, 축만 바꿈.**
  - **판단별:** `tags.판단`으로 그룹핑. 같은 판단을 하나로 묶고, 그 밑에 "어느 원천/계약들에서" + 관철/양보 통계. (집계 뷰)
  - **계약서별:** `원천="계약"`인 룰만, `contract_view`(계약종류>조항)로 그룹핑, 조항 순서 나열. (나열 뷰)
  - **부서별:** `tags.의뢰부서`로 그룹핑. "인사팀이 의뢰한 판단 총람."
  - **원천별:** `tags.원천`으로 그룹핑. "자문에서 나온 판단."
- 목차는 기존대로 **기본 접힘**, 각 그룹에 규칙 수·⚠. 축을 바꿔도 이 규칙 유지.
- **판단별은 집계, 계약서별은 나열** — 성격 차이를 살린다(같은 판단이 여러 계약에서 나오면 판단별에선 묶여 통계로, 계약서별에선 각 계약 조항으로 따로).

---

## 7. [카테고리 설정] 확장

- 기존 판단 카테고리 관리에 더해, **원천·의뢰부서 목록도 여기서 관리**(추가/이름편집/삭제).
- 각 닫힌 축에 "기본 시드 불러오기" 버튼(3장 시드). 시드는 제안일 뿐 편집·삭제 가능.
- 주제는 관리 대상 아님(열린 축).

---

## 8. 빈 상태

- 자문/회의 등 계약이 아닌 판단을 넣으면 → 계약서별 보기엔 안 뜨고, 판단별·부서별·원천별엔 뜬다(정상).
- 계약서별 보기에서 계약 원천 룰이 없으면: "아직 계약에서 추출한 판단이 없어요. [지식 넣기]에서 계약을 넣거나 다른 보기로 전환하세요."

---

## 9. CLAUDE.md 함께 갱신할 것

이 변경은 데이터 모델의 근본이라 헌법에 반영한다:
- **4장 스키마 교체:** `path` 단일 축 → `tags`(판단·원천·의뢰부서·주제) + `contract_view`. 위 4장 그대로.
- **3장 구조 원칙에 추가:** "지식은 계약에서만 나오지 않는다(자문·회의·사고·규제도 원천). 룰은 여러 축 태그를 갖는다. 닫힌 축(판단·원천·의뢰부서)은 사람이 정한 목록에서만, 열린 축(주제)만 AI 자유. 보기는 '어느 태그로 묶느냐'이며, 새 축이 필요하면 태그 종류만 추가한다(구조 불변)."
- **6장 입구 공통 규칙에 추가:** "모든 입구는 닫힌 축 목록을 읽어 그 안에서만 태그를 달고, 사람이 확정한다."
- **10장 화면 구조 갱신:** 위키 상단 축 토글(판단별/계약서별/부서별/원천별), 판단별=집계·계약서별=나열.

---

## 10. 완료 기준

1. 룰이 `tags`(판단·원천·의뢰부서·주제) + (계약이면)`contract_view`를 갖는다. 기존 `path`-only 룰도 오류 없이 마이그레이션된다.
2. 계약이 아닌 판단(예: 노조 자문 = 원천 "자문", 의뢰부서 "인사")을 넣으면 판단별·부서별·원천별엔 보이고 계약서별엔 안 보인다.
3. 축 토글을 바꾸면 같은 룰들이 다른 축으로 재배열된다(데이터 재생성 없음).
4. 판단별 보기에서 같은 판단이 여러 원천에서 나오면 하나로 묶여 통계(관철/양보)로 보인다.
5. 닫힌 축은 [카테고리 설정] 목록 안 값만 나온다. 안 맞으면 미분류.
6. 주제 태그만 AI가 자유롭게 단다.

---

## 11. 끝나고 확인할 것 (나에게)

- **2번**이 이 변경의 심장이다. 계약이 아닌 자문 건이 계약서별엔 안 뜨고 판단·부서·원천 축엔 뜨는지. 이게 되면 "계약 도구"가 "회사 전체 판단 저장소"로 풀린다.
- **4번**(판단별=집계)이 계약서별(나열)과 성격이 다르게 작동하는지. 둘이 똑같이 나열되면 판단별 보기의 쓸모가 죽는다.
- 발표 논거: "계약이든 자문이든 회의든, 회사의 모든 판단을 태그 달아 모으고 부서·주제·원천 어느 각도로든 꺼내 본다. 인사팀은 노조 입장을, 재무팀은 세무 판단을 같은 시스템에 쌓는다."
