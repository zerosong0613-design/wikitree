# 작업지시서 — 카테고리 고정 + 목차 개편 (2026-08-02)

> 먼저 `CLAUDE.md v0.2`를 읽는다. 이 지시서는 그 위에 얹는 **한 조각**이며, 구조를 바꾸는 조각이라 CLAUDE.md도 함께 갱신한다(9장).
> 이번 변경의 본질: **"AI가 분류를 발명한다"에서 "사람이 카테고리를 정하고 AI는 그 안에 배치한다"로.**
> 이유: 분류를 AI가 매번 만들면 트리가 자랄수록 흔들리고 찾기 어려워진다. 카테고리가 고정돼야 지도가 되고, 지도가 있어야 찾아진다(나무위키 원리).

> **표현 원칙: 무조건 직관적으로.** 은유(서랍·뼈대·나무 등)를 라벨·문구에 쓰지 않는다. 사람이 한 번 보고 바로 아는 말만 쓴다. 분류 틀을 정하는 기능의 이름은 **"카테고리 설정"**으로 통일한다.

---

## 0. 시작 전 전제

- [ ] 트리가 localStorage(`codex_tree`)에 룰 평면 배열로 저장된다.
- [ ] 룰이 CLAUDE.md 4장 스키마(path 포함)를 따른다.

**주의:** 지금은 각 입구(마이닝·검토 추출)가 path의 대분류·중분류를 **AI가 자유 생성**한다. 이번 작업으로 그걸 **사람이 정한 카테고리 중 선택**으로 바꾼다. 코어는 안 건드리고(구조 원칙 1), 카테고리 저장소 + 입구의 분류 제약 + 목차 렌더만 손댄다.

---

## 1. 목표 (한 문장)

사람이 카테고리(대분류·중분류)를 **먼저 한 번** 정하면, 이후 모든 입구는 그 카테고리 안에서만 분류하고, 목차는 그 카테고리를 **기본 접힘 지도**로 보여준다.

---

## 2. 오늘 범위 (이것만)

- 카테고리 저장소 신설 + **[카테고리 설정]** 화면.
- 입구(마이닝·검토 추출)가 정해진 카테고리 안에서만 분류하도록 제약(+ 안 맞으면 '미분류').
- 목차를 **기본 접힘**으로 개편 + 입력 폼을 목차에서 분리.

**오늘 안 하는 것:** 관련 룰 링크, AI 찾기 개편, 룰 페이지 대개편. (목차/카테고리만.)

---

## 3. 카테고리 저장소 (신설)

- localStorage 키 `codex_categories`에 저장. 코드에 하드코딩하지 않는다(도메인 중립, 구조 원칙 4).
```json
{
  "version": 1,
  "categories": [
    { "name": "정보통제", "subs": ["기술자료 정의", "제3자 제공", "보안조치"] },
    { "name": "책임·분쟁", "subs": ["배상", "면책", "관할·준거법"] }
  ]
}
```
- 카테고리는 사람만 만들고 고친다. AI·입구는 이걸 **읽기만** 한다.
- "법무 기본 카테고리 불러오기" 프리셋 1개 제공(코드 상수 아님, 버튼을 누르면 위 형태로 `codex_categories`에 심는 시드): 정보통제 / 책임·분쟁 / 절차·기한 / 규제 대응 / 계약 일반 / 분쟁 해결. **프리셋은 제안일 뿐, 사용자가 편집·삭제 가능.**

---

## 4. [카테고리 설정] 화면 (신설)

- 위키 목차 상단의 **[카테고리 설정]** 버튼으로 진입(모달 또는 별 화면).
- 대분류: 추가/이름편집/삭제/순서변경. 각 대분류 아래 중분류 동일.
- 상단 안내(직관적 문구, 은유 없음): **"먼저 카테고리를 정하세요. 앞으로 모든 판단은 이 카테고리 안에 분류됩니다."**
- 카테고리가 비었을 때 "법무 기본 카테고리 불러오기" 버튼 노출.
- 저장 시 `codex_categories` 갱신. 이미 트리에 룰이 있는데 대분류를 지우려 하면 "이 카테고리 안 규칙 N개는 어디로?" 경고(삭제 대신 '미분류'로 이동 옵션).
- **[지식 넣기]와 헷갈리지 않게 역할을 명시:** 버튼/화면 부제에 "판단을 분류할 큰 틀"이라고 붙인다. (지식 넣기 = 실제 판단을 채우기.)

---

## 5. 입구의 분류 제약 (핵심 변경)

- 계약 마이닝·검토 추출의 **AI 프롬프트에 현재 `codex_categories`를 주입**하고 지시한다:
  ```
  아래는 이 팀이 미리 정한 카테고리다. path의 [대분류, 중분류]는 반드시 이 목록에서 고른다. 새 카테고리를 만들지 마라.
  어디에도 맞지 않으면 대분류를 "미분류"로 두고 그 이유를 note에 남겨라.
  [카테고리] {{codex_categories}}
  ```
- 반환된 룰의 대분류·중분류가 카테고리에 없으면, 코드에서 강제로 `path[0]="미분류"` 처리(AI가 어겨도 코어가 막는다).
- **"미분류"** 는 목차 맨 아래 고정. 사람이 여기서 룰을 골라 제대로 된 카테고리로 옮기거나, "새 카테고리가 필요하다" 판단 시 [카테고리 설정]에서 늘린다. → 카테고리 확장도 사람 손.

---

## 6. 목차 개편 (지도로)

- **기본 접힘.** 처음엔 대분류 줄만 보인다(예: 6줄). 각 줄 앞 ▸, 클릭 시 중분류·항목 펼침.
- 대분류 줄: 이름 + 규칙 수(작게) + 다수 hold면 ⚠. **강도색 dot은 잎(항목)에만**, 대분류엔 안 붙인다.
- 스크롤 시 대분류 헤더 sticky.
- **입력 폼을 목차에서 제거.** 지금 목차 상단의 "새 판단 / 대분류·중분류 입력 / AI path 제안"을 뺀다. 수동 입력은 [지식 넣기] 탭으로 이동(거기서도 대분류·중분류는 카테고리에서 선택).
- 목차 상단엔 검색창 + **[카테고리 설정]** 버튼만.

---

## 7. 빈 상태 (직관적 문구)

- `codex_categories`가 없으면 위키 첫 화면: **"먼저 카테고리를 정하세요"** → [카테고리 설정] 유도.
- 카테고리는 있는데 룰이 없으면: **"카테고리는 준비됐어요. [지식 넣기]에서 판단을 채우세요."**

---

## 8. 완료 기준

1. [카테고리 설정]에서 대분류·중분류를 정하면 `codex_categories`에 저장, 목차에 빈 카테고리로 보인다.
2. 계약 마이닝/검토 추출 결과의 대분류·중분류가 **카테고리 목록 안에서만** 나온다(새 분류 발명 안 함).
3. 카테고리에 안 맞는 판단은 "미분류"에 들어가고, 사람이 옮길 수 있다.
4. 목차가 **기본 접힘**(대분류만) 상태로 열린다. 펼쳐야 항목이 보인다.
5. 목차 안에 입력 폼이 없다(보기 전용).
6. 프리셋 "법무 기본 카테고리"는 시드일 뿐, 편집·삭제된다(하드코딩 아님 검증).
7. 화면 어디에도 은유 라벨(서랍·뼈대 등)이 없다. "카테고리 설정"으로 통일돼 있다.

---

## 9. CLAUDE.md 함께 갱신할 것

이 변경은 구조라 헌법에 반영한다. 아래를 CLAUDE.md에 추가:
- **3장 구조 원칙에 항목 추가:** "카테고리(대분류·중분류)는 사람이 먼저 정한다. 코드가 아니라 사용자가(`codex_categories`). AI·입구는 이 카테고리 안에서만 분류하고 새 분류를 발명하지 않는다. 확장도 사람 손. — 이것이 '정리를 위한 정리'와 '찾아지는 지도'를 가르는 선이며, 도메인 중립(코드 하드코딩 금지)과 충돌하지 않는다."
- **6장 입구 공통 규칙에 추가:** "모든 입구는 `codex_categories`를 읽어 그 안에서만 path를 정한다. 안 맞으면 '미분류'."
- **10장 화면 구조 갱신:** 목차 기본 접힘, [카테고리 설정] 진입점(부제 "판단을 분류할 큰 틀"), 입력 폼은 위키에서 분리.
- **표현 규칙 명시:** 라벨·문구는 직관적으로. 은유 금지. 분류 틀 기능은 "카테고리 설정"으로 통일.

---

## 10. 끝나고 확인할 것 (나에게)

- **2번**이 핵심이다. 여러 계약을 연달아 넣어도 대분류가 안 늘어나고 카테고리 안에 정리되는지. 이게 되면 "AI가 만든 폴더 지옥"과 갈라진다.
- 발표 논거: "카테고리는 사람이 정하고 AI가 그 안에 채운다. 그래서 트리가 아무리 자라도 지도는 안 흔들린다. AI에 다 맡기지 않는 게 강점이다."
